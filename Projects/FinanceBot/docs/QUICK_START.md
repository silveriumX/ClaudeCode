# 🚀 Быстрый старт - Payment Management System

## Что изменилось в системе

### ✅ Упрощено
- **"Баланс счетов"** → теперь только крипто-кошельки (без компаний)
- **Фокус** → только на управлении выплатами
- **Минимум полей** → убрали лишнее

### 📊 Новая структура "Баланс счетов"
```
| Кошелек (USDT) | Баланс USDT | Последнее обновление |
```

**Что убрали:**
- ❌ Название компании
- ❌ Баланс RUB (он рассчитывается автоматически при выплате)
- ❌ Общий столбец "Баланс"

**Зачем:** Система теперь фокусируется ТОЛЬКО на выплатах, ничего лишнего!

---

## Тестирование

### 1. Запустить все тесты
```bash
cd Projects/FinanceBot
python run_tests.py
```

**Результат:**
```
✅ Passed:   35
❌ Failed:   0
📈 Overall:  100.0%
```

### 2. Если что-то сломалось в Sheets
```bash
python fix_sheets_structure.py  # Автоисправление
python run_tests.py             # Проверка
```

### 3. Применить упрощение системы
```bash
python refactor_to_payments.py  # Убрать лишнее
python run_tests.py             # Проверка
```

---

## Структура для выплат

### Минимум необходимого:

1. **Пользователи**
   - Кто может создавать заявки (Менеджер)
   - Кто одобряет (Владелец)
   - Кто проводит выплаты (Исполнитель)

2. **Журнал операций**
   - Все заявки с `REQ-` префиксом
   - Статусы: Создана → Одобрена → Оплачена

3. **Баланс счетов** (УПРОЩЕННЫЙ!)
   - Только USDT кошельки
   - Только баланс USDT
   - Без компаний и лишних полей

4. **Лог событий**
   - Автоматическая история действий

---

## Команды для работы

### Проверка статуса бота (VPS)
```bash
python check_status.py
```

### Деплой на VPS
```bash
python deploy_auto.py
```

### Жесткий перезапуск (если конфликты)
```bash
python hard_restart.py
```

---

## Важно! Что НЕ нужно делать вручную

### ❌ В "Баланс счетов" НЕ добавляйте:
- Названия компаний
- Баланс в RUB (рассчитывается автоматически)
- Дополнительные столбцы

### ✅ Достаточно:
- Адрес кошелька USDT
- Текущий баланс USDT
- Дата обновления

---

## Пример использования

### 1. Менеджер создает заявку
```
/new_request
→ Валюта: USDT
→ Способ: Крипта (адрес)
→ Сумма: 100 USDT
→ Адрес: TXzQ7...
→ Назначение: Выплата за дизайн
```

### 2. Владелец одобряет
```
/pending_approvals
→ Выбирает кошелек из "Баланс счетов"
→ Одобряет
```

### 3. Исполнитель проводит выплату
```
/pending_payments
→ Указывает курс USDT/RUB (например, 95.5)
→ Подтверждает выплату
```

### 4. Результат
- ✅ Статус → "Оплачена"
- ✅ Баланс кошелька обновлен
- ✅ Запись в "Лог событий"

---

## Что дальше?

1. **Проверь тесты:** `python run_tests.py` (должно быть 100%)
2. **Попробуй создать заявку** в боте: `/new_request`
3. **Проверь Google Sheets** - все данные должны появиться автоматически

---

**Система готова к работе!** 🎯
