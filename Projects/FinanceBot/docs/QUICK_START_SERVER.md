# 🚀 QUICK START: Проверка деплоя Finance Bot

**Для нового чата: прочитай этот файл первым!**

---

## 📋 Краткая инструкция

### 1. Подключиться к серверу

```bash
ssh root@ваш_хост
```

### 2. Проверить статус бота

```bash
cd /root/FinanceBot/
ps aux | grep bot.py | grep -v grep
```

**Ожидается:** ОДИН процесс
**Если 0 процессов** → бот упал
**Если 2+ процессов** → ДУБЛИ (проблема!)

### 3. Проверить логи

```bash
tail -n 50 /root/FinanceBot/bot.log
```

**Ищем:**
- ✅ "Finance Bot запущен и готов к работе!"
- ❌ Ошибки (ERROR, Exception, Traceback)

### 4. Проверить в Telegram

- Отправить `/start` боту
- Проверить что отвечает
- Проверить "📋 Мои заявки"

---

## ⚠️ Если есть проблемы

### Проблема: Дубли процессов

```bash
# Убить все
pkill -f "python.*bot.py"
sleep 3

# Запустить один раз
cd /root/FinanceBot/
nohup python3 bot.py > bot.log 2>&1 &
```

### Проблема: Бот не работает

```bash
# Перезапустить
pkill -f "python.*bot.py"
sleep 3
cd /root/FinanceBot/
git pull origin main
nohup python3 bot.py > bot.log 2>&1 &

# Проверить логи
tail -f bot.log
```

### Проблема: "Conflict: Terminated by other getUpdates"

**Это = ДУБЛЬ процесса!**

```bash
pkill -f "python.*bot.py"
sleep 3
ps aux | grep bot.py  # Проверить что НЕТ процессов
cd /root/FinanceBot/
nohup python3 bot.py > bot.log 2>&1 &
```

---

## 📚 Полная документация

См. `docs/SERVER_OPERATIONS_GUIDE.md` для:
- Детальная диагностика
- Частые ошибки и решения
- Checklist деплоя
- Работа с логами
- Откат изменений

---

## 🔍 Быстрая диагностика

```bash
# Одна команда - вся информация
/root/FinanceBot/diagnose.sh
```

Или вручную:

```bash
# 1. Процесс
ps aux | grep bot.py | grep -v grep | wc -l

# 2. Логи
tail -n 30 bot.log

# 3. Ошибки
grep -i "error" bot.log | tail -10
```

---

## ✅ Checklist последнего деплоя

**Дата:** 30.01.2026
**Изменения:** Улучшена функция "Мои заявки"

Проверить:
- [ ] Бот работает (1 процесс)
- [ ] Нет ошибок в логах
- [ ] "📋 Мои заявки" открывается
- [ ] Заявки отсортированы (новые сверху)
- [ ] Формат кнопок: `дата • сумма • получатель`
- [ ] Пагинация работает (если >10 заявок)
- [ ] Навигация ◀️/▶️ работает
- [ ] Возврат на правильную страницу работает

---

**Быстрые команды:**

```bash
# Статус
ps aux | grep bot.py

# Перезапуск
pkill -f "python.*bot.py" && sleep 3 && cd /root/FinanceBot && nohup python3 bot.py > bot.log 2>&1 &

# Логи
tail -f /root/FinanceBot/bot.log
```
