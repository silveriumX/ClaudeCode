# Миграция таблицы к версии 3.0

## ⚠️ ВАЖНО: Перед миграцией

1. **Сделайте резервную копию таблицы:**
   - Откройте Google Таблицу
   - Файл → Создать копию
   - Сохраните ссылку на копию

2. **Остановите бота** (если запущен)

3. **Никто не должен работать с таблицей** во время миграции

---

## 🚀 Запуск миграции

### Шаг 1: Перейдите в папку проекта

```bash
cd C:\Users\Admin\Documents\Cursor\Projects\FinanceBot
```

### Шаг 2: Запустите скрипт миграции

```bash
python migrate_to_v3.py
```

### Шаг 3: Подтвердите миграцию

Скрипт спросит: `Продолжить миграцию? (yes/no):`

Введите: `yes`

---

## 📊 Что делает скрипт

1. **Добавляет колонку A "ID заявки"** во все листы
2. **Добавляет колонку C "Валюта"** в листы RUB/BYN
3. **Генерирует уникальные ID** для всех существующих заявок
4. **Заполняет валюту** для всех существующих заявок:
   - Лист "Основные": RUB
   - Лист "Разные выплаты": BYN
   - Лист "USDT": без валюты
5. **Обновляет формулы** для колонки "Реквизиты" (теперь H вместо F)

---

## ✅ После миграции

### 1. Проверьте таблицу:

- Откройте Google Таблицу
- Проверьте что появилась колонка **A "ID заявки"**
- Проверьте что появилась колонка **C "Валюта"** (для RUB/BYN)
- Проверьте что все заявки имеют ID (формат: REQ-20260130-143025-A1B2C3D4)
- Проверьте что валюта заполнена (RUB/BYN)

### 2. Запустите бота:

```bash
python bot.py
```

### 3. Протестируйте:

- Создайте новую заявку → проверьте ID в таблице
- Отредактируйте реквизиты → проверьте что меняется правильная строка
- Попробуйте отменить заявку

---

## 🐛 Если что-то пошло не так

### Восстановление из резервной копии:

1. Откройте резервную копию
2. Скопируйте ID таблицы из URL
3. Обновите `.env`:
   ```
   GOOGLE_SHEETS_ID=id_резервной_копии
   ```
4. Перезапустите бота

### Повторный запуск миграции:

Скрипт автоматически пропустит листы, которые уже имеют новую структуру (проверяет наличие колонки "ID заявки").

---

## 📝 Примечания

- Миграция занимает **1-2 минуты** в зависимости от количества заявок
- Скрипт показывает прогресс для каждых 10 обработанных строк
- Все изменения происходят в Google Sheets через API
- Скрипт безопасен для повторного запуска

---

## 🎯 Альтернатива: Создать новую таблицу

Если не хотите рисковать со старой таблицей:

1. Создайте новую Google Таблицу
2. Добавьте 3 листа с новыми заголовками (см. `QUICK_START_V3.md`)
3. Обновите `.env` с новым ID таблицы
4. Запустите бота - он начнёт работать с новой таблицей

---

**Дата:** 30.01.2026
**Версия:** 3.0
