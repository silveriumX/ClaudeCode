# Быстрый старт: Обновление до версии 3.0

## 🚀 Что нужно сделать

### Шаг 1: Обновить структуру Google Sheets

#### Вариант А: Новая таблица (рекомендуется)

1. Создайте новую Google Таблицу
2. Добавьте листы с заголовками:

**Лист "Основные" (19 колонок):**
```
ID заявки | Дата | Валюта | Сумма | Получатель | Номер карты/телефона | Банк | Реквизиты | Назначение | Категория | Статус | ID сделки | Название аккаунта | Сумма USDT | Курс | Исполнитель | Telegram ID инициатора | Username инициатора | Полное имя инициатора
```

**Лист "Разные выплаты" (19 колонок):**
```
(То же самое, что "Основные")
```

**Лист "USDT" (13 колонок):**
```
ID заявки | Дата | Сумма USDT | Адрес кошелька | Назначение | Категория | Статус | ID транзакции | Название аккаунта | Исполнитель | Telegram ID инициатора | Username инициатора | Полное имя инициатора
```

3. Скопируйте ID новой таблицы
4. Обновите `.env`:
   ```
   GOOGLE_SHEETS_ID=ваш_новый_id
   ```

#### Вариант Б: Обновить существующую таблицу

⚠️ **ВАЖНО: Сначала сделайте резервную копию!**

1. Добавьте колонку **A** (ID заявки) во все 3 листа
2. Добавьте колонку **C** (Валюта) в листы "Основные" и "Разные выплаты"
3. Все остальные колонки сдвинутся вправо автоматически
4. Обновите формулы в колонке "Реквизиты" (теперь H вместо F):
   ```
   =IF(OR(ISBLANK(E2),ISBLANK(F2),ISBLANK(G2)),"",F2&CHAR(10)&G2&CHAR(10)&E2)
   ```
5. Заполните колонку "Валюта" для существующих заявок:
   - Лист "Основные": RUB
   - Лист "Разные выплаты": BYN

### Шаг 2: Обновить код бота

Код уже обновлён! Просто перезапустите бота:

```bash
python Projects/FinanceBot/bot.py
```

### Шаг 3: Проверить работу

1. Создайте тестовую заявку
2. Проверьте что в таблице появился ID заявки
3. Проверьте что валюта сохраняется
4. Попробуйте отредактировать заявку
5. Попробуйте отменить заявку

---

## ✅ Что работает

- ✅ Создание заявок с уникальным ID
- ✅ Отображение заявок в списке "Мои заявки"
- ✅ Редактирование реквизитов
- ✅ Отмена заявок
- ✅ Оплата заявок
- ✅ Поддержка валют RUB, BYN, USDT

---

## 🐛 Если что-то не работает

### Проблема: "Ошибка подключения к системе"

**Решение:**
- Проверьте `.env` (GOOGLE_SHEETS_ID, TELEGRAM_BOT_TOKEN)
- Проверьте `service_account.json`
- Проверьте права доступа к таблице

### Проблема: "Заявка не найдена"

**Решение:**
- Проверьте структуру таблицы (19/13 колонок)
- Проверьте заголовки листов (точное совпадение)
- Посмотрите логи бота

### Проблема: "Реквизиты не обновляются"

**Решение:**
- Проверьте формулу в колонке H (Реквизиты)
- Формула должна быть: `=IF(OR(ISBLANK(E2),ISBLANK(F2),ISBLANK(G2)),"",F2&CHAR(10)&G2&CHAR(10)&E2)`
- Скопируйте формулу на все строки

### Проблема: "Редактирование меняет неправильную заявку"

**Решение:**
- Убедитесь что колонка A (ID заявки) заполнена
- Убедитесь что ID уникальны
- Посмотрите логи с `[DEBUG]` для отладки

---

## 📊 Логи для отладки

Бот выводит подробные логи:

```
[DEBUG] update_request_fields: date=30.01.2026, amount=50000.0, currency=RUB
[DEBUG] Обновляемые поля: purpose=Зарплата
[DEBUG] Найдена заявка: request_id=REQ-20260130-143025-A1B2C3D4, sheet=Основные
[DEBUG] Найдена строка: row_number=3
[DEBUG] Обновлено назначение: Зарплата
OK: Polja obnovleny: 30.01.2026, 50000.0, RUB
```

Если видите `ERROR` - ищите причину в логах.

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи бота
2. Проверьте структуру таблицы
3. Посмотрите документацию: `docs/NEW_STRUCTURE_V3.md`

---

**Дата:** 30.01.2026
**Версия:** 3.0
