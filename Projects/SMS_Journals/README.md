# SMS Journals - Скрипты получения SMS через API

Проект содержит скрипты Google Apps Script для получения SMS и кодов подтверждения через различные SMS API сервисы.

## Структура проекта

```
SMS_Journals/
├── MTT/
│   └── mtt_sms.js          # Скрипт для API МТТ (webhook-based)
├── FlowSMS/
│   └── flow_sms.js         # Скрипт для Flow SMS API (polling-based)
└── README.md
```

---

## Сравнение скриптов

| Характеристика | MTT (`mtt_sms.js`) | Flow SMS (`flow_sms.js`) |
|----------------|---------------------|--------------------------|
| **Тип API** | REST API с Bearer токеном | REST API с api_key |
| **Получение SMS** | Webhook (push) | Polling (pull) |
| **Авторизация** | `Authorization: Bearer TOKEN` | `?api_key=KEY` в URL |
| **База URL** | `https://api.mtt.ru/ms-customer-gateway` | Настраиваемый |
| **Аренда номеров** | Нет (свои номера) | Да (временные номера) |
| **Автообновление** | Каждый час (триггер) | Каждые N минут |

---

## 1. MTT SMS (`MTT/mtt_sms.js`)

### Назначение
Журнал входящих SMS для **собственных номеров** через API МТТ. Использует webhook для мгновенного получения SMS.

### Настройка в Google Sheets

#### Шаг 1: Создать таблицу
1. Создайте новую Google Таблицу
2. Откройте **Расширения → Apps Script**
3. Скопируйте содержимое `mtt_sms.js` в редактор
4. Сохраните (Ctrl+S)

#### Шаг 2: Настроить Script Properties
1. В Apps Script: **Project Settings** (шестерёнка слева)
2. Прокрутите до **Script Properties**
3. Добавьте свойства:

| Свойство | Обязательно | Описание |
|----------|-------------|----------|
| `MSAPI_TOKEN` | ✅ | Bearer токен от МТТ |
| `WEBHOOK_SECRET` | ✅ | Секретный ключ для защиты webhook |
| `CUSTOMER_NAME` | ⚠️ | Лицевой счёт (нужен для истории) |
| `MTT_BASE` | ❌ | База URL (по умолчанию: `https://api.mtt.ru/ms-customer-gateway`) |

**Пример:**
```
MSAPI_TOKEN      = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
WEBHOOK_SECRET   = my-secret-key-12345
CUSTOMER_NAME    = ООО Компания
```

#### Шаг 3: Опубликовать Web App
1. **Deploy → New deployment**
2. Тип: **Web app**
3. Execute as: **Me**
4. Who has access: **Anyone** (или Anyone with the link)
5. **Deploy**
6. Скопируйте URL — это ваш webhook

#### Шаг 4: Подключить номера
1. Обновите страницу таблицы
2. Появится меню **📲 СМС**
3. Выберите **🔁 Получить список номеров (МТТ)**
4. Затем **🧩 Подключить уведомления на номерах (MTT)**

### Листы в таблице

| Лист | Описание |
|------|----------|
| `Номера` | Список номеров с MTT и статусы webhook |
| `Журнал СМС` | Входящие SMS (дата, получатель, отправитель, текст) |
| `Webhook RAW` | Сырые данные запросов (если включено) |

### Функции меню

| Пункт меню | Функция |
|------------|---------|
| 🔁 Получить список номеров | Загружает все номера с аккаунта МТТ |
| 🧩 Подключить уведомления | Регистрирует webhook на каждом номере |
| ▶️ Продолжить подключение | Продолжает с места остановки (если таймаут) |
| ⬇ Загрузить историю | Импортирует историю SMS из МТТ |
| 🔗 Показать URL веб-хука | Показывает URL для диагностики |
| 🔍 Диагностика веб-хука | Тестирует работу webhook |
| 🧪 Тестовая запись | Добавляет тестовое SMS в журнал |

---

## 2. Flow SMS (`FlowSMS/flow_sms.js`)

### Назначение
Получение **временных номеров** для приёма кодов подтверждения (активации). Работает через polling — скрипт периодически проверяет статус.

### Настройка в Google Sheets

#### Шаг 1: Создать таблицу
1. Создайте новую Google Таблицу
2. Откройте **Расширения → Apps Script**
3. Скопируйте содержимое `flow_sms.js` в редактор
4. Сохраните (Ctrl+S)

#### Шаг 2: Настроить Script Properties
1. В Apps Script: **Project Settings** (шестерёнка слева)
2. **Script Properties** → Add

| Свойство | Обязательно | Описание |
|----------|-------------|----------|
| `API_KEY` | ✅ | Ключ API от сервиса |
| `API_BASE_URL` | ❌ | URL API (по умолчанию настроен) |
| `DEFAULT_SERVICE` | ❌ | Код сервиса (по умолчанию: `oz`) |
| `DEFAULT_COUNTRY` | ❌ | Код страны (по умолчанию: `0` = Россия) |
| `POLL_MINUTES` | ❌ | Интервал автопроверки (по умолчанию: `1`) |

**Пример:**
```
API_KEY          = abc123def456
API_BASE_URL     = http://65.109.64.76:8011/stubs/handler_api.php
DEFAULT_SERVICE  = oz
DEFAULT_COUNTRY  = 0
POLL_MINUTES     = 1
```

#### Шаг 3: Использование
1. Обновите страницу таблицы
2. Появится меню **📲 СМС**
3. Настройки также доступны через **⚙️ Настройки** в меню

### Листы в таблице

| Лист | Описание |
|------|----------|
| `Номера` | Арендованные номера (ID, номер, сервис, статус, код) |
| `Журнал СМС` | Полученные коды (дата, номер, сервис, код, текст, ID) |
| `Страны` | Список доступных стран (после загрузки) |
| `API Диагностика` | Логи запросов (после диагностики) |

### Функции меню

| Пункт меню | Функция |
|------------|---------|
| 📞 Получить новый номер | Арендует номер для сервиса по умолчанию |
| 📞 Получить номер (выбор) | Арендует номер с выбором сервиса и страны |
| 📥 Проверить СМС | Проверяет все активные номера |
| ✅ Подтвердить активацию | Завершает активацию (статус 6) |
| ❌ Отменить активацию | Отменяет (статус -1) |
| 🔁 Запросить ещё СМС | Запрашивает повторную SMS (статус 3) |
| 💰 Баланс | Показывает баланс аккаунта |
| 🌍 Список стран | Загружает доступные страны |
| ⏱️ Автопроверка ВКЛ | Включает автоматическую проверку |
| ⏹️ Автопроверка ВЫКЛ | Выключает автопроверку |
| ⚙️ Настройки | Открывает диалог настроек |

### Статусы активации

| Код | Статус | Описание |
|-----|--------|----------|
| - | Ожидание СМС | Номер получен, ждём SMS |
| - | Код получен | SMS пришло, код извлечён |
| 6 | Завершено | Активация подтверждена |
| -1 | Отменено | Активация отменена |
| 3 | Ожидание повторной | Запрошена повторная SMS |

---

## Получение API ключей

### МТТ
1. Войдите в личный кабинет МТТ
2. Раздел API / Интеграции
3. Создайте Bearer токен

### Flow SMS
1. Зарегистрируйтесь на сервисе
2. В профиле скопируйте API Key

---

## Troubleshooting

### MTT: "Web App ещё не опубликован"
- Откройте Apps Script → Deploy → New deployment → Web app

### MTT: Webhook не работает
1. Проверьте, что Web App доступен **Anyone**
2. Используйте **🔍 Диагностика веб-хука**
3. Проверьте `WEBHOOK_SECRET` совпадает в настройках и URL

### Flow SMS: "Не задан API_KEY"
1. Project Settings → Script Properties
2. Добавьте `API_KEY` со значением вашего ключа

### Flow SMS: "Нет свободных номеров"
- Попробуйте другой сервис или страну
- Проверьте баланс

### Общее: Скрипт не появляется в меню
1. Обновите страницу таблицы (F5)
2. Подождите несколько секунд
3. Если не появилось — запустите `onOpen()` вручную из редактора

---

## Файлы

- `MTT/mtt_sms.js` — скрипт для МТТ (666 строк)
- `FlowSMS/flow_sms.js` — скрипт для Flow SMS (811 строк)

---

*Последнее обновление: Январь 2026*
