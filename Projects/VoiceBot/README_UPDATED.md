# Бот-транскрибатор голосовых сообщений

## 🎯 Возможности

### Работа в личных сообщениях
- **Голосовые сообщения** → автоматическая транскрибация через Whisper API
- **Текстовые сообщения** → сохранение с подтверждением "Сохранено"
- Любой пользователь может использовать бота (без ограничений)

### Работа в групповых чатах
- Добавьте бота в любой групповой чат
- Бот реагирует **только на голосовые сообщения**
- Автоматически транскрибирует голосовые в ответ на сообщение
- Игнорирует все текстовые сообщения в группах

## 📁 Структура хранения файлов

Все транскрибации сохраняются в структурированном виде:

```
Транскрибации/
├── Мои/                          # Твои личные сообщения
│   ├── 2026-01-19.md
│   └── 2026-01-20.md
├── Другие/                       # Личные сообщения других пользователей
│   ├── username1/
│   │   ├── 2026-01-19.md
│   │   └── 2026-01-20.md
│   └── user_123456/             # Если нет username, используется ID
│       └── 2026-01-19.md
└── Чаты/                        # Групповые чаты
    ├── Название чата 1/
    │   ├── 2026-01-19.md
    │   └── 2026-01-20.md
    └── Название чата 2/
        └── 2026-01-19.md
```

### Формат файлов

Каждый день = отдельный файл с разбивкой по времени:

```markdown
# Транскрибации за 19.01.2026

### 14:30 — @username
Текст первой транскрибации...

### 15:45 — @username
Текст второй транскрибации...

### 16:20
Текст третьей транскрибации (если нет username)...
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd Projects/VoiceBot
pip install -r requirements.txt
```

### 2. Запуск локально (Windows)

```bash
python bot.py
```

Файлы сохраняются в: `C:\Users\Admin\Documents\Cursor\Транскрибации\`

### 3. Запуск на сервере (Linux)

```bash
python bot_server.py
```

Файлы сохраняются в: `~/transcriptions/`

## 📱 Использование

### В личных сообщениях

1. Найди бота в Telegram
2. Отправь `/start` для приветствия
3. Отправь **голосовое** → получишь транскрибацию
4. Отправь **текст** → получишь "Сохранено"

**Команды:**
- `/today` — показать записи за сегодня
- `/file` — получить файл за сегодня

### В групповых чатах

1. Добавь бота в группу (права администратора **не требуются**)
2. Отправляй голосовые сообщения
3. Бот автоматически ответит транскрибацией
4. Все текстовые сообщения игнорируются

## 🔧 Настройки

В файлах `bot.py` и `bot_server.py`:

```python
TELEGRAM_TOKEN = "..."  # Токен твоего бота от @BotFather
OPENAI_API_KEY = "..."  # API ключ от OpenAI
MY_USER_IDS = [963129618, 8127547204]  # Твои Telegram ID (для папки "Мои")
```

### Изменение путей сохранения

**Локальная версия** (`bot.py`):
```python
BASE_DIR = Path(r"C:\Users\Admin\Documents\Cursor\Транскрибации")
```

**Серверная версия** (`bot_server.py`):
```python
BASE_DIR = Path.home() / "transcriptions"
```

## 🎨 Особенности поведения

### Ответы бота

| Тип сообщения | Контекст | Ответ бота |
|---------------|----------|------------|
| Голосовое | Личка | Транскрибация (без "Записано") |
| Голосовое | Группа | Транскрибация (ответ на сообщение) |
| Текст | Личка | "Сохранено" |
| Текст | Группа | Нет реакции |
| Команда | Личка | Выполняет команду |
| Команда | Группа | Игнорирует |

### Индикатор процесса

- **В личке:** показывается "🎤 Транскрибирую...", потом удаляется
- **В группе:** сразу отправляется результат

## 🔐 Безопасность

- Токены хранятся в коде (для production используй переменные окружения)
- Временные аудио-файлы удаляются после обработки
- Каждый пользователь видит только свои транскрибации через команды

## 📝 Примеры использования

### Сценарий 1: Личные заметки
```
Ты → [голосовое: "Не забыть купить молоко"]
Бот → "Не забыть купить молоко"
```

### Сценарий 2: Текстовые заметки
```
Ты → "Встреча в 15:00"
Бот → "Сохранено"
```

### Сценарий 3: Групповой чат
```
Участник → [голосовое в группе]
Бот → [ответ на сообщение] "Транскрибация текста..."
Участник → "Текстовое сообщение"
Бот → [нет реакции]
```

## 🛠️ Развертывание на сервере

### Создание systemd службы

См. файл `voice_bot.service` для примера.

### Синхронизация файлов

См. файл `sync_thoughts.ps1` для автоматической синхронизации с локальной машиной.

## ⚠️ Требования

- Python 3.8+
- aiogram 3.3.0+
- openai 1.12.0+
- Telegram Bot Token
- OpenAI API Key

## 📞 Поддержка

Если возникли проблемы:
1. Проверь токены в настройках
2. Убедись что у OpenAI есть баланс
3. Проверь права доступа к папкам для сохранения
4. Посмотри логи бота в терминале
