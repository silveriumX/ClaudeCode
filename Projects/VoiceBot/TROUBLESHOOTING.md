# 🔧 Бот не работает в группе - Решение проблем

## Проблема: Бот не реагирует на голосовые в группе

### ✅ Решение 1: Настройки приватности бота в BotFather

**ЭТО САМАЯ ЧАСТАЯ ПРИЧИНА!**

Боту нужно разрешить читать **все сообщения** в группе (по умолчанию боты видят только команды).

#### Шаги:

1. **Открой Telegram** и найди `@BotFather`
2. **Отправь:** `/mybots`
3. **Выбери своего бота** из списка
4. **Нажми:** `Bot Settings`
5. **Нажми:** `Group Privacy`
6. **Нажми:** `Turn off` (или `Disable`)

Должно появиться сообщение: **"Privacy mode is disabled"**

#### Важно:
- После изменения настройки **удали бота из группы и добавь заново**
- Только тогда изменения вступят в силу

---

### ✅ Решение 2: Проверь что бот обновлен на сервере

Подключись к серверу и проверь:

```bash
ssh root@195.177.94.189

# Проверь что бот запущен
systemctl status voice_bot

# Посмотри логи
journalctl -u voice_bot -n 30 --no-pager

# Проверь версию кода (должен быть MY_USER_IDS)
grep "MY_USER_IDS" ~/voice_bot/bot.py
```

Если `MY_USER_IDS` не найден - бот еще не обновлен, запусти команды из `DEPLOY_AUTO.txt`

---

### ✅ Решение 3: Убедись что используешь правильного бота

У тебя теперь **2 бота**:

| Бот | Токен | Для кого | Работает в группах? |
|-----|-------|----------|---------------------|
| Личный | ...I0s | Ты + друзья | ✅ Да (после обновления) |
| Публичный | ...ebI | Все | ✅ Да |

**Проверь:** в какую группу ты добавил бота?

---

### ✅ Решение 4: Права бота в группе

Боту **НЕ нужны** права администратора, но нужно:

1. **Privacy mode = OFF** (см. Решение 1)
2. Бот должен быть **участником группы** (не забанен)

---

## 🔍 Диагностика

### Проверь в PuTTY:

```bash
# Подключись
ssh root@195.177.94.189

# Смотри логи в реальном времени
journalctl -u voice_bot -f

# Теперь отправь голосовое в группу
# Ты должен увидеть активность в логах
```

**Если в логах ничего не появилось** → проблема в настройках BotFather (Privacy mode)

**Если видишь ошибку** → скинь текст ошибки

---

## 📱 Тест в Telegram

### После изменения Privacy mode:

1. **Удали бота** из группы
2. **Добавь бота** в группу заново
3. **Отправь голосовое**
4. Бот должен ответить транскрибацией

### Для публичного бота:

То же самое - проверь Privacy mode в BotFather для **этого бота**.

---

## 🆘 Если всё равно не работает

### Проверь какой бот у тебя запущен:

```bash
ssh root@195.177.94.189

# Проверь оба бота
systemctl status voice_bot
systemctl status voice_bot_public

# Посмотри токены
head -15 ~/voice_bot/bot.py | grep TOKEN
head -15 ~/voice_bot_public/bot.py | grep TOKEN
```

### Убедись что добавил правильного бота в группу:

- Токен `8526424039:...I0s` → @ИмяТвоегоЛичногоБота
- Токен `8351306990:...ebI` → @ИмяПубличногоБота

---

## ✅ Чеклист решения

- [ ] Открыл @BotFather
- [ ] Выбрал бота → Bot Settings → Group Privacy → Turn off
- [ ] Удалил бота из группы
- [ ] Добавил бота в группу заново
- [ ] Проверил что бот запущен на сервере
- [ ] Отправил голосовое в группу
- [ ] Бот ответил ✅

---

## 💡 Подсказка

**90% случаев** - это просто Privacy mode = ON в BotFather.

Измени настройку, удали и добавь бота заново - должно заработать!
