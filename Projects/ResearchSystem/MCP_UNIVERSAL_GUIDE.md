# 🔌 Универсальное руководство по MCP
## Как настроить любой MCP сервер

> **Цель:** Понять, что такое MCP и как настроить любой MCP сервер самостоятельно

**Дата создания:** 28.01.2026

---

## 📋 Содержание

1. [Что такое MCP](#что-такое-mcp)
2. [Где найти MCP серверы](#где-найти-mcp-серверы)
3. [Как установить MCP сервер](#как-установить-mcp-сервер)
4. [Примеры конфигураций](#примеры-конфигураций)
5. [Troubleshooting](#troubleshooting)
6. [Полезные ссылки](#полезные-ссылки)

---

## 🎯 Что такое MCP

### Определение

**MCP (Model Context Protocol)** - это протокол для подключения внешних инструментов к AI. Позволяет AI использовать внешние сервисы, API, базы данных и другие инструменты.

### Простыми словами

Представьте, что AI - это человек, который умеет думать, но не умеет пользоваться инструментами. MCP - это способ "дать ему инструменты". Например:
- Поиск в интернете (EXA)
- Работа с файлами (Filesystem)
- Парсинг веб-страниц (Firecrawl)
- Работа с Git (Git server)
- И многое другое

### Как это работает

```
┌─────────────┐                              ┌──────────────┐
│   Cursor    │         MCP Protocol         │  MCP Server   │
│     AI      │ ◄──────────────────────────► │  (например,   │
│             │                              │     EXA)      │
└─────────────┘                              └──────────────┘
      │                                              │
      │  1. AI просит: "Найди информацию о X"      │
      │─────────────────────────────────────────────►│
      │                                              │
      │  2. Сервер выполняет запрос                  │
      │     (например, поиск через EXA API)        │
      │                                              │
      │  3. Сервер возвращает результаты            │
      │◄─────────────────────────────────────────────│
      │                                              │
      │  4. AI использует результаты для ответа     │
```

### Зачем это нужно

**Без MCP:**
- AI может только думать и отвечать на основе своих знаний
- Не может искать в интернете
- Не может работать с файлами
- Не может использовать внешние API

**С MCP:**
- AI может использовать любые инструменты
- Может искать в интернете
- Может работать с файлами
- Может использовать внешние API
- Может делать практически всё, что может программа

---

## 🔍 Где найти MCP серверы

### Официальные источники

#### 1. GitHub репозиторий (основной)
**URL:** https://github.com/modelcontextprotocol/servers

**Что там:**
- Официальные референсные серверы
- Примеры реализации
- Документация по протоколу
- Активно поддерживается

**Основные серверы:**
- `@modelcontextprotocol/server-filesystem` - работа с файлами
- `@modelcontextprotocol/server-exa` - поиск через EXA
- `@modelcontextprotocol/server-firecrawl` - парсинг веб-страниц
- `@modelcontextprotocol/server-git` - работа с Git
- И многие другие

#### 2. Официальный сайт
**URL:** https://modelcontextprotocol.io

**Что там:**
- Документация по протоколу
- Примеры использования
- Список серверов
- Руководства

#### 3. Примеры серверов
**URL:** https://modelcontextprotocol.io/examples

**Что там:**
- Готовые примеры серверов
- Разные типы (npm, python, и т.д.)
- Код для изучения

### Как искать серверы

1. **На GitHub:**
   - Идите на https://github.com/modelcontextprotocol/servers
   - Ищите в списке или используйте поиск
   - Смотрите README каждого сервера

2. **По названию:**
   - Если знаете название (например, "exa"), ищите `@modelcontextprotocol/server-exa`
   - Или просто "mcp server exa" в поиске

3. **По функциональности:**
   - Нужен поиск? → EXA, Brave, и т.д.
   - Нужна работа с файлами? → Filesystem
   - Нужен парсинг? → Firecrawl
   - И т.д.

---

## 🛠️ Как установить MCP сервер

### Шаг 1: Найти нужный сервер

Найдите сервер на GitHub или официальном сайте. Например:
- EXA: `@modelcontextprotocol/server-exa`
- Filesystem: `@modelcontextprotocol/server-filesystem`
- Firecrawl: `@modelcontextprotocol/server-firecrawl`

### Шаг 2: Понять тип сервера

Серверы бывают разных типов:

#### Тип 1: NPM пакет
```json
{
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-exa"]
}
```

#### Тип 2: Python скрипт
```json
{
  "command": "python",
  "args": ["path/to/server.py"]
}
```

#### Тип 3: Другой язык
```json
{
  "command": "node",
  "args": ["path/to/server.js"]
}
```

### Шаг 3: Добавить в конфигурацию

Откройте файл `.cursor/mcp.json` (или создайте, если его нет).

**Структура файла:**
```json
{
  "mcpServers": {
    "название-сервера": {
      "command": "команда-запуска",
      "args": ["аргументы"],
      "env": {
        "ПЕРЕМЕННАЯ": "значение"
      }
    }
  }
}
```

**Пример для EXA:**
```json
{
  "mcpServers": {
    "exa": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-exa"
      ],
      "env": {
        "EXA_API_KEY": "${env:EXA_API_KEY}"
      }
    }
  }
}
```

### Шаг 4: Настроить переменные окружения

Если серверу нужны API ключи или другие переменные:

**Вариант 1: Прямо в mcp.json**
```json
{
  "env": {
    "EXA_API_KEY": "ваш_ключ_здесь"
  }
}
```

**Вариант 2: Через .env файл (рекомендуется)**
```json
{
  "env": {
    "EXA_API_KEY": "${env:EXA_API_KEY}"
  }
}
```

И создайте `.env` файл:
```
EXA_API_KEY=ваш_ключ_здесь
```

### Шаг 5: Перезапустить Cursor

**Важно:** После изменения `mcp.json` нужно перезапустить Cursor, чтобы изменения вступили в силу.

### Шаг 6: Проверить работу

После перезапуска:
1. Откройте чат с AI
2. Посмотрите в список "Available Tools"
3. Там должны появиться инструменты нового сервера

Или спросите AI:
```
Какие MCP серверы доступны?
```

---

## 📝 Примеры конфигураций

### Пример 1: EXA (NPM пакет)

```json
{
  "mcpServers": {
    "exa": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-exa"
      ],
      "env": {
        "EXA_API_KEY": "${env:EXA_API_KEY}"
      }
    }
  }
}
```

**Что нужно:**
- Node.js установлен
- API ключ EXA в `.env` файле

### Пример 2: Firecrawl (NPM пакет)

```json
{
  "mcpServers": {
    "firecrawl": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-firecrawl"
      ],
      "env": {
        "FIRECRAWL_API_KEY": "${env:FIRECRAWL_API_KEY}"
      }
    }
  }
}
```

### Пример 3: Filesystem (уже установлен)

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}",
        "${userHome}"
      ],
      "env": {}
    }
  }
}
```

### Пример 4: Python сервер (custom)

```json
{
  "mcpServers": {
    "document-processor": {
      "command": "python",
      "args": [
        "${workspaceFolder}\\Scripts\\mcp-document-processor\\server.py"
      ],
      "env": {
        "ANTHROPIC_API_KEY": "${env:ANTHROPIC_API_KEY}"
      }
    }
  }
}
```

### Пример 5: Несколько серверов

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${workspaceFolder}"],
      "env": {}
    },
    "exa": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-exa"],
      "env": {
        "EXA_API_KEY": "${env:EXA_API_KEY}"
      }
    },
    "firecrawl": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-firecrawl"],
      "env": {
        "FIRECRAWL_API_KEY": "${env:FIRECRAWL_API_KEY}"
      }
    }
  }
}
```

---

## 🔧 Troubleshooting

### Проблема: MCP сервер не запускается

**Причины и решения:**

1. **Node.js не установлен (для npm серверов)**
   ```bash
   # Проверьте
   node --version
   npm --version

   # Если не установлен, установите с nodejs.org
   ```

2. **Python не установлен (для python серверов)**
   ```bash
   # Проверьте
   python --version

   # Если не установлен, установите с python.org
   ```

3. **Неправильный путь к серверу**
   - Проверьте путь в `args`
   - Используйте абсолютные пути или переменные `${workspaceFolder}`

4. **API ключ не работает**
   - Проверьте, что ключ скопирован полностью
   - Проверьте, что ключ активен в панели управления сервиса
   - Проверьте лимиты использования

### Проблема: Сервер не появляется в списке инструментов

**Решения:**

1. **Перезапустите Cursor**
   - Закройте полностью
   - Откройте снова

2. **Проверьте логи MCP**
   - Путь: `C:\Users\Admin\AppData\Roaming\Cursor\logs\[дата]\window1\exthost\anysphere.cursor-mcp\MCP Logs.log`
   - Ищите ошибки

3. **Проверьте синтаксис JSON**
   - Убедитесь, что JSON валидный
   - Используйте валидатор JSON

4. **Проверьте переменные окружения**
   - Убедитесь, что `.env` файл существует
   - Убедитесь, что переменные правильно названы

### Проблема: "Command not found"

**Решения:**

1. **Для npx:**
   - Убедитесь, что Node.js установлен
   - Попробуйте: `npm install -g @modelcontextprotocol/server-exa`

2. **Для python:**
   - Убедитесь, что Python в PATH
   - Попробуйте полный путь: `C:\Python\python.exe`

### Проблема: Сервер работает, но инструменты не доступны

**Решения:**

1. **Проверьте права доступа**
   - Убедитесь, что сервер имеет права на нужные операции

2. **Проверьте конфигурацию сервера**
   - Некоторые серверы требуют дополнительной настройки
   - Читайте README сервера

---

## 📚 Полезные ссылки

### Официальные ресурсы

- **GitHub репозиторий:** https://github.com/modelcontextprotocol/servers
- **Официальный сайт:** https://modelcontextprotocol.io
- **Документация Cursor:** https://cursor.com/docs/context/mcp
- **Примеры серверов:** https://modelcontextprotocol.io/examples

### Документация в проекте

- [CONCEPTS_GUIDE.md](./CONCEPTS_GUIDE.md) - Объяснение всех концепций
- [MCP_SETUP.md](./config/MCP_SETUP.md) - Настройка MCP для Research System
- [Documentation/MCP_WINDOWS_SETUP.md](../Documentation/MCP_WINDOWS_SETUP.md) - Настройка MCP на Windows

### Полезные команды

```bash
# Проверить версию Node.js
node --version

# Проверить версию npm
npm --version

# Проверить версию Python
python --version

# Установить MCP сервер глобально (опционально)
npm install -g @modelcontextprotocol/server-exa

# Запустить сервер вручную для теста
npx -y @modelcontextprotocol/server-exa
```

---

## ✅ Чеклист настройки MCP сервера

- [ ] Найден нужный сервер на GitHub
- [ ] Прочитан README сервера
- [ ] Понят тип сервера (npm/python/другой)
- [ ] Получены необходимые API ключи (если нужны)
- [ ] Добавлен сервер в `.cursor/mcp.json`
- [ ] Настроены переменные окружения (если нужны)
- [ ] Создан `.env` файл (если используется)
- [ ] Проверен синтаксис JSON
- [ ] Перезапущен Cursor
- [ ] Проверена работа сервера (инструменты появились)
- [ ] Протестирован функционал

---

## 💡 Советы

1. **Начните с простого**
   - Сначала настройте filesystem (уже установлен)
   - Потом добавьте EXA
   - Потом другие серверы

2. **Используйте переменные окружения**
   - Не храните API ключи прямо в `mcp.json`
   - Используйте `.env` файл

3. **Читайте README**
   - Каждый сервер может иметь свои особенности
   - README содержит важную информацию

4. **Проверяйте логи**
   - Если что-то не работает, смотрите логи MCP
   - Там часто есть полезная информация об ошибках

5. **Тестируйте постепенно**
   - Добавляйте серверы по одному
   - Тестируйте каждый перед добавлением следующего

---

**Версия:** 1.0
**Последнее обновление:** 28.01.2026
