# 📊 Формулы для делегирования серверов

> Готовые формулы для копирования

---

## 1️⃣ Формула для листа "Для_Сисадмина" в главной таблице

**Назначение:** Фильтрует только сервера с меткой "Сисадмин" в колонке "Выдать"

### Вариант 1: Только безопасные колонки

```
=QUERY(Сервера!A:AZ, "SELECT A,G,H,AE,AF,AG,AH,AI,AJ,AK,AL,AM,AN WHERE AW='Сисадмин'", 1)
```

**Что выбирается:**
- A - Магазин
- G - RDP IP:Username:Password
- H - AnyDesk
- AE - Статус машины
- AF - Статус прокси
- AG - Текущий IP
- AH - Текущий город
- AI - Запущен anydesk
- AJ - Запущен rustdesk
- AK - Дата проверки
- AL - Команда
- AM - Результат проверки сервера
- AN - Результат последней команды

---

### Вариант 2: Если нужно больше данных (но всё ещё безопасно)

```
=QUERY(Сервера!A:AZ, "SELECT A,C,G,H,S,AE,AF,AG,AH,AI,AJ,AK,AL,AM,AN,AO WHERE AW='Сисадмин'", 1)
```

**Дополнительно включены:**
- C - ID сервера в админке
- S - Город прокси который нужен (целевой город)
- AO - Результат проверки прокси

---

### Вариант 3: Минимальный набор (для начального тестирования)

```
=QUERY(Сервера!A:AZ, "SELECT A,G,AE,AG,AH,AL,AM WHERE AW='Сисадмин'", 1)
```

**Только самое необходимое:**
- A - Магазин
- G - RDP
- AE - Статус машины
- AG - Текущий IP
- AH - Текущий город
- AL - Команда
- AM - Результат

---

## 2️⃣ Формула IMPORTRANGE для таблицы сисадмина

**Назначение:** Импортирует данные из главной таблицы в таблицу сисадмина

### Шаг 1: Узнай URL главной таблицы

Открой главную таблицу и скопируй URL из адресной строки. Например:
```
https://docs.google.com/spreadsheets/d/1ABC...XYZ/edit
```

### Шаг 2: Вставь формулу в A1 таблицы сисадмина

```
=IMPORTRANGE("ВСТАВЬ_СЮДА_URL_ГЛАВНОЙ_ТАБЛИЦЫ", "Для_Сисадмина!A:M")
```

**Пример с реальным URL:**
```
=IMPORTRANGE("https://docs.google.com/spreadsheets/d/1ABC...XYZ/edit", "Для_Сисадмина!A:M")
```

---

## 3️⃣ Альтернативные варианты фильтрации

### Фильтр по нескольким значениям

Если нужно выдать серверам разных сисадминов:

```
=QUERY(Сервера!A:AZ, "SELECT A,G,H,AE,AF,AG,AH,AI,AJ,AK,AL,AM,AN WHERE AW='Сисадмин' OR AW='Сисадмин2'", 1)
```

### Фильтр с исключением

Выдать все сервера КРОМЕ тех, где стоит "Нет":

```
=QUERY(Сервера!A:AZ, "SELECT A,G,H,AE,AF,AG,AH,AI,AJ,AK,AL,AM,AN WHERE AW<>'Нет' AND AW<>''", 1)
```

### Фильтр по статусу + выдача

Только онлайн сервера для сисадмина:

```
=QUERY(Сервера!A:AZ, "SELECT A,G,H,AE,AF,AG,AH,AI,AJ,AK,AL,AM,AN WHERE AW='Сисадмин' AND AE='Online'", 1)
```

---

## 4️⃣ Формулы для условного форматирования

### Зелёный фон для Online серверов

**Диапазон:** D2:D1000
**Формула:**
```
=REGEXMATCH(D2, "Online")
```
**Цвет фона:** #d9ead3

### Красный фон для ERROR/Offline

**Диапазон:** D2:D1000
**Формула:**
```
=OR(REGEXMATCH(D2, "ERROR"), REGEXMATCH(D2, "Offline"))
```
**Цвет фона:** #f4cccc

### Жёлтый фон для устаревших данных (более 30 минут)

**Диапазон:** J2:J1000 (колонка "Дата проверки")
**Формула:**
```
=AND(J2<>"", NOW()-J2>TIME(0,30,0))
```
**Цвет фона:** #fff2cc

---

## 5️⃣ Формулы для дополнительной аналитики

### Подсчёт онлайн серверов

```
=COUNTIF(D:D, "*Online*")
```

### Подсчёт проблемных серверов

```
=COUNTIF(D:D, "*ERROR*") + COUNTIF(D:D, "*Offline*")
```

### Процент доступности

```
=ROUND(COUNTIF(D:D, "*Online*") / COUNTA(D:D) * 100, 1) & "%"
```

### Последняя проверка (макс. дата)

```
=MAX(J:J)
```

### Сколько серверов не проверялись больше часа

```
=COUNTIF(J:J, "<"&NOW()-TIME(1,0,0))
```

---

## 6️⃣ Защита данных (Data Validation)

### Ограничить значения в колонке "Команда"

**Диапазон:** Колонка "Команда"
**Тип:** Список
**Значения:**
```
check
start_proxifier
stop_proxifier
restart_proxifier
get_timezone
set_timezone_msk
reboot
```

**Настройка:**
1. Выдели колонку "Команда"
2. Данные → Проверка данных
3. Критерий: "Значение из списка"
4. Вставь список команд (каждая с новой строки)
5. При ошибке: "Отклонить ввод"

---

## 📝 Пример структуры листа "Для_Сисадмина"

```
┌─────────────┬──────────────────────┬─────────────┬───────────────┬──────────────┬─────────────┬──────────────┬────────────────┬────────────────┬─────────────────┬─────────┬──────────────────┬──────────────────┐
│   Магазин   │         RDP          │   AnyDesk   │ Статус машины │ Статус прокси│ Текущий IP  │ Текущий город│  Запущен AD    │  Запущен RD    │  Дата проверки  │ Команда │ Результат сервера│ Результат команды│
├─────────────┼──────────────────────┼─────────────┼───────────────┼──────────────┼─────────────┼──────────────┼────────────────┼────────────────┼─────────────────┼─────────┼──────────────────┼──────────────────┤
│   Shop_02   │ 123.45.67.89:admin:  │  123456789  │    Online     │      OK      │ 10.20.30.40 │   Moscow     │       ✅       │       ✅       │ 24.01.26 10:30  │         │   ✅ OK          │                  │
│   Shop_04   │ 98.76.54.32:user:... │  987654321  │    Online     │      OK      │ 50.60.70.80 │ Saint Pete.. │       ✅       │       ❌       │ 24.01.26 10:32  │  check  │   ✅ OK          │ Checking...      │
│   Shop_05   │ 11.22.33.44:root:... │  111222333  │    ERROR      │    ERROR     │     N/A     │     N/A      │       ❌       │       ❌       │ 24.01.26 10:35  │         │  ❌ Offline      │                  │
└─────────────┴──────────────────────┴─────────────┴───────────────┴──────────────┴─────────────┴──────────────┴────────────────┴────────────────┴─────────────────┴─────────┴──────────────────┴──────────────────┘
```

---

## ⚠️ Важные примечания

### 1. Колонки могут отличаться!

Буквы колонок (A, G, AE и т.д.) зависят от структуры ТВОЕЙ таблицы!

**Как найти правильную букву колонки:**

1. Открой главную таблицу
2. Найди нужную колонку (например, "Магазин")
3. Посмотри букву вверху (A, B, C...)
4. Используй эту букву в формуле

**Пример:**
- Если "RDP" у тебя в колонке F (а не G), используй F в формуле
- Если "Выдать" у тебя в колонке AX (а не AW), используй AX

---

### 2. Тестирование формул

**Перед применением:**

1. Создай копию таблицы (для тестов)
2. Вставь формулу в тестовую таблицу
3. Проверь что данные подтянулись корректно
4. Только потом используй на рабочей таблице

---

### 3. Обновление данных

**IMPORTRANGE обновляется:**
- Автоматически при изменении источника
- Можно принудительно: Ctrl+R (или Cmd+R на Mac)
- Задержка обновления: 1-5 секунд обычно

---

## 🔧 Troubleshooting

### Ошибка: #REF!

**Причина:** Неверный URL или название листа

**Решение:**
1. Проверь URL главной таблицы
2. Проверь что лист "Для_Сисадмина" существует
3. Убери лишние пробелы в формуле

---

### Ошибка: "У вас нет доступа"

**Причина:** IMPORTRANGE требует разрешения

**Решение:**
1. Нажми на ячейку с формулой
2. Появится кнопка "Разрешить доступ"
3. Нажми её и подтверди

---

### Данные не обновляются

**Решение:**
1. Проверь что в главной таблице есть изменения
2. Нажми Ctrl+R для принудительного обновления
3. Если не помогает - пересоздай формулу

---

**Готово!** Используй эти формулы для настройки делегирования серверов.
