# План реализации: Удаленное управление настройками прокси Proxifier

## ✅ Что было реализовано

### 1. Функция парсинга реквизитов прокси (`parse_proxy_credentials`)

**Файл:** `server-monitor-package/command_handler.py`

Добавлена функция, которая парсит различные форматы реквизитов прокси:
- `IP:PORT` - простой формат (по умолчанию SOCKS5)
- `IP:PORT:USERNAME:PASSWORD` - с аутентификацией
- `PROTOCOL://IP:PORT` - с указанием протокола (SOCKS5, HTTP, HTTPS, SOCKS4)
- `PROTOCOL://USERNAME:PASSWORD@IP:PORT` - полный формат

**Примеры:**
```
127.0.0.1:1080
192.168.1.1:1080:user:pass
SOCKS5://127.0.0.1:1080
HTTP://user:pass@192.168.1.1:8080
```

### 2. Команда `set_proxy`

**Файл:** `server-monitor-package/command_handler.py`

Добавлена новая команда, которая:
1. Парсит реквизиты прокси из колонки Google Sheets
2. Находит файл профиля Proxifier на удаленном сервере
3. Создает резервную копию профиля
4. Обновляет настройки прокси в XML профиле
5. Сохраняет изменения
6. Загружает обновленный профиль в Proxifier через `silent-load`

### 3. Обновление Google Apps Script

**Файл:** `google_apps_script/Code.gs`

Добавлена поддержка новой колонки `PROXY_CREDENTIALS`:
- Автоматический поиск колонки по названию ("Реквизиты прокси", "Proxy Credentials")
- Чтение реквизитов из колонки при выполнении команд
- Передача реквизитов в webhook при выполнении команды `set_proxy`

---

## 📋 Пошаговая инструкция по использованию

### Шаг 1: Колонка в Google Sheets

Колонка уже должна существовать с названием **"Прокси реквизиты"** (или "Реквизиты прокси", "Proxy Credentials").

Система автоматически найдет колонку по любому из этих названий.

### Шаг 2: Заполнение реквизитов прокси

В колонке "Прокси реквизиты" укажите реквизиты в формате:

**Основной формат (SOCKS5 с аутентификацией):**
```
185.162.130.86:10000:TriWhhkl22f8Hnq3eNbW:f1f27lKxpGe1qGEXfOVb9B1PHvcDjijx
```

**Формат:** `IP:PORT:LOGIN:PASSWORD`

Все прокси используют протокол **SOCKS5** (указывать не нужно, система автоматически установит SOCKS5).

**Другие поддерживаемые форматы:**

Простой формат без аутентификации:
```
192.168.1.100:1080
```

С указанием протокола:
```
SOCKS5://192.168.1.100:1080
HTTP://192.168.1.100:8080
HTTPS://192.168.1.100:8443
```

С аутентификацией и протоколом:
```
SOCKS5://username:password@192.168.1.100:1080
```

### Шаг 3: Выполнение команды

1. В колонке **"Команда"** для нужного сервера введите:
   ```
   set_proxy
   ```

2. Нажмите меню **"🔧 Управление серверами"** → **"⚡ Выполнить команды"**

3. Система автоматически:
   - Прочитает реквизиты из колонки "Прокси реквизиты"
   - Распарсит формат `IP:PORT:LOGIN:PASSWORD`
   - Подключится к серверу через WinRM
   - Обновит настройки прокси в Proxifier (включая аутентификацию для SOCKS5)
   - Загрузит новый профиль в Proxifier

4. Результат выполнения появится в колонке **"Результат команды"**

---

## 🔍 Проверка результата

После выполнения команды `set_proxy`:

1. **Проверьте результат в колонке "Результат команды":**
   - ✅ Успешно: `✅ Настройки прокси обновлены`
   - ❌ Ошибка: `❌ Ошибка обновления прокси` (с описанием проблемы)

2. **Проверьте статус прокси:**
   - Выполните команду `check` для проверки текущего IP и города
   - Убедитесь, что IP изменился на IP прокси-сервера

3. **На удаленном сервере:**
   - Откройте Proxifier
   - Проверьте настройки прокси в профиле
   - Убедитесь, что прокси активен и работает

---

## 🛠️ Технические детали

### Пути к профилям Proxifier

Система автоматически ищет профиль в следующих местах:
1. `%APPDATA%\Proxifier4\Profiles\Default.ppx` (пользовательский профиль)
2. `%ProgramData%\Proxifier\Default.ppx` (глобальный профиль)
3. `C:\Users\<username>\AppData\Roaming\Proxifier4\Profiles\Default.ppx` (альтернативный путь)

### Резервное копирование

Перед изменением профиля автоматически создается резервная копия:
```
Default.ppx.backup.20260119143025
```

### Формат обновления профиля

Система обновляет первый прокси в списке `<ProxyList>`:
- Если прокси существует - обновляет его параметры
- Если прокси нет - создает новый с ID=100

Обновляются следующие параметры:
- `type` - тип протокола (SOCKS5 по умолчанию, HTTP, HTTPS, SOCKS4)
- `Address` - IP адрес прокси
- `Port` - порт прокси
- `Options` - опции (48 - базовые, 64 - с аутентификацией)
- `Auth` - элементы аутентификации для SOCKS5 (Username и Password, если указаны)

---

## ⚠️ Важные замечания

1. **Формат реквизитов:** Используйте правильный формат. Если формат не распознан, команда вернет ошибку.

2. **Proxifier должен быть установлен:** Команда работает только если Proxifier установлен на удаленном сервере.

3. **Права доступа:** Убедитесь, что пользователь WinRM имеет права на изменение файлов профиля Proxifier.

4. **Активная сессия:** Если Proxifier запущен, изменения применяются через `silent-load`. Если Proxifier не запущен, нужно будет запустить его вручную после обновления профиля.

5. **Валидация порта:** Порт должен быть в диапазоне 1-65535.

---

## 🐛 Решение проблем

### Ошибка: "Реквизиты прокси не указаны в таблице"
- **Решение:** Убедитесь, что колонка "Прокси реквизиты" заполнена для данного сервера

### Ошибка: "Неверный формат реквизитов прокси"
- **Решение:** Проверьте формат реквизитов. Основной формат: `IP:PORT:LOGIN:PASSWORD` (например: `185.162.130.86:10000:user:pass`)

### Ошибка: "Профиль Proxifier не найден"
- **Решение:** Убедитесь, что Proxifier установлен на сервере и профиль существует в стандартных путях

### Ошибка: "Ошибка при изменении профиля"
- **Решение:** Проверьте права доступа пользователя WinRM к файлам Proxifier. Убедитесь, что файл профиля не заблокирован другим процессом.

---

## 📝 Примеры использования

### Пример 1: SOCKS5 прокси с аутентификацией (основной формат)
```
Колонка "Прокси реквизиты": 185.162.130.86:10000:TriWhhkl22f8Hnq3eNbW:f1f27lKxpGe1qGEXfOVb9B1PHvcDjijx
Команда: set_proxy
```

### Пример 2: Простой SOCKS5 прокси без аутентификации
```
Колонка "Прокси реквизиты": 192.168.1.100:1080
Команда: set_proxy
```

### Пример 3: HTTP прокси с аутентификацией
```
Колонка "Прокси реквизиты": HTTP://user123:pass456@proxy.example.com:8080
Команда: set_proxy
```

---

## 🔄 Интеграция с существующей системой

Все изменения полностью интегрированы в существующую систему:

- ✅ Используется тот же механизм передачи параметров через Google Apps Script → webhook
- ✅ Используется существующий `WinRMConnector` для выполнения команд
- ✅ Используется существующий `ServerChecker` для проверки статуса после выполнения
- ✅ Результат записывается в колонку `commandResult` через существующий механизм обновления Google Sheets
- ✅ Следует тому же паттерну обработки команд, что и `check_proxyma`, `start_proxifier` и другие
- ✅ Не требует изменений в существующих командах или логике системы

---

## 📅 Версия

**Дата реализации:** 19.01.2026
**Версия системы:** 3.1
**Статус:** ✅ Реализовано и готово к использованию
