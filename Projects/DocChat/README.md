# 📄 DocChat

Локальный ассистент с Claude API для работы с документами.

## 🎯 Возможности

- **PDF** — извлечение текста, OCR для сканов, Vision для сложных документов
- **Excel** — чтение всех листов, отображение таблиц
- **Word** — извлечение текста с сохранением структуры
- **Изображения** — анализ через Claude Vision

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd Projects/DocChat
pip install -r requirements.txt
```

### 2. Настройка API ключа

Файл `.env` уже создан с вашим ключом. Если нужно изменить:

```env
ANTHROPIC_API_KEY=ваш_ключ_здесь
```

### 3. Установка Tesseract (для OCR сканов)

**Windows:**
1. Скачайте установщик: https://github.com/UB-Mannheim/tesseract/wiki
2. Установите с языковыми пакетами (русский + английский)
3. Добавьте в PATH: `C:\Program Files\Tesseract-OCR`

### 4. Запуск

```bash
streamlit run app.py
```

Приложение откроется в браузере: http://localhost:8501

## 📁 Структура проекта

```
DocChat/
├── app.py              # Основное приложение Streamlit
├── processors/         # Модули обработки файлов
│   ├── pdf_processor.py
│   ├── excel_processor.py
│   ├── word_processor.py
│   └── image_processor.py
├── .env               # API ключ (не коммитить!)
├── .gitignore
├── requirements.txt
└── README.md
```

## 💡 Использование

1. **Загрузите документы** через боковую панель
2. **Задайте вопрос** в чате
3. Claude проанализирует документы и ответит

### Примеры запросов:

- "Сделай краткое содержание этого документа"
- "Найди все упоминания о ценах в таблице"
- "Что изображено на картинке?"
- "Переведи этот документ на английский"
- "Создай инструкцию на основе этого PDF"

## ⚙️ Конфигурация

### Модель Claude

По умолчанию используется `claude-sonnet-4-20250514`. Можно изменить в `app.py`:

```python
model="claude-sonnet-4-20250514"  # или claude-3-haiku-20240307 для экономии
```

### Лимиты

- Максимум ~50,000 символов на документ
- Максимум 10 страниц PDF для Vision
- История чата сохраняется в сессии

## 🔧 Решение проблем

### "API ключ не настроен"
Проверьте файл `.env` и перезапустите приложение.

### "OCR недоступен"
Установите Tesseract-OCR и добавьте в PATH.

### Медленная обработка PDF
Большие PDF с изображениями обрабатываются дольше. Для сканов используется OCR.

## 📝 TODO (будущие улучшения)

- [ ] Интеграция с Google Docs
- [ ] Сохранение истории чатов
- [ ] Выбор модели в интерфейсе
- [ ] Batch-обработка множества файлов
- [ ] Экспорт результатов
