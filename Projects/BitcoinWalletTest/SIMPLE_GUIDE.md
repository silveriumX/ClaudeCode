# ПОШАГОВАЯ УСТАНОВКА Bitcoin Core 0.6.1

## ШАГ 1: Скачивание

1. Откройте в браузере:
   **https://sourceforge.net/projects/bitcoin/files/Bitcoin/bitcoin-0.6.1/**

2. Найдите файл: **bitcoin-0.6.1-win32.zip**

3. Нажмите на него, начнётся загрузка

4. Сохраните в: **C:\Downloads\** (или в вашу папку загрузок)

## ШАГ 2: Распаковка

1. Найдите скачанный файл **bitcoin-0.6.1-win32.zip**

2. Правой кнопкой → "Извлечь всё..." (Extract All...)

3. Укажите путь: **C:\BitcoinCore-0.6.1**

4. Нажмите "Извлечь" (Extract)

## ШАГ 3: Проверка файлов

Откройте папку **C:\BitcoinCore-0.6.1**

Должны быть файлы:
- ✅ bitcoin-qt.exe (главная программа)
- ✅ bitcoin-cli.exe (утилита команд)
- ✅ bitcoind.exe (демон)

Если их НЕТ, а есть подпапка (например bitcoin-0.6.1), то:
- Зайдите в эту подпапку
- Скопируйте ВСЕ файлы
- Вставьте в **C:\BitcoinCore-0.6.1**

## ШАГ 4: Создание папки для данных

Откройте PowerShell и выполните:

```powershell
New-Item -ItemType Directory -Path "D:\BitcoinData06" -Force
New-Item -ItemType Directory -Path "C:\BitcoinBackup" -Force
```

Или создайте вручную эти папки:
- **D:\BitcoinData06** (здесь будет кошелёк)
- **C:\BitcoinBackup** (для резервных копий)

## ШАГ 5: Создание конфигурации

1. Создайте файл: **D:\BitcoinData06\bitcoin.conf**

2. Скопируйте в него:

```
server=1
rpcuser=testuser
rpcpassword=testpass123
rpcport=8332
listen=0
connect=0
```

3. Сохраните и закройте

## ШАГ 6: Запуск Bitcoin Core

Откройте PowerShell и выполните:

```powershell
cd C:\BitcoinCore-0.6.1
.\bitcoin-qt.exe -datadir=D:\BitcoinData06
```

**Что произойдёт:**
- Откроется окно Bitcoin Core
- Начнётся создание кошелька
- Появится окно синхронизации (НЕ ждите полной синхронизации!)

**Подождите 30-60 секунд**

## ШАГ 7: Проверка wallet.dat

Откройте папку: **D:\BitcoinData06**

Должен появиться файл: **wallet.dat**

Если его нет - подождите ещё 30 секунд и обновите папку (F5)

## ШАГ 8: Проверка версии кошелька

Откройте НОВОЕ окно PowerShell (НЕ закрывайте Bitcoin Core!)

```powershell
cd C:\BitcoinCore-0.6.1
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 getinfo
```

**Ожидаемый результат:**
Должен показать JSON с информацией, найдите строку:

```
"walletversion": 60000
```

✅ **Если видите 60000 - УСПЕХ!**

## ШАГ 9: Шифрование кошелька паролем

В том же окне PowerShell выполните:

```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 encryptwallet "MySecurePassword123!"
```

**Замените `MySecurePassword123!` на свой пароль!**

⚠️ **ВАЖНО**: Bitcoin Core автоматически закроется после шифрования!

## ШАГ 10: Перезапуск

Запустите Bitcoin Core снова:

```powershell
cd C:\BitcoinCore-0.6.1
.\bitcoin-qt.exe -datadir=D:\BitcoinData06
```

Подождите 10-15 секунд

## ШАГ 11: Разблокировка и проверка

Разблокируйте кошелёк на 60 секунд:

```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 walletpassphrase "MySecurePassword123!" 60
```

Создайте тестовый адрес:

```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 getnewaddress
```

Если команда вернула Bitcoin адрес - **ВСЁ РАБОТАЕТ!** ✅

## ШАГ 12: Резервная копия

⚠️ **КРИТИЧНО!** Сделайте бэкап:

```powershell
copy D:\BitcoinData06\wallet.dat C:\BitcoinBackup\wallet_backup.dat
```

Сохраните эту копию в надёжное место!

---

## БЫСТРЫЕ КОМАНДЫ (шпаргалка)

### Запуск Bitcoin Core
```powershell
cd C:\BitcoinCore-0.6.1
.\bitcoin-qt.exe -datadir=D:\BitcoinData06
```

### Проверка информации
```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 getinfo
```

### Разблокировка кошелька
```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 walletpassphrase "ВАШ_ПАРОЛЬ" 60
```

### Новый адрес
```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 getnewaddress
```

### Остановка Bitcoin Core
```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 stop
```

---

## ПРОБЛЕМЫ И РЕШЕНИЯ

### "bitcoin-cli не работает"
- Убедитесь что Bitcoin-Qt запущен
- Подождите 10-15 секунд после запуска
- Проверьте файл bitcoin.conf

### "RPC connection failed"
- Создайте файл bitcoin.conf (Шаг 5)
- Перезапустите Bitcoin-Qt

### "wallet.dat не создаётся"
- Подождите 60 секунд
- Проверьте права на папку D:\BitcoinData06
- Попробуйте другую папку (например C:\BitcoinData06)

---

## ЧТО ДАЛЬШЕ?

После успешной установки у вас есть:
- ✅ Кошелёк с walletversion 60000
- ✅ Зашифрованный паролем
- ✅ Резервная копия wallet.dat

Можно:
1. Использовать для теста
2. Открыть в современном Bitcoin Core
3. Получать/отправлять Bitcoin (после синхронизации)

---

**Готово!** 🎉

Если нужна помощь на любом шаге - спрашивайте!
