# Bitcoin Wallet Test Project

Проект для тестирования создания Bitcoin Core кошелька с `walletversion 60000` и шифрованием паролем.

## Цель

Создать кошелёк Bitcoin Core с:
- `walletversion 60000` (поддержка сжатых публичных ключей)
- Шифрование паролем через `encryptwallet`
- Проверка работоспособности (разблокировка и генерация адреса)

## Файлы проекта

### Документация

| Файл | Описание |
|------|----------|
| `SIMPLE_GUIDE.md` | **Главная инструкция** - пошаговое руководство для ручной установки |
| `README.md` | Подробная документация со всеми деталями |
| `Work/Задачи/BITCOIN_CORE_WALLET_60000.md` | Техническое описание walletversion 60000 |

### Скрипты установки

| Файл | Описание | Когда использовать |
|------|----------|-------------------|
| `step_by_step.ps1` | ⭐ **РЕКОМЕНДУЕТСЯ** - интерактивный помощник с проверкой каждого шага | Первая установка |
| `check_wallet.ps1` | Проверка статуса кошелька | После установки |
| `quick_start.ps1` | Быстрая настройка (если Bitcoin Core уже установлен) | Переустановка |
| `test_bitcoin_wallet.py` | Полный автоматический тест на Python | Продвинутое тестирование |

### Устаревшие/Вспомогательные

| Файл | Описание |
|------|----------|
| `auto_install.ps1` | Первая попытка автоустановки (проблемы с загрузкой) |
| `auto_install_v2.ps1` | Вторая попытка (проблемы с распаковкой SourceForge) |
| `manual_install_helper.ps1` | Упрощённый помощник |

## Быстрый старт

### Способ 1: Интерактивный помощник (рекомендуется)

```powershell
cd Projects/BitcoinWalletTest
powershell -ExecutionPolicy Bypass -File step_by_step.ps1
```

**Что делает:**
1. Просит скачать Bitcoin Core 0.6.1
2. Автоматически распаковывает
3. Создаёт все нужные папки
4. Настраивает конфигурацию
5. Запускает Bitcoin Core
6. Проверяет создание wallet.dat
7. Проверяет версию кошелька

### Способ 2: Ручная установка

Следуйте инструкции в **SIMPLE_GUIDE.md**

### Способ 3: Python тест (после установки)

```powershell
python test_bitcoin_wallet.py
```

Полностью автоматический тест всех функций.

## Структура директорий

```
C:\BitcoinCore-0.6.1\     # Программа
  ├── bitcoin-qt.exe       # GUI клиент
  ├── bitcoin-cli.exe      # Утилита команд
  └── bitcoind.exe         # Демон

D:\BitcoinData06\          # Данные кошелька
  ├── wallet.dat           # КОШЕЛЁК (ВАЖНО!)
  ├── bitcoin.conf         # Конфигурация
  └── blocks/              # Блокчейн (можно удалить для теста)

C:\BitcoinBackup\          # Резервные копии
  └── wallet_backup.dat    # Бэкап кошелька
```

## Основные команды

### Запуск Bitcoin Core

```powershell
cd C:\BitcoinCore-0.6.1
.\bitcoin-qt.exe -datadir=D:\BitcoinData06
```

### Проверка версии кошелька

```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 getinfo
```

Ищите строку: `"walletversion": 60000`

### Шифрование кошелька

```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 encryptwallet "YourPassword123!"
```

⚠️ Bitcoin Core закроется после шифрования!

### Разблокировка кошелька

```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 walletpassphrase "YourPassword123!" 60
```

### Генерация адреса

```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 getnewaddress
```

### Остановка Bitcoin Core

```powershell
.\bitcoin-cli.exe -datadir=D:\BitcoinData06 -rpcuser=testuser -rpcpassword=testpass123 stop
```

## Проверка установки

После установки запустите:

```powershell
powershell -ExecutionPolicy Bypass -File check_wallet.ps1
```

Проверяет:
- [x] Наличие wallet.dat
- [x] Запущен ли Bitcoin Core
- [x] Работает ли RPC
- [x] Версию кошелька

## Требования

### Система
- Windows 10/11
- PowerShell 5.1+
- ~500 MB свободного места (программа + блокчейн для теста)

### Опционально для Python скриптов
- Python 3.7+
- Модули: `subprocess`, `json`, `pathlib`, `logging` (все встроенные)

## Ссылки

- **Скачать Bitcoin Core 0.6.1**: https://sourceforge.net/projects/bitcoin/files/Bitcoin/bitcoin-0.6.1/
- **Release Notes 0.6.1**: https://bitcoin.org/en/release/v0.6.1
- **Bitcoin Core Documentation**: https://bitcoin.org/en/bitcoin-core/

## Важные замечания

### ⚠️ Безопасность

- Bitcoin Core 0.6.1 - очень старая версия (май 2012)
- **НЕ используйте** для хранения реальных средств в интернете
- Используйте **только для теста** и создания кошелька с версией 60000
- Для постоянной работы - используйте актуальную версию Bitcoin Core

### 💾 Резервные копии

**КРИТИЧНО**: Всегда делайте бэкап `wallet.dat`!

```powershell
copy D:\BitcoinData06\wallet.dat C:\BitcoinBackup\wallet_backup_$(Get-Date -Format 'yyyyMMdd_HHmmss').dat
```

Храните копии:
- На внешнем диске
- В облаке (зашифрованном!)
- На USB-флешке

### 🔐 Пароли

- Запишите пароль в надёжном месте
- Без пароля **невозможно** получить доступ к зашифрованному кошельку
- Рекомендуется использовать менеджер паролей

### 🌐 Синхронизация блокчейна

Для теста создания кошелька синхронизация **НЕ нужна**.

Если планируете использовать:
- Полная синхронизация займёт несколько дней
- Требуется ~500 GB места
- Используйте актуальную версию Bitcoin Core

## Результат теста

После успешного выполнения у вас будет:

✅ Кошелёк Bitcoin Core с `walletversion 60000`
✅ Зашифрованный паролем через `encryptwallet`
✅ Проверенная возможность разблокировки
✅ Работающая генерация адресов
✅ Резервная копия `wallet.dat`

## Поддержка

Если возникли проблемы:

1. Проверьте `SIMPLE_GUIDE.md` - раздел "ПРОБЛЕМЫ И РЕШЕНИЯ"
2. Запустите `check_wallet.ps1` для диагностики
3. Проверьте логи Bitcoin Core в `D:\BitcoinData06\debug.log`

## Следующие шаги

После успешного создания кошелька можно:

1. **Миграция** - открыть `wallet.dat` в современном Bitcoin Core
2. **Экспорт ключей** - использовать для других целей
3. **Тестирование** - проверить совместимость с другими кошельками
4. **Изучение** - понять структуру BDB кошельков

---

**Версия проекта**: 1.0
**Дата создания**: 11.02.2026
**Статус**: Готов к использованию
