# Настройка окружения после объединения

## 🔐 Настройка .env файла

После объединения проектов все API ключи вынесены из конфигов в переменные окружения.

### 1. Скопировать и заполнить .env

```bash
# .env файл уже создан с шаблонами
# Заполните реальные значения для нужных сервисов
```

### 2. Получить API ключи

#### Anthropic (Claude)
1. Перейти на https://console.anthropic.com/
2. Account Settings → API Keys
3. Скопировать ключ в `ANTHROPIC_API_KEY`

#### Exa (Поиск)
1. Перейти на https://exa.ai/
2. Dashboard → API Keys
3. Скопировать ключ в `EXA_API_KEY`

#### Fireflies (Встречи)
1. Перейти на https://app.fireflies.ai/
2. Integrations → API
3. Скопировать токен в `FIREFLIES_API_TOKEN`

#### Zoom
1. Перейти на https://marketplace.zoom.us/
2. Build App → Server-to-Server OAuth
3. Скопировать Account ID, Client ID, Client Secret

#### Google Drive
1. Google Cloud Console → APIs & Services → Credentials
2. Создать OAuth 2.0 Client ID (или использовать существующий)
3. Скопировать Client ID и Client Secret

#### ClickUp
1. Перейти в https://app.clickup.com/
2. Settings → Apps → Generate API Token
3. Скопировать в `CLICKUP_API_TOKEN`

---

## 📁 Структура credentials

Все файлы credentials теперь в папке `.credentials/`:

```
.credentials/
├── credentials.json              # Google credentials
├── gdrive_creds.json            # Google Drive
├── gcp-oauth.keys.json          # Google OAuth
└── 1596335793q-...-0403e.json   # GCP Service Account
```

**Эта папка НЕ коммитится в git** (в .gitignore)

---

## 🔧 Использование mcp.json

### Claude Code (.claude/mcp.json)

```bash
# Файл .claude/mcp.json уже настроен с переменными
# Убедитесь, что переменные заполнены в .env
```

### Cursor (.cursor/mcp.json)

```bash
# Файл .cursor/mcp.json тоже обновлен
# Все хардкоженные ключи заменены на ${ПЕРЕМЕННЫЕ}
```

### Примеры файлов

- `.claude/mcp.json.example` - шаблон для Claude Code
- `.cursor/mcp.json.example` - шаблон для Cursor

---

## ✅ Проверка настройки

### 1. Проверить .env

```bash
# Убедитесь, что заполнены нужные ключи
cat .env | grep "your_" | wc -l
# Должно быть 0 если все заполнено
```

### 2. Проверить git status

```bash
git status
# mcp.json и .env НЕ должны быть в списке для коммита
```

### 3. Тестовый запуск

```bash
# Проверьте, что переменные доступны
echo $ANTHROPIC_API_KEY    # Linux/Mac
echo %ANTHROPIC_API_KEY%   # Windows CMD
echo $env:ANTHROPIC_API_KEY  # Windows PowerShell
```

---

## 🚨 Безопасность

### ✅ Что сделано:

- [x] Все API ключи вынесены из конфигов
- [x] Создан .env с шаблонами
- [x] mcp.json добавлен в .gitignore
- [x] Credentials перемещены в .credentials/
- [x] Удалены __pycache__ файлы
- [x] Созданы .example файлы для шаблонов

### ⚠️ Что нужно сделать:

- [ ] Заполнить .env реальными ключами
- [ ] **КРИТИЧНО:** Ротировать скомпрометированные ключи:
  - [ ] Anthropic API Key (был в .cursor/mcp.json)
  - [ ] Fireflies API Token
  - [ ] Zoom credentials
  - [ ] Google Drive OAuth credentials
  - [ ] Exa API Key

### 🔒 Правила безопасности:

1. **НИКОГДА** не коммитить .env или mcp.json
2. **ВСЕГДА** проверять `git status` перед коммитом
3. **НЕМЕДЛЕННО** ротировать ключи при утечке
4. Использовать `.example` файлы для шаблонов
5. Хранить credentials только в .credentials/ или .env

---

## 📝 Дополнительно

### Переменная WORKSPACE_FOLDER

Используется в .cursor/mcp.json вместо `${workspaceFolder}` (которая работает только в VSCode/Cursor).

Установите в .env:
```bash
WORKSPACE_FOLDER=C:\\Users\\Admin\\Documents\\workspaces for ai\\CloudeCode
```

---

## 🆘 Проблемы

### MCP серверы не подключаются

1. Проверьте, что .env заполнен
2. Перезапустите Claude Code / Cursor
3. Проверьте логи в `.claude/logs/` или `.cursor/logs/`

### Переменные не подставляются

1. Убедитесь, что используете правильный синтаксис: `${VAR_NAME}`
2. Проверьте, что переменная есть в .env
3. В Windows может потребоваться перезагрузка терминала

---

**Готово!** Теперь проект безопасен и готов к работе.
