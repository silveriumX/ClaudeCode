# Расширения Cursor для работы с документами

## 🎯 Рекомендуемые расширения

### 1. Excel Viewer (GrapeCity)
**ID:** `GrapeCity.gc-excelviewer`

**Возможности:**
- Просмотр Excel файлов (.xlsx, .xls, .csv)
- Редактирование данных в таблице
- Красивое отображение с форматированием

**Установка:**
1. `Ctrl+Shift+X` (открыть Extensions)
2. Найти "Excel Viewer"
3. Установить от GrapeCity

### 2. vscode-pdf
**ID:** `tomoki1207.pdf`

**Возможности:**
- Просмотр PDF прямо в Cursor
- Навигация по страницам
- Масштабирование

**Установка:**
1. `Ctrl+Shift+X`
2. Найти "vscode-pdf"
3. Установить

### 3. Rainbow CSV
**ID:** `mechatroner.rainbow-csv`

**Возможности:**
- Подсветка CSV файлов
- SQL запросы к CSV
- Выравнивание колонок

---

## 🚀 Быстрая установка через команду

Откройте терминал в Cursor и выполните:

```bash
code --install-extension GrapeCity.gc-excelviewer
code --install-extension tomoki1207.pdf
code --install-extension mechatroner.rainbow-csv
```

---

## 💡 Как использовать после установки

### Для Excel файлов:
1. Откройте `.xlsx` файл в Cursor
2. Справа сверху появится кнопка "Open Preview"
3. Нажмите - откроется красивая таблица

### Для PDF:
1. Откройте `.pdf` файл
2. Автоматически откроется в просмотрщике

### Для CSV:
1. Откройте `.csv` файл
2. Колонки будут подсвечены разными цветами

---

## 🤖 Работа с MCP Document Processor

**Важно:** Расширения только для просмотра. Для глубокого анализа используйте MCP:

```
Обработай файл C:\path\to\file.xlsx и найди аномалии
```

```
Сравни два файла: C:\file1.xlsx и C:\file2.xlsx
```

```
Извлеки из C:\report.pdf все таблицы с финансами
```

---

## ⚙️ Альтернатива: Настройка ассоциаций файлов

Добавьте в настройки Cursor (`Ctrl+,` → Open Settings JSON):

```json
{
  "workbench.editorAssociations": {
    "*.xlsx": "gc-excelviewer.csvEdit",
    "*.xls": "gc-excelviewer.csvEdit",
    "*.pdf": "pdf.preview"
  }
}
```

Теперь Excel и PDF будут автоматически открываться в удобном виде!
