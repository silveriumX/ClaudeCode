# MCP Document Processor

MCP сервер для обработки документов через Claude API с качеством веб-версии Claude.

## 🎯 Возможности

- **PDF** - полное распознавание с OCR, сохранение структуры
- **Word (DOCX)** - извлечение текста и таблиц
- **Excel (XLSX)** - конвертация в markdown таблицы
- **Изображения** (JPG, PNG, GIF, WebP) - распознавание текста и содержимого
- **PowerPoint** (через конвертацию в изображения)

## 📦 Установка

### 1. Установите зависимости

```powershell
cd Scripts\mcp-document-processor
pip install -r requirements.txt
```

### 2. Установите Poppler (для PDF → изображения)

**Windows:**
1. Скачайте Poppler: https://github.com/oschwartz10612/poppler-windows/releases
2. Распакуйте в `C:\Program Files\poppler`
3. Добавьте в PATH: `C:\Program Files\poppler\Library\bin`

**Или через Chocolatey:**
```powershell
choco install poppler
```

### 3. Настройте API ключ

Откройте `.cursor\mcp.json` и добавьте сервер (см. ниже).

## ⚙️ Конфигурация

Добавьте в `.cursor\mcp.json`:

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${workspaceFolder}", "${userHome}"],
      "env": {}
    },
    "document-processor": {
      "command": "python",
      "args": [
        "${workspaceFolder}\\Scripts\\mcp-document-processor\\server.py"
      ],
      "env": {
        "ANTHROPIC_API_KEY": "ВАШ_API_КЛЮЧ_ЗДЕСЬ"
      }
    }
  }
}
```

**Замените `ВАШ_API_КЛЮЧ_ЗДЕСЬ` на ваш реальный ключ от Anthropic!**

## 🚀 Использование

После перезапуска Cursor используйте в чате:

### Базовое использование
```
Обработай файл C:\Users\Admin\Documents\contract.pdf
```

### С дополнительной инструкцией
```
Извлеки из C:\Users\Admin\Documents\data.xlsx только таблицы с финансами
```

### Для изображений
```
Распознай текст с изображения C:\Users\Admin\Pictures\scan.jpg
```

## 🔧 Как это работает

1. Вы просите обработать файл в чате с Cursor
2. Я вызываю MCP инструмент `process_document`
3. Сервер:
   - Для PDF: конвертирует страницы в изображения
   - Для DOCX/XLSX: извлекает текст и структуру
   - Для изображений: кодирует в base64
4. Отправляет в Claude API (та же модель, что в веб-версии)
5. Возвращает распознанный текст с форматированием

## 💰 Стоимость

Используется модель `claude-sonnet-4-5` (последняя версия, сентябрь 2025):
- Ввод: $3 за 1M токенов
- Вывод: $15 за 1M токенов
- Контекст: 200K токенов
- Максимум вывода: 64K токенов

Примерная стоимость:
- PDF (10 страниц): ~$0.02-0.05
- Excel файл: ~$0.01-0.03
- Изображение: ~$0.01-0.02

## 🐛 Устранение проблем

### "Не найден ANTHROPIC_API_KEY"
- Проверьте `mcp.json` - ключ указан в секции `env`
- Перезапустите Cursor после изменения `mcp.json`

### "pdf2image: Unable to find pdftoppm"
- Установите Poppler (см. Установку выше)
- Убедитесь что `poppler/bin` в PATH

### "Module not found"
- Запустите `pip install -r requirements.txt`
- Проверьте что Python в PATH

## 📝 Примеры

### PDF контракт
```
Обработай контракт C:\Docs\contract.pdf и выдели ключевые условия
```

### Excel отчет
```
Извлеки из C:\Reports\sales.xlsx таблицу продаж за последний месяц
```

### Скан документа
```
Распознай текст с фото C:\Scans\passport.jpg
```

## 🔐 Приватность

- Файлы отправляются в Anthropic API (условия: https://www.anthropic.com/legal/privacy)
- Для конфиденциальных данных используйте локальные решения (OCR без API)
- Anthropic не использует данные для обучения при использовании через API

## 📚 Ссылки

- [MCP Documentation](https://modelcontextprotocol.io/)
- [Anthropic API](https://docs.anthropic.com/)
- [Claude Vision](https://docs.anthropic.com/en/docs/vision)
