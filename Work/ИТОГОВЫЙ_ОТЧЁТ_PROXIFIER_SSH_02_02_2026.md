# Итоговый отчёт: Управление Proxifier через SSH

**Дата:** 02.02.2026
**Сервер:** 62.84.101.97
**Цель:** Автоматизировать управление Proxifier без прямого подключения к серверу

---

## Результаты исследования

### ✅ Что работает

| Функция | Статус | Детали |
|---------|--------|--------|
| SSH подключение | ✅ | Стабильное подключение через paramiko |
| Изменение профиля Default.ppx | ✅ | Файл редактируется, бэкапы создаются |
| Запуск Proxifier в Session 2 | ✅ | Через Task Scheduler + InteractiveToken |
| Драйвер ProxifierDrv | ✅ | Загружен и работает (Status=Running) |
| Лицензия | ✅ | Найдена: `YTZGN-FYT53-J253L-ZQZS4-YLBN9` |
| Прокси напрямую через curl | ✅ | IP меняется на 81.177.254.254 |

### ❌ Проблема

**Proxifier запускается, но не перехватывает трафик:**
- `MainWindowHandle: 0` — нет GUI окна
- IP остаётся `62.84.101.97` (сервера)
- Нет соединений к прокси-серверу от Proxifier

### 🔍 Диагноз

Proxifier запущен как процесс, но находится в **неактивном состоянии**:
1. Возможно показывает диалог выбора профиля
2. Или ожидает подтверждения пользователя
3. Или не загрузил профиль автоматически

**Критическое ограничение Proxifier:**
- **Нет CLI** для управления (перезагрузка профиля, переключение прокси)
- Управление только через **GUI**
- При запуске из SSH сессии — GUI не полностью инициализируется

---

## Корень проблемы

### Почему Proxifier не управляется через SSH

1. **Proxifier — GUI приложение** без командной строки
2. **Task Scheduler с InteractiveToken** запускает процесс, но:
   - Не эмулирует пользовательское взаимодействие
   - Не может нажимать кнопки в диалогах
3. **SSH сессия изолирована** от интерактивной RDP сессии

### Почему прокси не применяется

Proxifier вероятно:
- Запустился но не загрузил профиль (ждёт выбора)
- Или показывает модальное окно которое блокирует работу
- Или первый запуск после манипуляций требует ручного подтверждения

---

## Решения

### Решение 1: Однократная настройка через RDP (рекомендуется)

**Действия:**
1. Подключиться по RDP к `62.84.101.97`
2. Открыть Proxifier GUI
3. Убедиться что:
   - Профиль Default.ppx загружен
   - Прокси активен (зелёная иконка)
   - Правило Default использует прокси
4. Минимизировать Proxifier в трей

После этого последующие перезапуски через SSH должны работать, так как настройки сохранятся в реестре.

### Решение 2: Агент на сервере (ProxifierAgent)

**Концепция:** PowerShell скрипт работающий в интерактивной сессии пользователя.

**Установлено:**
- `C:\ProxifierAgent\ProxifierAgent.ps1`
- Поддерживает команды: `status`, `set_proxy`, `restart`, `stop`

**Использование:**
```powershell
# Через SSH
powershell -File C:\ProxifierAgent\ProxifierAgent.ps1 -Action set_proxy -ProxyHost 185.162.130.86 -ProxyPort 10001 -ProxyUser "user" -ProxyPass "pass"
```

**Ограничение:** Агент может менять профиль и перезапускать Proxifier, но не может гарантировать что Proxifier загрузит профиль без GUI взаимодействия.

### Решение 3: UI Automation (AutoIt/SendKeys)

**Концепция:** Скрипт который:
1. Находит окно Proxifier
2. Отправляет клавиши (Alt+F → Import Profile)
3. Загружает профиль программно

**Недостаток:** Требует активное GUI окно, сложнее в реализации.

### Решение 4: Замена Proxifier

**Альтернативы с CLI:**
- **tun2socks** — кросс-платформенный, CLI управление
- **redsocks** — только Linux
- **Dante** — только Linux

**Недостаток:** Требует переход на другое ПО.

---

## Рекомендованный план действий

### Немедленно (сегодня)

1. **Подключиться по RDP** к серверу `62.84.101.97`
2. **Проверить Proxifier визуально:**
   - Если есть диалог — закрыть/подтвердить
   - Загрузить профиль Default.ppx
   - Убедиться что прокси работает (IP изменился)
3. **Свернуть Proxifier в трей**

### После настройки через RDP

Протестировать что теперь работает:
```bash
python Scripts/ProxifierAgent/test_agent.py
```

Ожидаемый результат: `IP_INTERACTIVE: 81.177.254.254` (или другой IP прокси)

### Среднесрочно

1. **Доработать ProxifierAgent** — добавить retry логику и проверку результата
2. **Интегрировать с ServerManager** — команда `set_proxy_reliable`
3. **Документировать процесс** для других серверов

---

## Файлы и скрипты

### Созданные скрипты

| Файл | Назначение |
|------|------------|
| `Scripts/ProxifierAgent/ProxifierAgent.ps1` | Агент на сервере |
| `Scripts/ProxifierAgent/install_agent_v2.py` | Установка агента |
| `Scripts/ProxifierAgent/test_agent.py` | Тестирование агента |
| `Scripts/reset_proxifier_complete.py` | Полный сброс Proxifier |
| `Scripts/final_restart_test.py` | Финальный тест |
| `Scripts/test_proxy_direct.py` | Прямой тест прокси через curl |

### Отчёты

| Файл | Содержание |
|------|------------|
| `Work/АНАЛИЗ_PROXIFIER_ПРОБЛЕМА_И_РЕШЕНИЕ.md` | Детальный технический анализ |
| `Work/ИТОГОВЫЙ_ОТЧЁТ_PROXIFIER_SSH_02_02_2026.md` | Этот файл |

---

## Ключевые выводы

1. **Proxifier нельзя полностью автоматизировать через SSH** из-за отсутствия CLI и зависимости от GUI

2. **Прокси работает** — проверено через curl с SOCKS5 (IP 81.177.254.254)

3. **Текущее решение:**
   - Однократная настройка через RDP
   - Далее управление через SSH + ProxifierAgent

4. **Для полной автоматизации** нужно либо:
   - UI Automation (AutoIt)
   - Альтернатива Proxifier с CLI
   - Агент с более глубокой интеграцией

---

## Реквизиты для работы

**Сервер:**
- IP: 62.84.101.97
- User: Administrator
- Pass: password222

**Прокси:**
- Host: 185.162.130.86
- Port: 10001
- User: 0tQfV66IulPiKVJRG6gm
- Pass: 0zOrPGFjDQcrqMT0Dq3Et5T8kz6jhseA

**Лицензия Proxifier:**
- Key: YTZGN-FYT53-J253L-ZQZS4-YLBN9
