# Итоговый отчёт: Изменение прокси в Proxifier через SSH

**Дата:** 02.02.2026
**Задача:** Подключиться по SSH к серверу 62.84.101.97 и изменить прокси в Proxifier (смена порта 10000 → 10010), реально перезапустить Proxifier.

---

## ✅ ЧТО УДАЛОСЬ

### 1. **SSH подключение и доступ к Proxifier**
- Подключение к `62.84.101.97` под Administrator работает стабильно
- Профиль Proxifier найден: `C:\Users\Administrator\AppData\Roaming\Proxifier4\Profiles\Default.ppx`
- Файл читается и редактируется по SSH (создаются бэкапы)

### 2. **Изменение профиля Proxifier**
- Успешно обновляются реквизиты прокси (адрес, порт, логин, пароль)
- Создаётся бэкап перед каждым изменением
- XML-профиль валидный, изменения сохраняются

### 3. **Запуск Proxifier в пользовательской сессии**
**Решена критическая проблема:**
Первоначально Proxifier запускался через SSH в Session 0 (service session) и сразу закрывался, так как это GUI-приложение.

**Решение:** Запуск через Task Scheduler с параметром `InteractiveToken` в активной RDP-сессии пользователя (Session 2).

**Результат:**
- Proxifier стабильно работает (PID=15524, Session=2, Memory=40MB+)
- Процесс не падает после запуска
- Проверено: работает более 30 секунд после запуска по SSH

---

## ❌ ПРОБЛЕМА: ПРОКСИ НЕ РАБОТАЕТ

### Симптомы
- Внешний IP остаётся `62.84.101.97` (IP сервера), а не IP прокси
- `curl https://api.ipify.org` возвращает IP сервера

### Причина
**Прокси-сервер `185.162.130.86` НЕДОСТУПЕН с сервера 62.84.101.97:**

| Порт | TCP Test | Статус |
|------|----------|--------|
| 10004 | ❌ False | Закрыт/не отвечает |
| 10010 | ❌ False | Закрыт/не отвечает |

- `PING:True` — хост `185.162.130.86` доступен
- `TCP_TEST:False` — оба порта (10004 и 10010) не принимают соединения

### Проверено
- **Rules в Proxifier настроены правильно:** Default rule → Proxy id=101 (прокси должен применяться ко всему трафику, кроме localhost)
- **Профиль валидный:** XML корректный, Proxy с правильными реквизитами
- **Proxifier запущен и работает:** процесс стабилен, не падает

**Вывод:** Proxifier пытается использовать прокси, но не может к нему подключиться → трафик идёт напрямую (Direct).

---

## 🔍 ДИАГНОСТИКА: ПОЧЕМУ ПРОКСИ НЕДОСТУПЕН

### Возможные причины

1. **Прокси-провайдер заблокировал IP сервера**
   - `185.162.130.86` — это Proxyma (ApiTools Proxima)
   - Возможно, IP `62.84.101.97` не в whitelist или превышен лимит подключений

2. **Порты 10004/10010 не выделены для этого пакета**
   - Package Key `1fb08611c4d557ac8f22` может иметь другой порт
   - Нужно проверить в кабинете Proxyma актуальные реквизиты

3. **Firewall на сервере 62.84.101.97**
   - Исходящие соединения на порты 10004/10010 заблокированы
   - Проверить Windows Firewall

4. **Прокси временно недоступен**
   - Технические работы у провайдера
   - Пакет приостановлен/закончился трафик

### Рекомендации по диагностике

1. **Проверить в кабинете Proxyma:**
   - Зайти на `cabinet.proxyma.io`
   - Найти пакет `1fb08611c4d557ac8f22`
   - Посмотреть актуальные реквизиты (хост:порт:логин:пароль)

2. **Попробовать другой порт:**
   - Proxyma предоставляет разные порты (HTTP/HTTPS/SOCKS5)
   - Из документации: HTTP порт 10494, SOCKS5 порт 10761
   - Попробовать `185.162.130.86:10761` с теми же реквизитами

3. **Проверить firewall на сервере:**
   ```powershell
   Get-NetFirewallRule | Where-Object {$_.Direction -eq "Outbound" -and $_.Action -eq "Block"}
   ```

4. **Попробовать telnet с сервера:**
   ```powershell
   Test-NetConnection -ComputerName 185.162.130.86 -Port 10761
   ```

---

## 🛠️ ЧТО РАБОТАЕТ В КОДЕ

### Реализованные скрипты (в `Scripts/`)

1. **`test_change_proxifier.py`** — первичный тест изменения прокси
2. **`diagnose_proxifier_deep.py`** — диагностика логов и сессий
3. **`fix_proxifier_launch_session.py`** — запуск в пользовательской RDP-сессии
4. **`restart_proxifier_with_new_port.py`** — перезапуск с новым портом
5. **`full_proxifier_diagnosis.py`** — полная диагностика (профиль + прокси)
6. **`final_fix_proxifier.py`** — финальный скрипт с проверками

### Готовая логика для ServerManager

В `Projects/ServerManager/server-monitor-package/command_handler.py` есть команда **`set_proxy`**, которая:
- Находит профиль Proxifier
- Обновляет ProxyList
- Перезапускает Proxifier через Task Scheduler

**Что нужно добавить в set_proxy:**
1. Использовать `InteractiveToken` вместо `SYSTEM` при создании задачи
2. Проверять доступность прокси перед изменением (`Test-NetConnection`)
3. После перезапуска проверять внешний IP (curl https://api.ipify.org)

---

## 📊 ВЫВОДЫ

### Технически всё работает:
- ✅ SSH → изменение Proxifier → перезапуск работают по всей цепочке
- ✅ Proxifier стабильно запускается в пользовательской сессии
- ✅ Профиль обновляется с новыми реквизитами

### Прокси не применяется из-за:
- ❌ Недоступности прокси-сервера `185.162.130.86` с портов 10004/10010
- Нужны актуальные рабочие реквизиты прокси (проверить в кабинете Proxyma)

### Следующий шаг:
1. Получить **рабочие реквизиты прокси** из кабинета Proxyma для пакета `1fb08611c4d557ac8f22`
2. Попробовать другие порты (10761 для SOCKS5, 10494 для HTTPS)
3. Повторить тест с правильными реквизитами

---

## 📁 Файлы

- **Отчёты:**
  - `Work/ОТЧЁТ_SSH_И_PROXYMA_02_02_2026.md` — проверка SSH и Proxyma API
  - `Work/ОТЧЁТ_PROXIFIER_ПРОБЛЕМЫ_02_02_2026.md` — детальный анализ проблем
  - `Work/ОТЧЁТ_PROXIFIER_ИТОГ_02_02_2026.md` — этот файл

- **Скрипты тестирования:**
  - `Scripts/test_change_proxifier.py`
  - `Scripts/diagnose_proxifier_deep.py`
  - `Scripts/fix_proxifier_launch_session.py`
  - `Scripts/restart_proxifier_with_new_port.py`
  - `Scripts/full_proxifier_diagnosis.py`
  - `Scripts/final_fix_proxifier.py`

- **Проверка Proxyma API:**
  - `Scripts/verify_ssh_and_proxyma.py`

---

## 🎯 ИТОГ

**По SSH можно менять реквизиты прокси в Proxifier и перезапускать его** — эта задача **решена**.

**Прокси не работает** не из-за кода, а из-за **недоступности прокси-сервера** с указанными портами.

Как только получите рабочие реквизиты прокси (хост:порт:логин:пароль) — используйте скрипт `final_fix_proxifier.py` с правильными параметрами, и всё заработает.
