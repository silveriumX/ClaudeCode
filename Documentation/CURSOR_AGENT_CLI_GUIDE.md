# 🚀 Полное руководство по Cursor Agent CLI

**Версия:** 2026.01.23
**Дата:** 27.01.2026
**Цель:** Максимально эффективное использование всех возможностей Agent CLI

---

## 📋 СОДЕРЖАНИЕ

1. [Первоначальная настройка](#первоначальная-настройка)
2. [Основные режимы работы](#основные-режимы-работы)
3. [Практические сценарии использования](#практические-сценарии-использования)
4. [Продвинутые возможности](#продвинутые-возможности)
5. [Автоматизация и скрипты](#автоматизация-и-скрипты)
6. [Best Practices](#best-practices)
7. [Troubleshooting](#troubleshooting)

---

# 🔧 ПЕРВОНАЧАЛЬНАЯ НАСТРОЙКА

## Шаг 1: Аутентификация

```powershell
# Войти в аккаунт Cursor
agent login

# Проверить статус
agent status

# Посмотреть доступные модели
agent models
```

**Что происходит при `agent login`:**
- Открывается браузер для аутентификации
- Токен сохраняется локально
- Получаете доступ ко всем моделям по подписке

**Если браузер не должен открываться:**
```powershell
$env:NO_OPEN_BROWSER = "1"
agent login
```

## Шаг 2: Проверка установки

```powershell
# Версия
agent --version

# Полная информация о системе
agent about

# Список всех команд
agent --help
```

---

# 🎯 ОСНОВНЫЕ РЕЖИМЫ РАБОТЫ

## Режим 1: Интерактивный Agent (по умолчанию)

**Самый мощный режим** - полный доступ ко всем инструментам.

```powershell
# Запуск с начальной задачей
agent "создай систему авторизации с JWT"

# Запуск без задачи (введете позже)
agent

# Запуск в конкретной директории
agent --workspace "C:\Projects\MyApp" "добавь логирование"
```

**Что может делать:**
- ✅ Читать файлы
- ✅ Редактировать файлы
- ✅ Создавать файлы
- ✅ Удалять файлы
- ✅ Выполнять команды в терминале
- ✅ Искать в коде
- ✅ Работать с Git
- ✅ Устанавливать пакеты
- ✅ Запускать тесты

**Пример сессии:**
```powershell
PS> agent "оптимизируй performance этого API"

Agent: Анализирую код API...
Agent: Обнаружил 3 проблемы:
       1. N+1 queries в /api/users
       2. Отсутствие кеширования
       3. Неэффективная сериализация

Agent: Предлагаю исправить. Продолжить? [y/n]
User: y

Agent: [Редактирует файлы...]
Agent: Готово! Производительность улучшена на ~60%.
```

---

## Режим 2: Plan Mode (только планирование)

**Read-only режим** для анализа и составления планов **без изменений**.

```powershell
# Запуск Plan mode
agent --plan "проанализируй архитектуру и предложи улучшения"

# Или
agent --mode plan "как рефакторить эту систему?"
```

**Что может делать:**
- ✅ Читать файлы
- ✅ Анализировать код
- ✅ Предлагать решения
- ✅ Создавать планы
- ❌ НЕ может редактировать
- ❌ НЕ может создавать файлы
- ❌ НЕ может выполнять команды

**Когда использовать:**
- 🔍 Исследование незнакомого кодбейса
- 📊 Анализ архитектуры
- 🎯 Планирование больших изменений
- 💡 Получение рекомендаций
- 🔒 Когда нужна безопасность (не испортит код)

**Пример:**
```powershell
PS> agent --plan "как лучше организовать эту базу данных?"

Agent: Анализирую структуру БД...
Agent:
Текущая структура:
- 15 таблиц
- Нет индексов на внешних ключах
- Дублирование данных в 3 местах

План оптимизации:
1. Нормализация таблиц users/profiles
2. Добавление индексов (ожидаемый прирост: 3-5x)
3. Партиционирование таблицы logs
4. Введение материализованных представлений

Ориентировочное время: 2-3 дня
Риски: Нужен downtime для миграции (2-4 часа)

Хотите чтобы я создал детальный план миграции?
```

---

## Режим 3: Ask Mode (Q&A)

**Read-only режим** для ответов на вопросы о коде.

```powershell
# Запуск Ask mode
agent --mode ask "как работает эта функция authenticate()?"

# Вопросы об ошибках
agent --mode ask "почему тесты падают?"

# Объяснение кода
agent --mode ask "объясни эту регулярку"
```

**Что может делать:**
- ✅ Читать код
- ✅ Объяснять логику
- ✅ Находить баги
- ✅ Документировать
- ❌ НЕ может редактировать
- ❌ НЕ может создавать файлы

**Когда использовать:**
- ❓ Изучение чужого кода
- 🐛 Дебаг (без изменений)
- 📚 Генерация документации
- 🎓 Обучение

---

## Режим 4: Headless Mode (для автоматизации)

**Неинтерактивный режим** для скриптов и CI/CD.

```powershell
# Базовый headless mode
agent --print "найди все TODO комментарии"

# JSON вывод для парсинга
agent --print --output-format json "проверь безопасность"

# Stream JSON (для real-time обработки)
agent --print --output-format stream-json --stream-partial-output "код ревью"
```

**Форматы вывода:**
- `text` - обычный текст (по умолчанию)
- `json` - JSON объект с результатом
- `stream-json` - потоковый JSON (каждая строка = JSON объект)

**Особенности:**
- Автоматически одобряет все действия (с `--force`)
- Не требует интерактивного ввода
- Идеален для автоматизации
- Можно использовать в CI/CD

---

# 💼 ПРАКТИЧЕСКИЕ СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ

## Сценарий 1: Разработка новой функции

```powershell
# Шаг 1: Планирование
agent --plan "добавить функцию экспорта в PDF"

# Агент предложит план реализации

# Шаг 2: Реализация
agent "реализуй план экспорта в PDF из предыдущего чата"

# Или с resume предыдущей сессии
agent --continue "реализуй план"
```

---

## Сценарий 2: Рефакторинг

```powershell
# Анализ
agent --plan "найди проблемные места в архитектуре"

# Рефакторинг по частям
agent "рефактори модуль auth согласно SOLID"
agent "рефактори модуль database"
agent "обнови тесты после рефакторинга"
```

---

## Сценарий 3: Code Review

```powershell
# Обзор изменений в Git
agent "сделай код ревью последнего коммита"

# Проверка безопасности
agent "проверь код на уязвимости безопасности"

# Проверка производительности
agent "найди узкие места по производительности"
```

---

## Сценарий 4: Debugging

```powershell
# Режим вопросов для анализа
agent --mode ask "почему функция getUsers() возвращает null?"

# Затем fix в обычном режиме
agent "исправь баг в getUsers()"
```

---

## Сценарий 5: Документация

```powershell
# Генерация README
agent "создай подробный README для этого проекта"

# API документация
agent "создай OpenAPI спецификацию для всех эндпоинтов"

# Комментарии к коду
agent "добавь JSDoc комментарии ко всем функциям в src/"

# Changelog
agent "создай CHANGELOG.md на основе git истории"
```

---

## Сценарий 6: Тестирование

```powershell
# Создание тестов
agent "создай unit тесты для auth.service.ts"

# Покрытие тестами
agent "проверь coverage и добавь тесты где их нет"

# Интеграционные тесты
agent "создай интеграционные тесты для API"

# Запуск и исправление
agent "запусти тесты и исправь все падающие"
```

---

## Сценарий 7: Миграция

```powershell
# Миграция с JavaScript на TypeScript
agent "конвертируй все .js файлы в .ts с типами"

# Миграция библиотек
agent "обнови React 17 до React 18"

# Миграция базы данных
agent "создай SQL миграцию для новой схемы users"
```

---

## Сценарий 8: CI/CD Integration

```powershell
# В GitHub Actions / GitLab CI
agent --print --force "запусти тесты и создай отчет о покрытии"

# Pre-commit hook
agent --print "проверь код на соответствие стандартам"

# Автоматический код ревью
agent --print --output-format json "найди потенциальные баги" > review.json
```

---

# 🔥 ПРОДВИНУТЫЕ ВОЗМОЖНОСТИ

## 1. Работа с сессиями

```powershell
# Создать новую пустую сессию
agent create-chat
# Вернет ID: chat-abc123

# Продолжить конкретную сессию
agent --resume chat-abc123 "продолжи работу"

# Продолжить последнюю сессию
agent --continue

# Посмотреть все сессии
agent ls
```

**Когда использовать:**
- 📝 Длинные задачи (разбить на сессии)
- 🔄 Итеративная разработка
- 👥 Работа в команде (шарить ID сессии)

---

## 2. Выбор модели

```powershell
# Список доступных моделей
agent models

# Использование конкретной модели
agent --model sonnet-4 "сложная задача требующая reasoning"
agent --model gpt-5 "другая задача"
agent --model sonnet-4-thinking "задача требующая глубокого анализа"
```

**Выбор модели:**
- `sonnet-4` - Claude Sonnet 4 (баланс скорость/качество)
- `sonnet-4-thinking` - Claude с extended thinking (сложные задачи)
- `gpt-5` - GPT-5 от OpenAI
- `gpt-4.5` - GPT-4.5 (быстрее GPT-5)

---

## 3. Custom Headers и API Keys

```powershell
# Свой API ключ (альтернатива логину)
agent --api-key "your-api-key" "задача"

# Или через env переменную
$env:CURSOR_API_KEY = "your-api-key"
agent "задача"

# Custom headers
agent -H "X-Custom-Header: value" -H "X-Another: value2" "задача"
```

**Когда использовать:**
- 🏢 Корпоративные окружения
- 🔐 Разные аккаунты для разных проектов
- 🌐 Прокси и специфичные настройки

---

## 4. Browser Automation

```powershell
# Включить поддержку браузера
agent --browser "создай E2E тесты с Playwright"
agent --browser "протестируй форму регистрации на localhost:3000"
```

**Что может делать:**
- 🌐 Открывать веб-страницы
- 🖱️ Кликать, заполнять формы
- 📸 Делать скриншоты
- ✅ Тестировать UI

---

## 5. MCP Servers (Model Context Protocol)

```powershell
# Управление MCP серверами
agent mcp --help

# Автоматическое одобрение MCP серверов в headless режиме
agent --print --approve-mcps "задача с MCP"
```

**MCP серверы** - это плагины для расширения возможностей агента.

---

## 6. Sandbox Mode

```powershell
# Включить sandbox (безопасный режим)
agent --sandbox enabled "опасная задача"

# Отключить sandbox
agent --sandbox disabled "нужен полный доступ"
```

**Sandbox mode:**
- Ограничивает действия агента
- Предотвращает опасные операции
- Полезно при тестировании

---

## 7. Force Mode

```powershell
# Автоматически разрешать все действия
agent --force "большая задача с множеством изменений"
```

**⚠️ Осторожно:**
- Агент НЕ будет спрашивать подтверждения
- Использовать только в безопасном окружении
- Идеально для автоматизации

---

# 🤖 АВТОМАТИЗАЦИЯ И СКРИПТЫ

## Bash/PowerShell скрипты

### Скрипт 1: Ежедневная проверка кода

```powershell
# daily-check.ps1
Write-Host "🔍 Ежедневная проверка кода..."

# Проверка безопасности
$security = agent --print --force "проверь безопасность, найди SQL injection, XSS, CSRF"

# Проверка производительности
$perf = agent --print --force "найди узкие места по производительности"

# Проверка стиля кода
$style = agent --print --force "проверь соответствие code style"

# Отправка отчета
$report = @"
ЕЖЕДНЕВНЫЙ ОТЧЕТ
================
Безопасность: $security
Производительность: $perf
Стиль кода: $style
"@

$report | Out-File "daily-report-$(Get-Date -Format 'yyyy-MM-dd').txt"
Write-Host "✅ Отчет сохранен"
```

### Скрипт 2: Автоматическое обновление документации

```powershell
# update-docs.ps1
agent --print --force "обнови README на основе изменений в коде"
agent --print --force "обнови API документацию"
agent --print --force "создай changelog для последнего релиза"

git add .
git commit -m "docs: автоматическое обновление документации"
```

### Скрипт 3: Pre-commit hook

```powershell
# .git/hooks/pre-commit
#!/usr/bin/env pwsh

Write-Host "🔍 Проверка перед коммитом..."

# Проверка на TODO/FIXME
$todos = agent --print "найди все TODO и FIXME комментарии"
if ($todos -match "TODO|FIXME") {
    Write-Host "⚠️ Найдены TODO/FIXME комментарии"
}

# Проверка на console.log (для production)
$consoles = agent --print "найди все console.log в коде"
if ($consoles -match "console\.log") {
    Write-Host "❌ Найдены console.log. Удали их перед коммитом."
    exit 1
}

# Проверка тестов
$tests = agent --print "запусти тесты"
if ($LASTEXITCODE -ne 0) {
    Write-Host "❌ Тесты упали. Исправь перед коммитом."
    exit 1
}

Write-Host "✅ Все проверки пройдены"
```

---

## CI/CD интеграция

### GitHub Actions

```yaml
# .github/workflows/agent-review.yml
name: Cursor Agent Code Review

on: [pull_request]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Cursor Agent
        run: curl https://cursor.com/install -fsS | bash

      - name: Login
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
        run: agent login

      - name: Code Review
        run: |
          agent --print --force "сделай код ревью изменений в этом PR" > review.txt
          cat review.txt >> $GITHUB_STEP_SUMMARY

      - name: Security Check
        run: |
          agent --print --force "проверь безопасность кода" > security.txt
          cat security.txt
```

### GitLab CI

```yaml
# .gitlab-ci.yml
agent-review:
  stage: test
  script:
    - curl https://cursor.com/install -fsS | bash
    - export CURSOR_API_KEY=$CURSOR_API_KEY
    - agent --print --force "код ревью" > review.txt
    - cat review.txt
  artifacts:
    paths:
      - review.txt
```

---

# 🎓 BEST PRACTICES

## 1. Четкие и конкретные промпты

### ❌ Плохо:
```powershell
agent "исправь код"
agent "улучши это"
agent "сделай лучше"
```

### ✅ Хорошо:
```powershell
agent "исправь SQL injection уязвимость в функции login()"
agent "оптимизируй запрос к БД в getUserPosts() используя eager loading"
agent "рефактори класс UserService согласно Single Responsibility Principle"
```

---

## 2. Разбивайте большие задачи

### ❌ Плохо:
```powershell
agent "создай полную систему аутентификации с JWT, OAuth, 2FA, password reset, email verification"
```

### ✅ Хорошо:
```powershell
# Шаг 1: План
agent --plan "спроектируй систему аутентификации с JWT, OAuth, 2FA"

# Шаг 2: Базовая аутентификация
agent "создай JWT аутентификацию с login/register"

# Шаг 3: OAuth
agent --continue "добавь OAuth (Google, GitHub)"

# Шаг 4: 2FA
agent --continue "добавь 2FA с TOTP"

# Шаг 5: Дополнительно
agent --continue "добавь password reset и email verification"
```

---

## 3. Используйте правильный режим

| Задача | Режим | Почему |
|--------|-------|--------|
| Изучение кода | `--mode ask` | Read-only, не испортит |
| Планирование архитектуры | `--plan` | Сначала план, потом код |
| Быстрый фикс | обычный | Полный доступ |
| Code review | `--mode ask` | Только анализ |
| Автоматизация | `--print --force` | Без взаимодействия |
| Опасные операции | `--sandbox enabled` | Безопасность |

---

## 4. Контекст и workspace

```powershell
# ❌ Плохо: агент не знает где работать
cd C:\
agent "добавь feature X"

# ✅ Хорошо: явно указываем workspace
agent --workspace "C:\Projects\MyApp" "добавь feature X"

# ✅ Или работаем в нужной директории
cd C:\Projects\MyApp
agent "добавь feature X"
```

---

## 5. Используйте сессии для связанных задач

```powershell
# День 1: Начало
agent "создай REST API для users"
# Сохраните chat ID

# День 2: Продолжение
agent --resume chat-abc123 "добавь пагинацию к API"

# День 3: Завершение
agent --resume chat-abc123 "добавь фильтрацию и сортировку"
```

**Преимущества:**
- 🧠 Агент помнит контекст
- 📝 История решений
- 🔄 Итеративная разработка

---

## 6. Проверяйте результаты

```powershell
# После изменений агента
agent "добавь валидацию email"

# Проверьте
git diff
npm test

# Если что-то не так
agent "тесты падают, исправь валидацию email"
```

---

## 7. Комбинируйте с Git

```powershell
# Создайте ветку для экспериментов
git checkout -b feature/new-thing

# Работа с агентом
agent "реализуй feature X"

# Проверка
git diff
git log

# Если всё ок
git commit -am "feat: добавлен feature X"

# Если нет - откат
git reset --hard
```

---

# 🔧 TROUBLESHOOTING

## Проблема 1: "Not logged in"

```powershell
# Решение
agent login

# Проверка
agent status
```

---

## Проблема 2: Агент слишком много спрашивает

```powershell
# Используйте force mode
agent --force "большая задача"
```

---

## Проблема 3: Агент не видит файлы

```powershell
# Проверьте workspace
agent --workspace "C:\правильный\путь" "задача"

# Или явно укажите файлы в промпте
agent "отредактируй файл src/auth/login.ts"
```

---

## Проблема 4: Медленная работа

```powershell
# Используйте более быстрые модели
agent --model gpt-4.5 "быстрая задача"

# Или разбейте на подзадачи
agent "только исправь эту функцию" # вместо "рефактори весь проект"
```

---

## Проблема 5: Неправильные изменения

```powershell
# Используйте Plan mode сначала
agent --plan "как лучше реализовать это?"
# Изучите план
# Затем реализуйте
agent "реализуй предложенный план"

# Или Ask mode для понимания
agent --mode ask "что делает эта функция?"
```

---

## Проблема 6: Нужно откатить изменения

```powershell
# Используйте Git
git status
git diff
git restore .  # откат всех изменений

# Или откат конкретного файла
git restore path/to/file.ts
```

---

# 💡 СОВЕТЫ ПРО-УРОВНЯ

## 1. Создайте алиасы для частых команд

```powershell
# В профиле PowerShell ($PROFILE)

# Быстрый план
function aplan { agent --plan $args }

# Быстрый вопрос
function aask { agent --mode ask $args }

# Продолжить последнюю сессию
function acont { agent --continue $args }

# Code review текущих изменений
function areview { agent --mode ask "сделай код ревью git diff" }

# Использование
aplan "как организовать БД?"
aask "что делает эта функция?"
acont "продолжи"
areview
```

---

## 2. Шаблоны задач

Создайте файл с шаблонами:

```powershell
# templates.txt

# Code Review Template
agent --mode ask "
Сделай code review учитывая:
1. Безопасность (SQL injection, XSS, CSRF)
2. Производительность (N+1, кеширование)
3. Читаемость (naming, структура)
4. Тесты (coverage, edge cases)
5. Best practices (SOLID, DRY, KISS)
"

# Feature Template
agent --plan "
Спроектируй feature: [NAME]

Требования:
- [REQ1]
- [REQ2]

Constraints:
- [CONST1]

Создай:
1. Архитектуру
2. API контракты
3. Схему БД
4. План тестирования
"
```

---

## 3. Workflow для крупных задач

```powershell
# 1. ИССЛЕДОВАНИЕ (Ask mode)
agent --mode ask "объясни текущую архитектуру auth модуля"

# 2. ПЛАНИРОВАНИЕ (Plan mode)
agent --plan "как добавить OAuth не ломая текущую систему?"

# 3. ПОДГОТОВКА (обычный режим, подготовительные задачи)
agent "создай миграцию БД для OAuth"
agent "добавь зависимости для OAuth"

# 4. РЕАЛИЗАЦИЯ (обычный режим, с сохранением сессии)
$chatId = agent create-chat
agent --resume $chatId "реализуй OAuth провайдер для Google"
agent --resume $chatId "добавь UI для OAuth логина"
agent --resume $chatId "интегрируй с текущей системой"

# 5. ТЕСТИРОВАНИЕ
agent --resume $chatId "создай тесты для OAuth"
agent --resume $chatId "запусти тесты и исправь ошибки"

# 6. ДОКУМЕНТАЦИЯ
agent --resume $chatId "обнови документацию с описанием OAuth"

# 7. CODE REVIEW
agent --mode ask "код ревью всех изменений по OAuth"
```

---

## 4. Интеграция с другими инструментами

```powershell
# С Jira
agent "создай тикет в Jira для найденных багов"

# С Slack
$review = agent --print "код ревью"
Send-SlackMessage -Channel dev -Message $review

# С базой знаний
agent "создай статью в Confluence о новом API"
```

---

# 📚 ПОЛЕЗНЫЕ КОМАНДЫ (CHEAT SHEET)

```powershell
# ═══════════════════════════════════════════════════
# БАЗОВЫЕ
# ═══════════════════════════════════════════════════
agent --version                              # Версия
agent --help                                 # Справка
agent about                                  # Полная информация
agent login                                  # Войти
agent logout                                 # Выйти
agent status                                 # Статус аутентификации

# ═══════════════════════════════════════════════════
# МОДЕЛИ
# ═══════════════════════════════════════════════════
agent models                                 # Список моделей
agent --model sonnet-4 "задача"             # Конкретная модель
agent --list-models                         # Показать модели и выйти

# ═══════════════════════════════════════════════════
# РЕЖИМЫ
# ═══════════════════════════════════════════════════
agent "задача"                              # Интерактивный (полный доступ)
agent --plan "задача"                       # Plan mode (read-only)
agent --mode ask "вопрос"                   # Ask mode (Q&A)
agent --print "задача"                      # Headless mode

# ═══════════════════════════════════════════════════
# СЕССИИ
# ═══════════════════════════════════════════════════
agent create-chat                           # Создать новую сессию
agent --resume chat-id "задача"             # Продолжить сессию
agent --continue                            # Продолжить последнюю
agent ls                                    # Список сессий

# ═══════════════════════════════════════════════════
# WORKSPACE
# ═══════════════════════════════════════════════════
agent --workspace "путь" "задача"           # Указать workspace
agent "задача"                              # Текущая директория

# ═══════════════════════════════════════════════════
# HEADLESS / АВТОМАТИЗАЦИЯ
# ═══════════════════════════════════════════════════
agent --print "задача"                      # Вывод в консоль
agent --print --output-format json "задача" # JSON вывод
agent --print --force "задача"              # Без подтверждений
agent --approve-mcps --print "задача"       # Автоодобрение MCP

# ═══════════════════════════════════════════════════
# ПРОДВИНУТОЕ
# ═══════════════════════════════════════════════════
agent --browser "задача"                    # С поддержкой браузера
agent --sandbox enabled "задача"            # Sandbox режим
agent -H "Header: value" "задача"           # Custom headers
agent --api-key "key" "задача"              # Свой API ключ

# ═══════════════════════════════════════════════════
# ПРАВИЛА (Rules)
# ═══════════════════════════════════════════════════
agent generate-rule                         # Создать правило интерактивно
agent rule                                  # Alias для generate-rule

# ═══════════════════════════════════════════════════
# ОБНОВЛЕНИЕ
# ═══════════════════════════════════════════════════
agent update                                # Обновить до последней версии
agent upgrade                               # Alias для update
```

---

# 🎬 ПРИМЕРЫ ИЗ РЕАЛЬНОЙ ЖИЗНИ

## Пример 1: Создание нового проекта с нуля

```powershell
# Создаем директорию
mkdir MyNewProject
cd MyNewProject

# План проекта
agent --plan "создай план для REST API на Node.js + Express + PostgreSQL с аутентификацией"

# Инициализация
agent "создай package.json с необходимыми зависимостями"
agent "создай базовую структуру проекта (src/, tests/, config/)"
agent "настрой TypeScript"
agent "настрой ESLint и Prettier"

# Реализация
agent "создай Express сервер с базовыми роутами"
agent "настрой подключение к PostgreSQL"
agent "создай модель User и миграции"
agent "реализуй JWT аутентификацию"
agent "добавь middleware для валидации"

# Тестирование
agent "создай unit тесты для auth модуля"
agent "создай интеграционные тесты для API"

# Документация
agent "создай README с инструкциями по установке"
agent "создай API документацию в формате OpenAPI"

# CI/CD
agent "создай GitHub Actions workflow для тестов"
agent "создай Dockerfile"

# Готово!
npm test
```

**Время:** 15-30 минут (vs 2-4 часа вручную)

---

## Пример 2: Миграция legacy кода

```powershell
cd legacy-project

# Анализ
agent --mode ask "проанализируй архитектуру этого проекта и найди проблемы"

# План миграции
agent --plan "создай план миграции на TypeScript + современный стек"

# Поэтапная миграция
agent "конвертируй модуль utils в TypeScript"
agent "обнови тесты для utils"
npm test  # проверка

agent "конвертируй модуль services в TypeScript"
agent "обнови тесты для services"
npm test

# ... продолжаем для каждого модуля

# Финальная проверка
agent "проверь весь код на соответствие TypeScript best practices"
agent "оптимизируй импорты и удали неиспользуемый код"
```

---

## Пример 3: Исправление критического бага в production

```powershell
# Создаем hotfix ветку
git checkout -b hotfix/critical-bug

# Быстрая диагностика
agent --mode ask "почему функция processPayment() иногда возвращает null?"

# Анализ логов
agent --mode ask "проанализируй логи и найди причину"

# Исправление
agent "исправь баг в processPayment() чтобы всегда возвращать результат"

# Тесты
agent "создай тест который воспроизводит баг"
agent "запусти тесты и убедись что баг исправлен"

# Проверка
npm test
git diff

# Деплой
git commit -am "hotfix: исправлен null в processPayment()"
git push origin hotfix/critical-bug
```

**Время:** 5-10 минут (vs 30-60 минут вручную)

---

# 🏆 ВЫВОДЫ

## Ключевые преимущества Agent CLI:

1. **Скорость** - задачи выполняются в 3-5 раз быстрее
2. **Качество** - AI знает best practices
3. **Автоматизация** - можно встроить в CI/CD
4. **Гибкость** - разные режимы для разных задач
5. **Обучение** - Ask mode как персональный ментор

## Когда использовать Agent CLI:

✅ Рутинные задачи (тесты, документация)
✅ Рефакторинг и миграции
✅ Быстрые фиксы
✅ Code review
✅ Обучение и исследование кода
✅ Автоматизация проверок

## Когда НЕ использовать:

❌ Критичные изменения без проверки
❌ Сложная бизнес-логика (нужен человек)
❌ Принятие архитектурных решений
❌ Работа с конфиденциальными данными (без sandbox)

---

**Начните с малого:**
1. Попробуйте `agent --mode ask` для изучения кода
2. Используйте `agent --plan` для планирования
3. Постепенно переходите к полноценному использованию
4. Создайте свои алиасы и workflows

**Удачи в использовании Cursor Agent CLI! 🚀**

---

**Версия:** 1.0
**Дата:** 27.01.2026
**Автор:** AI Assistant
**Лицензия:** Free to use
