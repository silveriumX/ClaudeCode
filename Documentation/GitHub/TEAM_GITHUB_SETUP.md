# Настройка командной работы через GitHub и Cursor

## 📋 Оглавление
1. [Структура репозиториев](#структура-репозиториев)
2. [Создание командного репозитория](#создание-командного-репозитория)
3. [Добавление сотрудников](#добавление-сотрудников)
4. [Работа в Cursor](#работа-в-cursor)
5. [Git Workflow для команды](#git-workflow-для-команды)
6. [Защита конфиденциальных данных](#защита-конфиденциальных-данных)

---

## 📁 Структура репозиториев

### Рекомендуемая схема:

```
📦 cursor-workspace (Private)          ← Личный репозиторий
├── Задачи/
├── Финансы/
├── Личные разборы/
└── Зарплаты/

📦 team-workspace (Private)            ← Командный репозиторий
├── CreatorBot/
├── VoiceBot/
├── Парсер USDT кошельков/
├── Управление выдачей ВБ/
└── README.md
```

**Преимущества:**
- ✅ Четкое разделение личного и рабочего
- ✅ Разные права доступа
- ✅ Безопасность конфиденциальных данных
- ✅ Независимые истории изменений

---

## 🚀 Создание командного репозитория

### Шаг 1: Создать новый репозиторий на GitHub

1. Зайди на https://github.com/new
2. Заполни:
   - **Repository name:** `team-workspace`
   - **Description:** `Командное рабочее пространство`
   - **Private** ✓ (ВАЖНО!)
   - **Add .gitignore:** Python (если используете)
   - **Add README:** ✓
3. **Create repository**

### Шаг 2: Создать структуру папок локально

```powershell
# Создать отдельную папку для командной работы
cd C:\Users\Admin\Documents\
mkdir TeamCursor
cd TeamCursor

# Клонировать пустой репозиторий
git clone https://github.com/USERNAME/team-workspace.git
cd team-workspace
```

### Шаг 3: Перенести командные проекты

```powershell
# Скопировать нужные папки из личного workspace
Copy-Item "C:\Users\Admin\Documents\Cursor\CreatorBot" -Destination . -Recurse
Copy-Item "C:\Users\Admin\Documents\Cursor\VoiceBot" -Destination . -Recurse
Copy-Item "C:\Users\Admin\Documents\Cursor\Парсер USDT кошельков" -Destination . -Recurse

# Добавить и закоммитить
git add .
git commit -m "Initial team workspace setup"
git push origin main
```

---

## 👥 Добавление сотрудников

### Вариант A: Добавить коллабораторов (для маленькой команды, до 5 человек)

1. Перейди в репозиторий: `https://github.com/USERNAME/team-workspace`
2. **Settings** → **Collaborators** → **Add people**
3. Введи GitHub username сотрудника
4. Выбери уровень доступа:
   - **Write** - могут пушить код (для разработчиков)
   - **Read** - только чтение (для наблюдателей)
   - **Admin** - полный доступ (для тимлидов)

5. Сотрудник получит email-приглашение

### Вариант B: Создать GitHub Organization (для большой команды, 5+ человек)

1. https://github.com/account/organizations/new
2. **Organization name:** `your-company-name`
3. **Contact email:** ваш email
4. **Free plan** → **Next**
5. Пропустить опросы → **Complete setup**

**Преимущества Organization:**
- Неограниченное количество участников
- Управление командами (Teams)
- Детальные права доступа
- Профессиональный вид

#### Создать репозиторий в Organization:

1. В Organization: **New repository**
2. Настройки аналогично, но owner будет Organization
3. **Settings** → **Teams** → **Create team**
4. Добавить участников в команду
5. Назначить права команде на репозиторий

---

## 💻 Работа в Cursor

### Вариант 1: Открывать разные папки (ПРОСТОЙ)

**Для личной работы:**
```powershell
cd C:\Users\Admin\Documents\Cursor
cursor .
```

**Для командной работы:**
```powershell
cd C:\Users\Admin\Documents\TeamCursor\team-workspace
cursor .
```

**Плюсы:**
- ✅ Простота
- ✅ Нет путаницы
- ✅ Разные настройки для разных проектов

### Вариант 2: Multi-root Workspace (ПРОДВИНУТЫЙ)

Создать файл `.code-workspace`:

```json
{
  "folders": [
    {
      "name": "Личное",
      "path": "C:\\Users\\Admin\\Documents\\Cursor"
    },
    {
      "name": "Командное",
      "path": "C:\\Users\\Admin\\Documents\\TeamCursor\\team-workspace"
    }
  ],
  "settings": {}
}
```

Сохрани как `combined-workspace.code-workspace` и открывай через Cursor.

**Плюсы:**
- ✅ Всё в одном окне
- ✅ Легко переключаться

**Минусы:**
- ⚠️ Нужно следить за тем, в какую папку коммитишь

---

## 🔄 Git Workflow для команды

### Защита главной ветки (Branch Protection)

1. **Settings** → **Branches** → **Add rule**
2. **Branch name pattern:** `main`
3. Включи:
   - ✓ **Require pull request before merging**
   - ✓ **Require approvals:** 1
   - ✓ **Dismiss stale reviews** (при новых изменениях)

**Это предотвратит прямые пуши в main!**

### Рабочий процесс для сотрудников:

#### 1. Клонировать репозиторий (первый раз)
```powershell
git clone https://github.com/USERNAME/team-workspace.git
cd team-workspace
```

#### 2. Создать ветку для задачи
```powershell
# Обновить main
git checkout main
git pull origin main

# Создать новую ветку
git checkout -b feature/new-bot-function
```

**Схема именования веток:**
- `feature/название` - новые функции
- `fix/название` - исправления
- `docs/название` - документация

#### 3. Работать и коммитить
```powershell
# Сделать изменения
# ...

# Добавить файлы
git add .

# Закоммитить с понятным сообщением
git commit -m "feat: добавил обработку ошибок в VoiceBot"
```

**Формат commit message:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `refactor:` - рефакторинг
- `test:` - тесты

#### 4. Запушить ветку
```powershell
git push origin feature/new-bot-function
```

#### 5. Создать Pull Request (PR)

На GitHub появится кнопка **"Compare & pull request"**

**В PR укажи:**
- Что сделано
- Почему (ссылка на задачу, если есть)
- Как тестировать

#### 6. Code Review

Ты или другой участник проверяет код:
- **Approve** ✅ - всё ок, можно мержить
- **Request changes** ❌ - нужны исправления
- **Comment** 💬 - просто комментарий

#### 7. Смержить в main

После approve нажми **"Merge pull request"**

Выбери тип:
- **Merge commit** - сохраняет всю историю
- **Squash and merge** - объединяет коммиты (РЕКОМЕНДУЕТСЯ)
- **Rebase and merge** - линейная история

#### 8. Удалить ветку
После мержа ветку можно удалить (GitHub предложит автоматически)

---

## 🔐 Защита конфиденциальных данных

### В командном репозитории создай `.gitignore`:

```gitignore
# Credentials и секреты
*CREDENTIALS*.md
*.env
.env.*
secrets/
credentials/

# Личные данные
Зарплаты/
Финансы/
*Личн*/

# API ключи и токены
*.token
*.key
api_keys.txt

# Системные файлы
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/

# Logs
*.log
logs/

# Временные файлы
*.tmp
*.temp
~$*
```

### Создай шаблон для credentials:

**В репозитории:** `CREDENTIALS.example.md`
```markdown
# Credentials Template

## Telegram Bot
- Bot Token: `YOUR_BOT_TOKEN_HERE`
- Chat ID: `YOUR_CHAT_ID_HERE`

## Google Sheets
- Service Account: `path/to/credentials.json`

## OpenAI
- API Key: `YOUR_OPENAI_KEY_HERE`
```

**Каждый сотрудник:**
1. Копирует `CREDENTIALS.example.md` → `CREDENTIALS.md`
2. Заполняет своими данными
3. Файл `CREDENTIALS.md` не коммитится (в `.gitignore`)

### Альтернатива: GitHub Secrets (для production)

1. **Settings** → **Secrets and variables** → **Actions**
2. **New repository secret**
3. Добавь секреты: `TELEGRAM_TOKEN`, `OPENAI_KEY`, и т.д.

Использование в коде:
```python
import os
token = os.environ.get('TELEGRAM_TOKEN')
```

---

## 📝 Рекомендации по структуре командного проекта

### Создай README.md в корне:

```markdown
# Team Workspace

## Проекты
- [CreatorBot](./CreatorBot/) - бот для создания контента
- [VoiceBot](./VoiceBot/) - голосовой бот
- [Парсер USDT](./Парсер%20USDT%20кошельков/) - парсер кошельков

## Быстрый старт
1. Клонировать репозиторий
2. Скопировать `CREDENTIALS.example.md` → `CREDENTIALS.md`
3. Заполнить свои credentials
4. Установить зависимости (см. документацию проекта)

## Workflow
1. Создай ветку от `main`
2. Сделай изменения
3. Создай Pull Request
4. Дождись review
5. Мержим!

## Контакты
- Админ: @your_telegram
- Issues: используй GitHub Issues
```

### В каждом проекте создай README.md:

```markdown
# CreatorBot

## Описание
Краткое описание бота

## Установка
\`\`\`bash
pip install -r requirements.txt
\`\`\`

## Настройка
1. Скопируй credentials
2. Настрой Google Sheets
3. ...

## Запуск
\`\`\`bash
python bot.py
\`\`\`

## Документация
См. [SETUP_GUIDE.md](./SETUP_GUIDE.md)
```

---

## 🔧 Автоматизация для командного репозитория

### Создай отдельный auto_commit для команды:

`C:\Users\Admin\Documents\TeamCursor\team-workspace\auto_commit_team.ps1`

```powershell
$repoPath = "C:\Users\Admin\Documents\TeamCursor\team-workspace"
cd $repoPath

# Проверить есть ли изменения
$status = git status --porcelain
if ($status) {
    $timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"

    git add .
    git commit -m "auto: $timestamp"

    # НЕ автопушим! Работаем через branches
    Write-Host "Changes committed. Use 'git push' manually or create PR."
} else {
    Write-Host "No changes to commit"
}
```

**Не автопушим в main!** Работаем через ветки и PR.

---

## ⚠️ Важные правила для команды

### DO ✅
- Всегда создавай ветку для задачи
- Пиши понятные commit messages
- Делай pull перед началом работы
- Проси code review перед мержем
- Документируй сложные решения
- Используй `.gitignore`

### DON'T ❌
- Не пуш напрямую в `main`
- Не коммить credentials
- Не коммить огромные файлы (>50MB)
- Не удалять чужой код без обсуждения
- Не мержить свои PR без review

---

## 🆘 Частые проблемы

### Конфликты при pull

```powershell
# Обновляем main
git checkout main
git pull origin main

# Переходим в свою ветку
git checkout feature/my-branch

# Мержим изменения из main
git merge main

# Если конфликты - разреши их в Cursor
# Затем:
git add .
git commit -m "merge: resolved conflicts with main"
git push origin feature/my-branch
```

### Случайно закоммитил credentials

```powershell
# НЕМЕДЛЕННО удали из истории:
git rm --cached CREDENTIALS.md
git commit -m "fix: remove credentials from git"
git push origin your-branch

# Если уже в main - сообщи админу!
# Нужно будет сменить все ключи
```

### Нужно откатить изменения

```powershell
# Откатить незакоммиченные изменения
git checkout -- filename.py

# Откатить последний коммит (локально)
git reset --soft HEAD~1

# Откатить после push (создать revert commit)
git revert commit_hash
git push
```

---

## 📊 Мониторинг активности команды

### GitHub Insights

**Репозиторий** → **Insights** → **Pulse**
- Кто и что коммитил
- Активность за неделю/месяц

**Contributors**
- Статистика по участникам

**Network**
- Визуализация веток

### GitHub Actions (опционально)

Настрой автотесты, линтеры, автодеплой.

Создай `.github/workflows/tests.yml`:
```yaml
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: python -m pytest
```

---

## 🎯 Итоговая схема работы

```
┌─────────────────────────────────────────────────────────┐
│                    GitHub Organization                   │
│                                                           │
│  ┌─────────────────────┐    ┌─────────────────────┐    │
│  │  team-workspace     │    │  team-workspace-2   │    │
│  │  (Private)          │    │  (Private)          │    │
│  └─────────────────────┘    └─────────────────────┘    │
│           │                           │                  │
│           ↓                           ↓                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Developer 1  │  │ Developer 2  │  │ Developer 3  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘

        ↓ git clone

┌─────────────────────────────────────────────────────────┐
│              Local Machines (Cursor)                     │
│                                                           │
│  Dev 1:  C:\...\TeamCursor\team-workspace                │
│  Dev 2:  D:\Projects\team-workspace                      │
│  Dev 3:  /home/user/team-workspace                       │
└─────────────────────────────────────────────────────────┘

Workflow:
1. Clone → 2. Branch → 3. Code → 4. Commit → 5. Push → 6. PR → 7. Review → 8. Merge
```

---

## 📚 Дополнительные ресурсы

- **Git Book:** https://git-scm.com/book/ru/v2
- **GitHub Guides:** https://guides.github.com/
- **Git Cheat Sheet:** https://education.github.com/git-cheat-sheet-education.pdf

---

## ✅ Чеклист настройки

### Для админа:
- [ ] Создал командный репозиторий
- [ ] Настроил `.gitignore`
- [ ] Добавил `CREDENTIALS.example.md`
- [ ] Настроил Branch Protection для `main`
- [ ] Добавил сотрудников
- [ ] Создал README.md с инструкциями
- [ ] Объяснил workflow команде

### Для сотрудника:
- [ ] Создал GitHub аккаунт
- [ ] Принял приглашение в репозиторий
- [ ] Установил Git
- [ ] Клонировал репозиторий
- [ ] Настроил `CREDENTIALS.md`
- [ ] Создал тестовую ветку и PR
- [ ] Прочитал README и правила

---

**Готово! Теперь у вас профессиональная командная работа 🚀**
