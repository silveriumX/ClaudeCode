# 🎯 Командная работа: Схема и команды

## 📊 Визуальная схема

```
┌─────────────────────────────────────────────────────────────────┐
│                         GITHUB.COM                              │
│                                                                 │
│  ┌──────────────────────────┐  ┌──────────────────────────┐   │
│  │ cursor-workspace (PRIVATE)│  │ team-workspace (PRIVATE)  │   │
│  │  👤 Только ты            │  │  👥 Ты + Команда         │   │
│  │                          │  │                          │   │
│  │  • Задачи/               │  │  • CreatorBot/           │   │
│  │  • Финансы/              │  │  • VoiceBot/             │   │
│  │  • Зарплаты/             │  │  • Парсеры/              │   │
│  │  • Личные разборы/       │  │  • README.md             │   │
│  └──────────────────────────┘  └──────────────────────────┘   │
│          ↕ auto push                    ↕ manual PR            │
└─────────────────────────────────────────────────────────────────┘
                    ↕ git clone/pull
┌─────────────────────────────────────────────────────────────────┐
│              ТВОЙ КОМПЬЮТЕР (Cursor)                            │
│                                                                 │
│  C:\Users\Admin\Documents\                                      │
│  │                                                              │
│  ├── 📁 Cursor\                    (личный workspace)          │
│  │   ├── .git ← cursor-workspace                               │
│  │   ├── Задачи\                                               │
│  │   ├── Финансы\                                              │
│  │   └── auto_commit.ps1          ⚙ автопуш каждые 30 мин    │
│  │                                                              │
│  └── 📁 TeamCursor\                (командный workspace)       │
│      └── team-workspace\                                        │
│          ├── .git ← team-workspace                              │
│          ├── CreatorBot\                                        │
│          ├── VoiceBot\                                          │
│          └── .gitignore            🔒 защита credentials       │
│                                                                 │
│  📄 combined-workspace.code-workspace   (открывает обе папки)   │
└─────────────────────────────────────────────────────────────────┘
                              ↕
                    ┌─────────────────────┐
                    │   Cursor IDE        │
                    │                     │
                    │  Explorer:          │
                    │  ├─💼 Командное     │
                    │  └─🔒 Личное        │
                    │                     │
                    │  Source Control:    │
                    │  ├─ team-workspace  │
                    │  └─ cursor-workspace│
                    └─────────────────────┘
```

---

## 🔄 Git Workflow для команды

```
main (защищена)
  │
  ├─────→ feature/new-bot        (Сотрудник 1)
  │         │
  │         ├─ commit: add bot logic
  │         ├─ commit: fix bugs
  │         └─ PR → Review → Merge
  │
  ├─────→ fix/credentials        (Сотрудник 2)
  │         │
  │         ├─ commit: fix env vars
  │         └─ PR → Review → Merge
  │
  └─────→ docs/update-readme     (Ты)
            │
            ├─ commit: update docs
            └─ PR → Review → Merge
```

---

## ⚡ Команды-шпаргалка

### 🏁 Начало работы (первый раз)
```powershell
# Клонировать репозиторий
cd C:\Projects
git clone https://github.com/USERNAME/team-workspace.git
cd team-workspace

# Настроить credentials
Copy-Item CREDENTIALS.example.md CREDENTIALS.md
# Заполнить CREDENTIALS.md своими ключами

# Открыть в Cursor
cursor .
```

### 📅 Начало рабочего дня
```powershell
# Перейти в репозиторий
cd C:\Projects\team-workspace

# Обновить main
git checkout main
git pull origin main

# Создать ветку для задачи
git checkout -b feature/название-задачи
```

### ✏️ Во время работы
```powershell
# Проверить что изменилось
git status

# Добавить изменения
git add .
# или конкретный файл:
git add путь/к/файлу.py

# Сохранить изменения
git commit -m "feat: описание что сделано"

# Отправить на GitHub
git push origin feature/название-задачи
```

### 📤 Завершение задачи (создание PR)
```powershell
# Запушить последние изменения
git push origin feature/название-задачи

# Перейти на GitHub и создать Pull Request
# https://github.com/USERNAME/team-workspace/pulls
# Нажать: Compare & pull request
```

### 🔄 Обновление ветки из main (если main изменился)
```powershell
# Обновить main
git checkout main
git pull origin main

# Вернуться в свою ветку
git checkout feature/название-задачи

# Влить изменения из main
git merge main

# Если есть конфликты - разрешить в Cursor
# Затем:
git add .
git commit -m "merge: resolved conflicts"
git push origin feature/название-задачи
```

### 🧹 После мержа PR (уборка)
```powershell
# Переключиться на main
git checkout main

# Обновить main
git pull origin main

# Удалить локальную ветку
git branch -d feature/название-задачи

# (ветка на GitHub удалится автоматически после мержа)
```

---

## 📝 Формат commit messages

```
<тип>: краткое описание (до 50 символов)

Детальное описание при необходимости
(после пустой строки)
```

### Типы коммитов:
- **feat:** новая функция
  ```
  feat: add Telegram notification on error
  ```

- **fix:** исправление бага
  ```
  fix: resolve credentials loading issue
  ```

- **docs:** документация
  ```
  docs: update README with setup instructions
  ```

- **refactor:** рефакторинг (без изменения функциональности)
  ```
  refactor: simplify bot initialization logic
  ```

- **test:** добавление тестов
  ```
  test: add unit tests for parser
  ```

- **chore:** рутинные задачи (обновление зависимостей и т.п.)
  ```
  chore: update dependencies
  ```

---

## 🎮 Работа в Cursor с двумя репозиториями

### Открыть workspace
```powershell
# Открыть файл workspace
cursor "C:\Users\Admin\Documents\combined-workspace.code-workspace"
```

### В Cursor:
```
EXPLORER (боковая панель)
├── 💼 КОМАНДНОЕ (TEAM-WORKSPACE)    ← Работа с командой
│   ├── CreatorBot/
│   ├── VoiceBot/
│   └── .git
│
└── 🔒 ЛИЧНОЕ (CURSOR-WORKSPACE)     ← Личные задачи
    ├── Задачи/
    ├── Финансы/
    └── .git

SOURCE CONTROL (иконка веток слева)
├── team-workspace (командное)
│   Branch: feature/my-task
│   Changes: 2 files
│   └── [Commit] [Push] [Pull]
│
└── cursor-workspace (личное)
    Branch: main
    Changes: 1 file
    └── [Commit] [Push] [Pull]
```

### ⚠️ Важно: Проверяй куда коммитишь!
- В **Source Control** всегда видно ДВА репозитория
- Перед коммитом смотри в какой репозиторий коммитишь
- Для командных проектов: `team-workspace`
- Для личных задач: `cursor-workspace`

---

## 🔐 Защита credentials

### Файлы которые НИКОГДА не коммитятся:
```
❌ CREDENTIALS.md
❌ .env
❌ service-account.json
❌ *.key
❌ *.token
❌ secrets/
```

### Как это работает:
1. `.gitignore` содержит список запрещенных файлов
2. Git не отслеживает эти файлы
3. Они остаются только на твоем компьютере
4. Команда создает свои копии из `CREDENTIALS.example.md`

### Проверить что файл НЕ отслеживается:
```powershell
git status

# Если CREDENTIALS.md НЕ показывается - всё ок ✅
# Если показывается - добавь в .gitignore ❌
```

---

## 🆘 Частые проблемы и решения

### "git: command not found"
```powershell
# Установить Git:
# https://git-scm.com/download/win
# Перезапустить PowerShell после установки
```

### "Permission denied (publickey)"
```powershell
# Использовать HTTPS вместо SSH:
git remote set-url origin https://github.com/USERNAME/team-workspace.git
```

### "Your branch is behind 'origin/main'"
```powershell
# Обновить локальную ветку:
git pull origin main
```

### "CONFLICT (content): Merge conflict"
```powershell
# 1. Открыть конфликтный файл в Cursor
# 2. Найти секции:
#    <<<<<<< HEAD
#    твой код
#    =======
#    чужой код
#    >>>>>>> branch-name
# 3. Выбрать нужный вариант или объединить
# 4. Удалить маркеры (<<<<, ====, >>>>)
# 5. Сохранить файл
# 6. Закоммитить:
git add .
git commit -m "merge: resolved conflicts"
git push
```

### Случайно закоммитил в main
```powershell
# НЕ ПАНИКУЙ!
# Откатить последний коммит (если НЕ запушил):
git reset --soft HEAD~1

# Создать ветку и закоммитить туда:
git checkout -b feature/fix
git add .
git commit -m "feat: мой код"
git push origin feature/fix
```

### Случайно закоммитил credentials
```powershell
# СРОЧНО!
# 1. Удалить файл из git:
git rm --cached CREDENTIALS.md
git commit -m "fix: remove credentials from git"
git push origin ваша-ветка

# 2. Добавить в .gitignore (если не было)
echo "CREDENTIALS.md" >> .gitignore
git add .gitignore
git commit -m "chore: update gitignore"

# 3. ОБЯЗАТЕЛЬНО сменить все ключи/пароли!
```

---

## 📊 GitHub: Полезные страницы

```
Репозиторий:
https://github.com/USERNAME/team-workspace

Pull Requests:
https://github.com/USERNAME/team-workspace/pulls

Issues (задачи):
https://github.com/USERNAME/team-workspace/issues

Settings (настройки):
https://github.com/USERNAME/team-workspace/settings

Collaborators (участники):
https://github.com/USERNAME/team-workspace/settings/access

Branch Protection:
https://github.com/USERNAME/team-workspace/settings/branches

Insights (статистика):
https://github.com/USERNAME/team-workspace/pulse
```

---

## ✅ Ежедневный чеклист

### Утро:
- [ ] `git checkout main`
- [ ] `git pull origin main`
- [ ] `git checkout -b feature/новая-задача`
- [ ] Начать работать

### Во время работы:
- [ ] Периодически: `git add . && git commit -m "..."`
- [ ] Периодически: `git push origin feature/название`

### Вечер:
- [ ] Финальный: `git push origin feature/название`
- [ ] Создать PR на GitHub
- [ ] Попросить review

### На следующий день:
- [ ] Проверить статус PR
- [ ] Если approved → смержить
- [ ] `git checkout main && git pull`
- [ ] Начать новую задачу

---

## 🎓 Дополнительно

### Git визуализация
```powershell
# Красивая история коммитов
git log --oneline --graph --all

# Кто и что менял в файле
git blame путь/к/файлу.py

# Список веток
git branch -a
```

### Полезные alias (сокращения)
```powershell
# Добавить в git config
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.cm commit

# Теперь можно:
git st      # вместо git status
git co main # вместо git checkout main
git br      # вместо git branch
```

---

**Распечатай эту страницу и держи рядом! 📄**

Или добавь в закладки: `COMMANDS_CHEATSHEET.md`
