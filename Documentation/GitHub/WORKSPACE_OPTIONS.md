# 🎨 Варианты организации рабочего пространства

## Сравнение подходов

| Критерий | Вариант 1: Два репозитория | Вариант 2: Один репозиторий с доступом | Вариант 3: Монорепо с папками |
|----------|---------------------------|---------------------------------------|------------------------------|
| **Сложность** | ⭐ Легко | ⭐⭐ Средне | ⭐⭐⭐ Сложно |
| **Безопасность** | ⭐⭐⭐ Отлично | ⭐⭐ Хорошо | ⭐ Средне |
| **Удобство** | ⭐⭐ Хорошо | ⭐⭐⭐ Отлично | ⭐⭐ Хорошо |
| **Рекомендация** | ✅ **ДЛЯ ТЕБЯ** | ❌ Не подходит | ❌ Сложно |

---

## ✅ Вариант 1: Два отдельных репозитория (РЕКОМЕНДУЕТСЯ)

### Схема

```
📂 C:\Users\Admin\Documents\
│
├── 📁 Cursor\                           ← Личное пространство
│   ├── .git                            (Private репозиторий)
│   ├── Задачи\
│   │   ├── План на 17.01.2026.md
│   │   └── Задачи.md
│   ├── Финансы\
│   ├── Личные разборы\
│   ├── Зарплаты\
│   └── auto_commit.ps1                 ← Автобэкап личного
│
└── 📁 TeamCursor\                       ← Командное пространство
    └── 📁 team-workspace\
        ├── .git                        (Private репозиторий с доступом команды)
        ├── CreatorBot\
        │   ├── google_apps_script.js
        │   ├── SETUP_GUIDE.md
        │   └── README.md
        ├── VoiceBot\
        │   ├── bot.py
        │   ├── requirements.txt
        │   └── SETUP.md
        ├── Парсер USDT кошельков\
        ├── CREDENTIALS.example.md      ← Шаблон (в git)
        ├── CREDENTIALS.md              ← Личные данные (НЕ в git)
        └── README.md

```

### Настройка за 10 минут

#### Шаг 1: Создать репозиторий
```
https://github.com/new
Name: team-workspace
Private: ✓
```

#### Шаг 2: Инициализировать локально
```powershell
cd C:\Users\Admin\Documents\
mkdir TeamCursor
cd TeamCursor
git clone https://github.com/YOUR_USERNAME/team-workspace.git
cd team-workspace
```

#### Шаг 3: Перенести проекты
```powershell
# Копировать только командные проекты
$source = "C:\Users\Admin\Documents\Cursor"
Copy-Item "$source\CreatorBot" -Destination . -Recurse
Copy-Item "$source\VoiceBot" -Destination . -Recurse
Copy-Item "$source\Парсер USDT кошельков" -Destination . -Recurse
Copy-Item "$source\Управление выдачей ВБ" -Destination . -Recurse
```

#### Шаг 4: Создать .gitignore
```powershell
@"
# Конфиденциальные данные
*CREDENTIALS*.md
!CREDENTIALS.example.md
*.env
.env*

# Личная информация
Задачи/
Финансы/
Зарплаты/
*Личн*/

# Python
__pycache__/
*.pyc
venv/
env/

# Логи
*.log
"@ | Out-File -FilePath .gitignore -Encoding utf8
```

#### Шаг 5: Создать шаблон credentials
```powershell
# В VoiceBot/CREDENTIALS.example.md
@"
# Credentials Template

## Telegram
- Bot Token: \`PASTE_YOUR_TOKEN_HERE\`
- Chat ID: \`PASTE_YOUR_CHAT_ID_HERE\`

## OpenAI
- API Key: \`PASTE_YOUR_KEY_HERE\`

## Google Sheets
1. Создай Service Account на https://console.cloud.google.com
2. Скачай JSON файл
3. Сохрани как \`service-account.json\` в корне проекта
4. Добавь в .gitignore
"@ | Out-File -FilePath "CREDENTIALS.example.md" -Encoding utf8
```

#### Шаг 6: Первый коммит
```powershell
git add .
git commit -m "Initial team workspace setup"
git push origin main
```

#### Шаг 7: Добавить сотрудников
```
GitHub → Settings → Collaborators → Add people
Выбрать роль: Write
```

### Работа в Cursor

#### Открыть workspace с двумя папками:

Создай `combined-workspace.code-workspace`:
```json
{
  "folders": [
    {
      "name": "💼 Командное (team-workspace)",
      "path": "C:\\Users\\Admin\\Documents\\TeamCursor\\team-workspace"
    },
    {
      "name": "🔒 Личное (cursor-workspace)",
      "path": "C:\\Users\\Admin\\Documents\\Cursor"
    }
  ],
  "settings": {
    "files.exclude": {
      "**/.git": false
    },
    "git.enabled": true,
    "git.autorefresh": true
  }
}
```

Сохрани в `C:\Users\Admin\Documents\combined-workspace.code-workspace`

**Открыть:** Cursor → File → Open Workspace from File → выбрать файл

#### Результат в Cursor:

```
EXPLORER (боковая панель)
├── 💼 КОМАНДНОЕ (TEAM-WORKSPACE)
│   ├── CreatorBot
│   │   ├── google_apps_script.js
│   │   └── README.md
│   ├── VoiceBot
│   │   ├── bot.py
│   │   └── SETUP.md
│   └── .git (team-workspace)
│
└── 🔒 ЛИЧНОЕ (CURSOR-WORKSPACE)
    ├── Задачи
    │   └── План на 17.01.2026.md
    ├── Финансы
    └── .git (personal workspace)
```

**Важно:** В Source Control (левая панель с иконкой веток) будет **два репозитория**. Следи куда коммитишь!

### Ежедневная работа

#### Личная работа (Задачи, Финансы):
1. В Cursor открой папку `🔒 Личное`
2. Редактируй файлы
3. Source Control покажет изменения для **личного репозитория**
4. Коммит автоматически через `auto_commit.ps1`

#### Командная работа (CreatorBot, VoiceBot):
1. В Cursor открой папку `💼 Командное`
2. Создай ветку: `git checkout -b feature/my-task`
3. Редактируй код
4. Source Control покажет изменения для **командного репозитория**
5. Коммит → Push → Pull Request на GitHub

---

## ❌ Вариант 2: Один репозиторий с выборочным доступом (НЕ РЕКОМЕНДУЕТСЯ)

### Почему не подходит:
- ⚠️ Сотрудники увидят структуру папок (включая Финансы/, Зарплаты/)
- ⚠️ Можно случайно закоммитить личные данные
- ⚠️ Сложная настройка `.gitignore`
- ⚠️ История коммитов смешивается

**Не используй этот вариант!**

---

## ❌ Вариант 3: Монорепозиторий с Git Submodules (СЛОЖНО)

### Схема:
```
📁 workspace/
├── personal/        (git submodule → личный репо)
└── team/           (git submodule → командный репо)
```

### Почему не рекомендуется:
- ⭐⭐⭐ Очень сложная настройка
- ⚠️ Запутанная работа с коммитами
- ⚠️ Проблемы при клонировании для новых сотрудников
- ✅ Нет реальных преимуществ

**Не используй без крайней необходимости!**

---

## 📊 Конкретный план для твоего случая

### Текущее состояние:
```
C:\Users\Admin\Documents\Cursor\
├── CreatorBot\           ← КОМАНДА
├── VoiceBot\             ← КОМАНДА
├── Парсер USDT\          ← КОМАНДА
├── Управление выдачей ВБ\ ← КОМАНДА
├── Задачи\               ← ЛИЧНОЕ
├── Финансы\              ← ЛИЧНОЕ
├── Зарплаты\             ← ЛИЧНОЕ
└── Личные разборы\       ← ЛИЧНОЕ
```

### Целевое состояние:

```
C:\Users\Admin\Documents\

├── 📁 Cursor\                          ← ЛИЧНЫЙ РЕПО
│   ├── .git
│   ├── Задачи\
│   ├── Финансы\
│   ├── Зарплаты\
│   └── Личные разборы\
│
└── 📁 TeamCursor\team-workspace\       ← КОМАНДНЫЙ РЕПО
    ├── .git
    ├── CreatorBot\
    ├── VoiceBot\
    ├── Парсер USDT кошельков\
    └── Управление выдачей ВБ\
```

### План миграции (30 минут):

#### 1. Создать командный репозиторий (5 мин)
```powershell
# На GitHub
https://github.com/new → team-workspace → Private

# Локально
cd C:\Users\Admin\Documents\
mkdir TeamCursor
cd TeamCursor
git clone https://github.com/YOUR_USERNAME/team-workspace.git
```

#### 2. Перенести командные проекты (10 мин)
```powershell
cd team-workspace

# Копировать (НЕ перемещать, чтобы сохранить оригиналы)
Copy-Item "C:\Users\Admin\Documents\Cursor\CreatorBot" -Destination . -Recurse
Copy-Item "C:\Users\Admin\Documents\Cursor\VoiceBot" -Destination . -Recurse
Copy-Item "C:\Users\Admin\Documents\Cursor\Парсер USDT кошельков" -Destination . -Recurse
Copy-Item "C:\Users\Admin\Documents\Cursor\Управление выдачей ВБ" -Destination . -Recurse
```

#### 3. Настроить .gitignore (2 мин)
```powershell
# Создать .gitignore
@"
*CREDENTIALS*.md
!CREDENTIALS.example.md
*.env
__pycache__/
*.pyc
venv/
*.log
"@ | Out-File -FilePath .gitignore -Encoding utf8
```

#### 4. Защитить credentials (5 мин)
```powershell
# В каждом проекте: найти CREDENTIALS.md
# Создать CREDENTIALS.example.md с шаблоном
# Убедиться что CREDENTIALS.md в .gitignore
```

#### 5. Первый коммит (2 мин)
```powershell
git add .
git commit -m "Initial team workspace"
git push origin main
```

#### 6. Добавить сотрудников (1 мин × кол-во)
```
GitHub → team-workspace → Settings → Collaborators
Add people → Write access
```

#### 7. Настроить Cursor workspace (5 мин)
Создать `combined-workspace.code-workspace` (см. выше)

#### 8. Проверить (5 мин)
- [ ] Командный репозиторий доступен на GitHub
- [ ] Личные данные не попали в командный репо
- [ ] `.gitignore` работает
- [ ] Сотрудники получили доступ
- [ ] Cursor workspace открывает обе папки

---

## 🎯 Итоговые рекомендации

### Используй Вариант 1: Два репозитория

**Преимущества:**
- ✅ Простая настройка (30 минут)
- ✅ Полная безопасность личных данных
- ✅ Четкое разделение
- ✅ Удобная работа в Cursor (одно окно, две папки)
- ✅ Независимые истории коммитов
- ✅ Легко добавлять/удалять сотрудников

**Недостатки:**
- ⚠️ Нужно следить в какой репо коммитишь (но Cursor показывает это явно)

### Структура Source Control в Cursor:

```
SOURCE CONTROL (левая панель)
├── 💼 team-workspace (2 изменения)
│   ├── Modified: CreatorBot/bot.py
│   └── Modified: VoiceBot/SETUP.md
│   └── [Branch: feature/new-function]
│
└── 🔒 cursor-workspace (1 изменение)
    ├── Modified: Задачи/План на 17.01.2026.md
    └── [Branch: main]
```

Курсор **четко показывает** в какой репозиторий ты коммитишь!

---

## 🚀 Следующие шаги

1. **Прочитай:** `QUICK_START_TEAM.md` - быстрая инструкция
2. **Настрой:** Создай командный репозиторий (30 мин)
3. **Проверь:** Пригласи одного сотрудника для теста
4. **Масштабируй:** Добавь остальную команду
5. **Документируй:** Создай README в командном репо

**Подробная инструкция:** `TEAM_GITHUB_SETUP.md`

---

**Успехов в настройке! 🎉**
