# ✅ Чеклист: Проверка что всё работает

> Быстрая проверка созданных Rules и Skills

---

## 🎯 Проверка за 5 минут

### 1. Task Management Rule ⏰

**Что проверяем:** Автоматическая проверка задач при открытии Cursor

**Как проверить:**
1. Закрой Cursor полностью
2. Открой Cursor снова
3. Открой любой файл задач: `Work/Задачи/План на *.md`

**Ожидаемый результат:**
```
AI должен сказать что-то вроде:

"Сегодня 25.01.2026. Вижу план на 24.01..."
ИЛИ
"План актуален, сегодня 25.01.2026"
```

**Статус:** [ ] Работает / [ ] Не работает

**Если не работает:**
- Проверь что файл существует: `.cursor/rules/task-management.mdc`
- Проверь что в нём `alwaysApply: true`
- Попробуй спросить AI: "Какие rules применяются сейчас?"

---

### 2. Python Standards Rule 🐍

**Что проверяем:** Автоматическое применение стандартов к Python файлам

**Как проверить:**
1. Открой любой `.py` файл, например: `Scripts/ProxyMA/check_api.py`
2. Попроси AI: "Создай функцию для чтения JSON файла"

**Ожидаемый результат:**
AI должен использовать:
```python
from pathlib import Path  # ← НЕ import os
import json

def load_config(config_name: str) -> dict:  # ← Type hints
    """Загрузить конфиг"""  # ← Docstring
    config_path = Path(__file__).parent / "data" / f"{config_name}.json"
    # ...
```

**Статус:** [ ] Работает / [ ] Не работает

**Если не работает:**
- Проверь файл: `.cursor/rules/python-standards.mdc`
- Проверь glob pattern: `["**/*.py"]`

---

### 3. ProxyMA API Standards Rule 🌐

**Что проверяем:** Применение стандартов к ProxyMA скриптам

**Как проверить:**
1. Открой файл: `Scripts/ProxyMA/proxyma_api.py` (или любой другой)
2. Попроси AI: "Создай функцию для получения данных аккаунтов"

**Ожидаемый результат:**
AI должен использовать:
```python
from pathlib import Path
import json

def load_accounts():
    data_path = Path(__file__).parent.parent.parent / "Data" / "proxyma_data_complete.json"
    # ← Правильный путь к данным
    with open(data_path, 'r', encoding='utf-8') as f:
        return json.load(f)
```

**Статус:** [ ] Работает / [ ] Не работает

**Если не работает:**
- Проверь файл: `.cursor/rules/proxyma-api-standards.mdc`
- Проверь glob: `["**/Scripts/ProxyMA/**/*.py"]`

---

### 4. Deploy Bot VPS Skill 🚀

**Что проверяем:** AI использует skill при деплое

**Как проверить:**
1. Напиши в чат: "Как задеплоить бота на VPS?"
2. ИЛИ: "Задеплой VoiceBot на VPS"

**Ожидаемый результат:**
AI должен:
- Упомянуть структуру проекта (local/, vps/)
- Рассказать про systemd service
- Дать конкретные команды
- Упомянуть примеры (VoiceBot, CursorBridge)

**Статус:** [ ] Работает / [ ] Не работает

**Если не работает:**
- Проверь папку: `C:\Users\Admin\.cursor\skills\deploy-bot-vps\`
- Проверь файл: `SKILL.md`
- Попробуй прямо: "Используй skill deploy-bot-vps"

---

### 5. Google Sheets Integration Skill 📊

**Что проверяем:** AI использует skill для Google Sheets

**Как проверить:**
1. Напиши: "Как подключить Google Sheets API?"
2. ИЛИ: "Настрой Google Sheets для проекта"

**Ожидаемый результат:**
AI должен:
- Рассказать про Google Cloud Console
- Объяснить Service Account
- Дать код подключения
- Упомянуть примеры (FinanceBot, CreatorBot)

**Статус:** [ ] Работает / [ ] Не работает

**Если не работает:**
- Проверь папку: `C:\Users\Admin\.cursor\skills\google-sheets-integration\`
- Проверь файл: `SKILL.md`

---

## 🔍 Дополнительные проверки

### 6. Instructions Processor Rule 📝

**Как проверить:**
1. Открой любой файл инструкции в `Documentation/Instructions/`
2. Попроси: "Переработай эту инструкцию по стандарту"

**Ожидаемый результат:**
AI должен использовать стандарт:
- Заголовки с номерами шагов
- Блоки кода с языками
- Плейсхолдеры для изображений
- Разделители `---`

**Статус:** [ ] Работает / [ ] Не работает

---

### 7. .gitignore обновлён 🗂️

**Как проверить:**
1. Открой `.gitignore`
2. Проверь что добавлено:
   - `Personal/`
   - `Личное/`
   - `credentials.json`
   - `*.session`

**Ожидаемый результат:**
Эти папки и файлы НЕ должны коммититься в Git

**Статус:** [ ] Обновлён / [ ] Не обновлён

---

## 📊 Итоговый чеклист

- [ ] Task Management работает автоматически
- [ ] Python Standards применяется к .py файлам
- [ ] ProxyMA Standards применяется к ProxyMA скриптам
- [ ] Skill Deploy Bot VPS срабатывает
- [ ] Skill Google Sheets Integration срабатывает
- [ ] Instructions Processor работает
- [ ] .gitignore обновлён

**Если все галочки:** ✅ Всё работает отлично!

**Если что-то не работает:**
1. Проверь пути к файлам
2. Проверь содержимое .mdc файлов
3. Перезапусти Cursor
4. Спроси AI: "Какие rules и skills доступны?"

---

## 🆘 Troubleshooting

### Проблема: Rules не применяются

**Решения:**
1. Проверь расположение: `.cursor/rules/*.mdc`
2. Проверь формат файла (YAML frontmatter + Markdown)
3. Проверь glob patterns
4. Перезапусти Cursor

---

### Проблема: Skills не срабатывают

**Решения:**
1. Проверь расположение: `C:\Users\Admin\.cursor\skills\*/SKILL.md`
2. Попробуй явно: "Используй skill {name}"
3. Проверь что в SKILL.md есть раздел "Когда использовать"

---

### Проблема: Task Management не проверяет автоматически

**Решения:**
1. Убедись что `alwaysApply: true` в `task-management.mdc`
2. Попробуй открыть файл задач
3. Спроси AI: "Проверь дату и план задач"

---

## 📖 Дополнительно

### Как спросить AI какие правила активны:

```
"Какие rules применяются к этому файлу?"
"Покажи активные правила"
"Какие skills доступны?"
```

### Как явно использовать skill:

```
"Используй skill deploy-bot-vps для деплоя"
"Примени skill google-sheets-integration"
```

### Как проверить glob pattern:

```
"Этот файл соответствует glob pattern **/*.py?"
```

---

**Последнее обновление:** 25.01.2026
**Время проверки:** ~5 минут
