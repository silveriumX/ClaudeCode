# Обновление сервера command-webhook через PuTTY

> **Цель:** Обновить файл `command_handler.py` на сервере мониторинга без потери работоспособности системы.

## Требования

Перед началом убедитесь, что у вас есть:

- Доступ к серверу по SSH (IP: `151.241.154.57`)
- Установленный PuTTY или другой SSH-клиент
- Новый код файла `command_handler.py`
- Права root на сервере

---

## Шаг 1: Подключение к серверу

Откройте PuTTY и подключитесь к серверу по SSH.

```bash
ssh root@151.241.154.57
```

> 💡 **Совет:** В PuTTY можно сохранить сессию с этим IP для быстрого подключения.

<!-- TODO: Добавить скриншот -->
![Окно подключения PuTTY](./images/step1_putty_connect.png)

---

## Шаг 2: Остановка сервиса

Остановите сервис перед обновлением, чтобы избежать конфликтов.

```bash
systemctl stop command-webhook
```

Проверьте, что сервис остановлен:

```bash
systemctl status command-webhook
```

Должно показать `inactive (dead)`.

---

## Шаг 3: Создание резервной копии

Создайте backup текущего файла на случай, если потребуется откат.

```bash
cp /opt/server-monitor/command_handler.py /opt/server-monitor/command_handler.py.backup
```

> ⚠️ **Внимание:** Всегда создавайте backup перед изменением файлов!

---

## Шаг 4: Замена файла

Скопируйте команду с новым кодом и вставьте в PuTTY (правой кнопкой мыши).

```bash
cat > /opt/server-monitor/command_handler.py << 'ENDOFFILE'
# Вставьте сюда новый код файла
ENDOFFILE
```

> ℹ️ **Примечание:** Используйте `'ENDOFFILE'` в кавычках, чтобы избежать подстановки переменных.

---

## Шаг 5: Запуск сервиса

Запустите обновлённый сервис.

```bash
systemctl start command-webhook
```

---

## Проверка результата

Убедитесь, что обновление прошло успешно:

1. Проверьте статус сервиса:

```bash
systemctl status command-webhook
```

Ожидаемый результат: `Active: active (running)`

2. Проверьте логи на наличие ошибок:

```bash
journalctl -u command-webhook -f
```

Нажмите `Ctrl+C` для выхода из просмотра логов.

3. В Google Sheets выполните тестовую команду `check` на любом сервере.

---

## Частые ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Active: failed` | Синтаксическая ошибка в Python коде | Проверьте логи: `journalctl -u command-webhook -n 50` |
| `Connection refused` | Сервис не запущен | Запустите: `systemctl start command-webhook` |
| `Permission denied` | Нет прав на файл | Проверьте права: `ls -la /opt/server-monitor/` |
| Старый код работает | Сервис не перезапущен | Перезапустите: `systemctl restart command-webhook` |

---

## Откат изменений

Если что-то пошло не так, восстановите backup:

```bash
systemctl stop command-webhook
cp /opt/server-monitor/command_handler.py.backup /opt/server-monitor/command_handler.py
systemctl start command-webhook
```

---

**Версия:** 1.0
**Дата создания:** 22.01.2026
**Автор:** AI Assistant
**Исходный документ:** `Projects/ServerManager/ИНСТРУКЦИЯ_ОБНОВЛЕНИЯ_СЕРВЕРА.md`
