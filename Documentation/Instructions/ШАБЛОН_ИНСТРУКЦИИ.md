# Шаблон: Название вашей инструкции

> **Цель:** Опишите здесь, что получит пользователь после выполнения всех шагов этой инструкции. Например: "Настроенный и работающий сервер с доступом по SSH".

## Требования

Перед началом убедитесь, что у вас есть:

- Требование 1 (например: доступ к серверу)
- Требование 2 (например: установленный Python 3.10+)
- Требование 3 (например: права администратора)

---

## Шаг 1: Название первого действия

Опишите, что нужно сделать на этом шаге. Будьте конкретны и лаконичны.

```bash
# Пример команды
echo "Hello World"
```

> 💡 **Совет:** Добавьте полезную подсказку, если она уместна.

<!-- TODO: Добавить скриншот -->
![Результат выполнения команды](./images/step1_result.png)

---

## Шаг 2: Название второго действия

Описание следующего шага. Если шаг сложный, разбейте его на подпункты:

1. Сначала сделайте это
2. Затем это
3. И наконец это

```powershell
# Пример PowerShell команды
Get-Service | Where-Object {$_.Status -eq "Running"}
```

![Список запущенных сервисов](./images/step2_services.png)

---

## Шаг 3: Работа с конфигурацией

Иногда нужно отредактировать конфигурационный файл:

```yaml
# config.yaml
server:
  host: localhost
  port: 8080
  debug: false

database:
  url: postgresql://localhost:5432/mydb
```

> ⚠️ **Внимание:** Не забудьте изменить значения на свои!

---

## Шаг 4: Проверка настроек

После внесения изменений проверьте конфигурацию:

```bash
# Проверка синтаксиса
python -m py_compile config.py

# Запуск тестов
pytest tests/ -v
```

| Параметр | Ожидаемое значение | Ваше значение |
|----------|-------------------|---------------|
| host     | localhost         |               |
| port     | 8080              |               |
| status   | Running           |               |

---

## Проверка результата

Как убедиться, что всё работает правильно:

1. Откройте браузер и перейдите по адресу `http://localhost:8080`
2. Вы должны увидеть страницу приветствия
3. В консоли не должно быть ошибок

**Ожидаемый результат:**

```
Server started successfully on port 8080
Ready to accept connections
```

![Успешный запуск сервера](./images/final_result.png)

---

## Частые ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Connection refused` | Сервер не запущен | Выполните `systemctl start myservice` |
| `Permission denied` | Нет прав доступа | Запустите с `sudo` или проверьте права файла |
| `Port already in use` | Порт занят другим процессом | Измените порт в конфиге или остановите другой процесс |
| `Module not found` | Не установлены зависимости | Выполните `pip install -r requirements.txt` |

---

## Дополнительные материалы

- [Ссылка на документацию](https://example.com/docs)
- [Видеоинструкция](https://example.com/video)
- Связанные инструкции: `Инструкция_по_деплою.md`

---

**Версия:** 1.0
**Дата создания:** 22.01.2026
**Последнее обновление:** 22.01.2026
**Автор:** [Ваше имя]
