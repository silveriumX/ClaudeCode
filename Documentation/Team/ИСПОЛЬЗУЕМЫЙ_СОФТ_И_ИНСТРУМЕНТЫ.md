# 🛠️ Используемый софт и инструменты

> **Уровень доступа:** L0-L1 (Общедоступная информация)

---

## 📋 Оглавление

1. [ADS Power](#ads-power) - Мультипрофильный браузер
2. [Google Sheets + Apps Script](#google-sheets--apps-script) - Центр управления
3. [Proxifier](#proxifier) - Прокси-клиент для Windows
4. [AnyDesk](#anydesk) - Удалённый доступ
5. [WinRM](#winrm) - Удалённое управление Windows
6. [Python Stack](#python-stack) - Автоматизация на Linux
7. [Telegram Bot](#telegram-bot) - Уведомления
8. [Хостеры](#хостеры) - Провайдеры VPS
9. [Прокси-провайдеры](#прокси-провайдеры) - SOCKS5 Residential

---

## 1. ADS Power

### 📖 Что это?

**ADS Power (Anti-Detect Browser)** - специализированный браузер для работы с множественными изолированными профилями.

### 🎯 Зачем нам нужен?

**Основная задача:** Изоляция доступов к разным хостерам и прокси-провайдерам.

**Проблема, которую решает:**
```
БЕЗ ADS Power:
Хостер A → Вход с IP1
Хостер B → Вход с IP2
❌ Риск: случайно зашли в Хостер A с IP2 → бан аккаунта

С ADS Power:
Профиль A → Автоматически использует прокси IP1 → Хостер A
Профиль B → Автоматически использует прокси IP2 → Хостер B
✅ Невозможно перепутать
```

### 🔧 Основные возможности

**1. Изоляция профилей**
- Каждый профиль = отдельный браузер
- Свои cookies, localStorage, fingerprint
- Свой прокси

**2. Управление прокси**
```
Профиль настроек:
├── Прокси: SOCKS5
├── Host: proxy-server.com
├── Port: 12345
├── Username: user123
├── Password: pass123
└── Автоматическое подключение: ✅
```

**3. Закладки и автозаполнение**
- Сохранённые логины для каждого профиля
- Закладки с панелями хостеров
- Быстрый доступ одним кликом

**4. Синхронизация и бэкапы**
- Экспорт/импорт профилей
- Облачная синхронизация
- Восстановление при сбое

### 📝 Структура профилей (пример)

```
Profile_Hoster_A_Proxyma_GEO_RU
├── Прокси: socks5://user:pass@proxy.com:1080 (RU)
├── Закладки:
│   ├── Панель Хостера A
│   └── Панель Proxyma
├── Сохранённые логины:
│   ├── hoster-a.com: login_A / ****
│   └── proxyma.com: login_A / ****
└── Fingerprint: Windows 10, Chrome, RU locale

Profile_Hoster_B_Proxyma_GEO_US
├── Прокси: socks5://user:pass@proxy.com:2080 (US)
├── Закладки:
│   ├── Панель Хостера B
│   └── Панель Proxyma
├── Сохранённые логины:
│   ├── hoster-b.com: login_B / ****
│   └── proxyma.com: login_B / ****
└── Fingerprint: Windows 10, Chrome, US locale
```

### ✅ Когда использовать?

- ✅ Вход в панель хостера для заказа/управления серверами
- ✅ Вход в панель прокси-провайдера для заказа/управления прокси
- ✅ Проверка биллинга и оплаты
- ✅ Открытие тикетов в саппорт

### ❌ Когда НЕ использовать?

- ❌ Подключение к самим VPS серверам (используем RDP/AnyDesk)
- ❌ Автоматизация через API (используем Python скрипты)
- ❌ Повседневные задачи, не связанные с хостерами

### 🎓 Обучение

**Базовый уровень:**
1. Запуск профиля
2. Проверка IP (whoer.net, 2ip.io)
3. Добавление закладки
4. Сохранение логина

**Продвинутый уровень:**
1. Создание нового профиля
2. Настройка прокси
3. Настройка fingerprint
4. Экспорт/импорт профиля

**Время на освоение:** 30-60 минут

### 🔗 Ресурсы

- Официальный сайт: `adspower.com` (поиск в интернете)
- Документация: в приложении (Help → Documentation)

---

## 2. Google Sheets + Apps Script

### 📖 Что это?

**Google Sheets** - облачная таблица, используется как центральный интерфейс управления.
**Apps Script** - JavaScript-based язык для автоматизации Google Sheets.

### 🎯 Зачем нам нужны?

**Основная задача:** Единая точка для просмотра статусов и отправки команд на сервера.

### 🔧 Структура таблицы (концептуально)

```
┌────────┬───────┬────────┬────────┬──────────┬──────────┬─────────┬────────────┐
│ Server │  IP   │  City  │ Status │ Proxifier│ AnyDesk  │ Command │ Last Check │
├────────┼───────┼────────┼────────┼──────────┼──────────┼─────────┼────────────┤
│ VPS-01 │ 5.x.x │ Moscow │ ONLINE │ Running  │ Running  │         │ 14:25:03   │
│ VPS-02 │ 8.x.x │ Berlin │ ONLINE │ Running  │ Running  │ check   │ 14:25:08   │
│ VPS-03 │ 2.x.x │ London │ OFFLINE│ Unknown  │ Unknown  │         │ 14:05:12   │
└────────┴───────┴────────┴────────┴──────────┴──────────┴─────────┴────────────┘
```

**Колонки:**

**Статические (не меняются):**
- Server ID - идентификатор сервера
- Credentials - реквизиты доступа (IP:User:Pass)

**Динамические (обновляются автоматически):**
- IP - внешний IP сервера
- City - геолокация
- Status - ONLINE/OFFLINE
- Proxifier - статус процесса
- AnyDesk - статус процесса
- Last Check - время последней проверки

**Интерактивные:**
- Command - сюда вводится команда для выполнения
- Result - результат выполнения команды

### 🔧 Google Apps Script

**Функции скрипта:**

**1. Отправка команд на Linux сервер**
```javascript
function sendCommand() {
  // Читает команду из ячейки
  // Отправляет POST запрос на Linux сервер (webhook)
  // Получает результат
  // Записывает результат обратно в таблицу
}
```

**2. Автообновление статусов**
```javascript
function updateStatuses() {
  // Вызывается триггером каждые 20 минут
  // Запрашивает свежие данные с Linux сервера
  // Обновляет все статусные колонки
}
```

**3. Форматирование и валидация**
```javascript
function onEdit() {
  // Подсвечивает ячейки по статусу
  // Валидирует команды
  // Очищает старые результаты
}
```

### ✅ Когда использовать?

- ✅ Просмотр статуса всех серверов одним взглядом
- ✅ Выполнение команд на одном или нескольких серверах
- ✅ Проверка истории изменений
- ✅ Добавление нового сервера в систему

### 📝 Доступные команды

Смотрите в [АРХИТЕКТУРА_СИСТЕМЫ_ОБЕЗЛИЧЕННАЯ.md](#) раздел "Доступные команды"

### 🎓 Обучение

**Базовый уровень (L1 - Read Only):**
1. Чтение статусов серверов
2. Понимание значений колонок
3. Просмотр истории изменений

**Средний уровень (L2 - Test Environment):**
1. Отправка команды на тестовый сервер
2. Анализ результата выполнения
3. Добавление сервера в таблицу

**Продвинутый уровень (L3-L4):**
1. Массовые операции
2. Модификация Apps Script
3. Настройка триггеров

**Время на освоение:**
- L1: 15 минут
- L2: 1-2 часа (с практикой)
- L3-L4: несколько дней

---

## 3. Proxifier

### 📖 Что это?

**Proxifier** - клиент для Windows, который перенаправляет весь трафик через прокси-сервер.

### 🎯 Зачем нам нужен?

**Основная задача:** Маршрутизация всего трафика Windows VPS через SOCKS5 прокси.

**Проблема, которую решает:**
```
БЕЗ Proxifier:
- Каждая программа должна поддерживать прокси
- Некоторые программы не поддерживают SOCKS5
- Нет централизованного управления

С Proxifier:
- ВСЕ программы работают через прокси
- Не важно, поддерживает программа прокси или нет
- Настройка в одном месте
```

### 🔧 Конфигурация

**1. Proxy Server**
```
Name: Main Proxy
Address: proxy-server.com
Port: 12345
Protocol: SOCKS5
Authentication: Yes
  Username: user123
  Password: pass123
```

**2. Proxification Rules**
```
Rule 1: Весь трафик
- Target Applications: *
- Target Ports: Any
- Action: Proxy SOCKS5 Main Proxy

Rule 2: Локальный трафик (исключение)
- Target IP: 127.0.0.1, 192.168.*.*, 10.*.*.*
- Action: Direct
```

**3. Name Resolution (DNS)**
```
Resolve hostnames through proxy: ✅
```

### 🔍 Как проверить, что работает?

**Метод 1: Через сайты проверки IP**
1. Открыть браузер на Windows VPS
2. Перейти на `2ip.io` или `whoer.net`
3. Проверить, что IP и GEO совпадают с прокси

**Метод 2: Через логи Proxifier**
```
View → Log
Должны быть записи:
[13:45:23] chrome.exe - Remote IP: proxy-server.com [SOCKS5]
[13:45:24] firefox.exe - Remote IP: proxy-server.com [SOCKS5]
```

**Метод 3: Через PowerShell API**
```powershell
Invoke-RestMethod -Uri "https://api.2ip.io/geo.json"
# Должен вернуть GEO прокси, а не сервера
```

### ⚠️ Типичные проблемы

**Проблема 1: "Tunnel failed"**
```
Причина: Неверные реквизиты прокси
Решение: Проверить username:password
```

**Проблема 2: "Connection timeout"**
```
Причина: Прокси сервер недоступен или заблокирован
Решение: Проверить статус прокси у провайдера
```

**Проблема 3: "DNS resolution failed"**
```
Причина: DNS не резолвится через прокси
Решение: Включить "Resolve hostnames through proxy"
```

### 🎓 Обучение

**Базовый уровень:**
1. Запуск/остановка Proxifier
2. Просмотр логов
3. Проверка текущего прокси

**Средний уровень:**
1. Изменение реквизитов прокси
2. Настройка правил (rules)
3. Диагностика проблем

**Продвинутый уровень:**
1. Создание сложных правил
2. Настройка профилей для разных приложений
3. Автоматизация через PowerShell

**Время на освоение:** 1-2 часа

### 🔗 Ресурсы

- Официальный сайт: `proxifier.com`
- Наша инструкция: [ИНСТРУКЦИЯ_РАБОТЫ_С_PROXIFIER.md](#) (создать отдельно)

---

## 4. AnyDesk

### 📖 Что это?

**AnyDesk** - программа для удалённого доступа к рабочему столу (аналог TeamViewer, RDP).

### 🎯 Зачем нам нужен?

**Основная задача:** Визуальная диагностика и управление Windows VPS.

**Когда использовать AnyDesk вместо RDP:**
- ✅ RDP не работает (проблемы с сетью)
- ✅ Нужно показать экран кому-то ещё
- ✅ Лучшая производительность на медленных каналах
- ✅ Работа через прокси (RDP может не работать)

### 🔧 Настройка на сервере

**1. Установка**
```
Скачать: anydesk.com/download
Установить с настройками по умолчанию
```

**2. Конфигурация**
```
Settings → Security:
- Set password for unattended access: ✅
- Allow unattended access: ✅

Settings → Display:
- Quality: Automatic

Settings → General:
- Start AnyDesk with Windows: ✅
```

**3. ID сервера**
```
Каждый сервер получает уникальный AnyDesk ID
Пример: 123 456 789

Подключение:
AnyDesk на вашем компьютере → Ввести ID → Ввести пароль
```

### ✅ Когда использовать?

- ✅ Первичная настройка сервера (установка Proxifier, настройка)
- ✅ Визуальная диагностика (GUI не отвечает, нужно посмотреть)
- ✅ Настройка приложений с графическим интерфейсом
- ✅ Проверка работы Proxifier визуально

### ❌ Когда НЕ использовать?

- ❌ Рутинные операции (используем WinRM автоматизацию)
- ❌ Массовые операции на множестве серверов
- ❌ Простые команды (перезагрузка, проверка статуса)

### 🎓 Обучение

**Базовый уровень:**
1. Подключение по AnyDesk ID
2. Управление мышью/клавиатурой
3. Копирование файлов между компьютерами

**Время на освоение:** 15-30 минут

---

## 5. WinRM

### 📖 Что это?

**WinRM (Windows Remote Management)** - технология Microsoft для удалённого выполнения команд на Windows серверах.

### 🎯 Зачем нам нужен?

**Основная задача:** Автоматизация управления без GUI.

**Преимущества:**
- ✅ Выполнение PowerShell команд удалённо
- ✅ Не требует графического интерфейса
- ✅ Быстро (низкие накладные расходы)
- ✅ Надёжно (встроенная технология Windows)

### 🔧 Настройка на Windows сервере

**Однократная настройка (при первом подключении):**

```powershell
# 1. Включить WinRM
Enable-PSRemoting -Force

# 2. Разрешить подключения с любых хостов
Set-Item WSMan:\localhost\Client\TrustedHosts -Value "*" -Force

# 3. Перезапустить сервис
Restart-Service WinRM

# 4. Проверить статус
Get-Service WinRM
# Должен быть: Status = Running
```

**Порты:**
- 5985 (HTTP) - используется для автоматизации
- 5986 (HTTPS) - опционально, для зашифрованного соединения

### 🔧 Использование с Linux

**Python библиотека: pywinrm**

```python
from winrm import Session

# Подключение
session = Session('http://SERVER-IP:5985/wsman',
                  auth=('USERNAME', 'PASSWORD'))

# Выполнение команды
result = session.run_ps('Get-Process Proxifier')

# Результат
print(result.std_out)  # Вывод команды
print(result.status_code)  # 0 = успех
```

### 🎓 Команды, которые выполняются через WinRM

**Примеры:**

**1. Проверка процессов**
```powershell
Get-Process Proxifier
Get-Process AnyDesk
```

**2. Запуск программы**
```powershell
Start-Process "C:\Program Files\Proxifier\Proxifier.exe"
```

**3. Остановка программы**
```powershell
Stop-Process -Name Proxifier -Force
```

**4. Получение информации о системе**
```powershell
# Таймзона
Get-TimeZone

# Внешний IP
Invoke-RestMethod -Uri "https://api.2ip.io/geo.json"

# Установленные языки
Get-WinUserLanguageList
```

**5. Изменение настроек**
```powershell
# Изменить таймзону
Set-TimeZone -Id "Russian Standard Time"

# Перезагрузка
Restart-Computer -Force
```

### ⚠️ Типичные проблемы

**Проблема: "Connection timeout"**
```
Причина: Файрволл блокирует порт 5985
Решение: Открыть порт в Windows Firewall
```

**Проблема: "Authentication failed"**
```
Причина: Неверные username/password
Решение: Проверить реквизиты доступа
```

### 🎓 Обучение

**Базовый уровень:**
- Понимание, что такое WinRM
- Проверка статуса WinRM на сервере

**Средний уровень:**
- Выполнение простых команд через WinRM
- Диагностика проблем подключения

**Продвинутый уровень:**
- Написание PowerShell скриптов для автоматизации
- Обработка ошибок и таймаутов

**Время на освоение:** 2-4 часа (с практикой)

---

## 6. Python Stack

### 📖 Что это?

Набор Python модулей для автоматизации на Linux сервере управления.

### 🔧 Основные модули

**1. `pywinrm`** - WinRM клиент
```python
from winrm import Session
```

**2. `flask`** - Webhook сервер
```python
from flask import Flask, request, jsonify
```

**3. `gspread`** - Работа с Google Sheets
```python
import gspread
```

**4. `requests`** - HTTP запросы
```python
import requests
```

### 📝 Структура кода

Смотрите [АРХИТЕКТУРА_СИСТЕМЫ_ОБЕЗЛИЧЕННАЯ.md](#) раздел "Linux VPS"

### 🎓 Обучение

**Требуемые навыки:**
- Базовое знание Python
- Понимание HTTP/REST API
- Работа с Linux командной строкой

**Время на освоение:** Зависит от уровня подготовки (1-4 недели)

---

## 7. Telegram Bot

### 📖 Что это?

Telegram бот для отправки уведомлений об ошибках и статусах.

### 🎯 Зачем нужен?

**Основная задача:** Мгновенное оповещение о проблемах.

**Примеры уведомлений:**
```
🔴 ALERT: Server VPS-03 is OFFLINE
Last seen: 5 minutes ago

🟡 WARNING: Proxifier not running on VPS-07

✅ SUCCESS: Timezone changed on VPS-12 to MSK
```

### 🔧 Как работает?

```python
import requests

TELEGRAM_BOT_TOKEN = "..."
CHAT_ID = "..."

def send_alert(message):
    url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage"
    requests.post(url, json={"chat_id": CHAT_ID, "text": message})
```

---

## 8. Хостеры

### 📖 Что это?

Провайдеры VPS (Virtual Private Server) на Windows.

### 🎯 Критерии выбора хостера

**Обязательные:**
- ✅ Поддержка Windows (Server или Desktop)
- ✅ Возможность удалённого доступа (RDP)
- ✅ Адекватная стоимость (<1000₽/месяц за базовую конфигурацию)

**Желательные:**
- ✅ Разные геолокации (EU, US, RU, Asia)
- ✅ Гибкий биллинг (почасовая оплата)
- ✅ Быстрый саппорт (< 1 часа ответ)
- ✅ Автоматическая установка Windows
- ✅ Панель управления на русском языке

### 📝 Типичный процесс заказа

**Шаг 1: Вход через ADS Power профиль**
```
Открыть профиль "Hoster_X_Profile"
→ Автоматически подключается через нужный прокси
```

**Шаг 2: Заказ VPS**
```
Выбрать конфигурацию:
- OS: Windows Server 2019/2022 или Windows 10/11
- RAM: 2-4 GB
- CPU: 1-2 cores
- Location: по требованию (RU/EU/US)
- Billing: Monthly или Hourly
```

**Шаг 3: Получение доступов**
```
После создания получаем:
- IP адрес: например 5.182.xxx.xxx
- Username: Administrator или другое
- Password: генерируется автоматически
```

**Шаг 4: Добавление в систему**
```
Добавить в Google Sheets:
Формат: IP:Username:Password
```

### 🎓 Обучение

**Базовый уровень (L1):**
- Понимание, что такое VPS
- Знание критериев выбора
- Чтение документации хостеров

**Средний уровень (L2-L3):**
- Заказ VPS самостоятельно
- Первичная настройка
- Работа с биллингом

**Продвинутый уровень (L4):**
- Оценка и выбор новых хостеров
- Переговоры с саппортом
- Оптимизация затрат

---

## 9. Прокси-провайдеры

### 📖 Что это?

Провайдеры резидентских SOCKS5 прокси с геотаргетингом.

### 🎯 Требования к прокси

**Обязательные:**
- ✅ Протокол: SOCKS5
- ✅ Тип: Residential (резидентские)
- ✅ Геотаргетинг: выбор страны и города
- ✅ Авторизация: username:password
- ✅ Стабильность: uptime >95%

**Желательные:**
- ✅ API для автоматизации
- ✅ Ротация IP по запросу
- ✅ Sticky sessions (один IP на определённое время)
- ✅ Гибкий биллинг (pay-as-you-go)

### 📝 Формат реквизитов

```
Формат 1 (полный URL):
socks5://username:password@proxy-host:port

Формат 2 (раздельно):
Protocol: SOCKS5
Host: proxy-host.com
Port: 12345
Username: user123
Password: pass456
GEO: RU-MOW (Russia, Moscow)
```

### 🔧 Процесс работы

**Шаг 1: Заказ прокси**
```
Войти через ADS Power профиль
→ Выбрать GEO (страна + город)
→ Выбрать тип (sticky/rotating)
→ Получить реквизиты
```

**Шаг 2: Настройка на сервере**
```
Метод A: Вручную через AnyDesk
- Открыть Proxifier
- Settings → Proxy Servers
- Ввести реквизиты

Метод B: Автоматически через команду
- В Google Sheets: команда set_proxy
- Параметры: socks5://user:pass@host:port:GEO
```

**Шаг 3: Проверка**
```
Открыть 2ip.io на Windows VPS
Проверить:
- IP изменился
- GEO совпадает с заказанным
```

### 🎓 Обучение

**Базовый уровень (L1):**
- Понимание разницы между типами прокси
- Знание требований к прокси для нашей системы

**Средний уровень (L2-L3):**
- Заказ прокси самостоятельно
- Настройка на сервере
- Диагностика проблем

**Продвинутый уровень (L4):**
- Работа с API провайдера
- Автоматизация ротации
- Оптимизация затрат

---

## 📊 Сводная таблица инструментов

| Инструмент | Назначение | Уровень освоения | Время обучения |
|-----------|------------|------------------|----------------|
| ADS Power | Изоляция профилей | Средний | 1 час |
| Google Sheets | Центр управления | Базовый | 15 минут |
| Apps Script | Автоматизация таблиц | Продвинутый | 4-8 часов |
| Proxifier | Прокси-клиент | Средний | 1-2 часа |
| AnyDesk | Удалённый доступ | Базовый | 30 минут |
| WinRM | Автоматизация | Средний | 2-4 часа |
| Python | Разработка автоматизации | Продвинутый | 1-4 недели |
| Telegram Bot | Уведомления | Базовый (использование) | 5 минут |

---

## 🎯 Чек-лист готовности

После изучения вы должны понимать:

- [ ] Зачем нужен каждый инструмент
- [ ] Когда использовать каждый инструмент
- [ ] Базовые операции с каждым инструментом
- [ ] Типичные проблемы и их решения
- [ ] Где искать дополнительную информацию

---

**Следующий шаг:** После изучения переходите к [СТАНДАРТНЫЕ_ОПЕРАЦИОННЫЕ_ПРОЦЕДУРЫ.md](#) для практического применения знаний.
