# 💬 Сообщения для онбординга системного администратора

> **Назначение:** Готовые сообщения для поэтапного введения нового администратора в систему

---

## 📋 Структура онбординга

```
День 1: Сообщения 1-4 (Общая картина)
День 2: Сообщения 5-7 (Техническая база)
День 3: Сообщения 8-10 (Практическая работа)
```

---

## Сообщение 1: Приветствие и контекст

```
Добрый день!

Рад, что вы присоединились к команде. Сейчас покажу в общих чертах,
как работает наша система и какие составные части в ней есть.

Не переживайте, если сразу не всё будет понятно - мы будем разбирать
постепенно, сначала на уровне общего понимания, потом перейдём к практике.

Начнём с того, что у нас есть и зачем это нужно 👇
```

---

## Сообщение 2: Используемый софт (ADS Power)

```
**Что используется из софта:**

🌐 **ADS Power** - специальный браузер с профилями

Главная фишка: 1 профиль = вход в аккаунт хостера + прокси-провайдера

Зачем это нужно?
• Каждый хостер/прокси должен открываться через свой прокси
• Нельзя случайно перепутать аккаунты
• Всё изолировано: свои куки, свои сохранённые пароли

Пример:
Профиль "Hoster_A" → автоматически через прокси RU → заходим в панель хостера A
Профиль "Hoster_B" → автоматически через прокси US → заходим в панель хостера B

Невозможно ошибиться и зайти не через тот прокси ✅
```

---

## Сообщение 3: Хостеры и прокси

```
**Хостеры VPS (основные):**

• rdp.monster
• vdsina.com
• aeza.net

И подобные провайдеры Windows VPS.

Что важно:
- Поддержка Windows (Server или Desktop)
- Разные геолокации
- Адекватная цена (300-1000₽/месяц)
- Почасовая оплата (можно быстро тестировать)

Иногда нужно находить новых, подобного же плана, которые поддерживают
Windows и соответствуют нашим критериям.

---

**Прокси-провайдеры:**

Используем резидентские прокси с выбором GEO / города

Важно:
• Протокол: SOCKS5 (всегда!)
• Тип: Residential (резидентские IP)
• Геотаргетинг: можем выбрать страну и город
• Формат: socks5://username:password@host:port

Зачем резидентские? Они выглядят как обычные пользователи,
а не как датацентры (меньше банов).
```

---

## Сообщение 4: Google Таблица - центр управления

```
**Вся основная работа ведётся через Google Таблицу.**

Это наш центральный "пульт управления" 🎛

Что там хранится:
✅ Данные по всем серверам (выданные/свободные)
✅ Сколько штук активны
✅ IP-адреса и геолокация
✅ Статусы серверов (ONLINE/OFFLINE)
✅ Какие программы запущены (Proxifier, AnyDesk)
✅ Доступы к серверам (IP:Username:Password)
✅ Привязка к профилям ADS Power

**Золотое правило:**
Если какие-то данные меняются - они ОБЯЗАТЕЛЬНО вносятся СРАЗУ в Google Таблицу.

Таблица = единственный источник правды о том, что у нас есть и как оно настроено.
```

---

## Сообщение 5: Система автоматического мониторинга

```
**Как работает мониторинг серверов:**

У нас есть специальный Linux VPS-сервер - это "мозг" системы 🧠

Что он делает:
1️⃣ Каждые 20 минут автоматически проверяет ВСЕ Windows сервера
2️⃣ Собирает данные: IP, GEO, запущенные процессы, статус
3️⃣ Обновляет Google Таблицу (видно актуальное состояние)
4️⃣ Отправляет алерты в Telegram, если что-то не так

**Как он подключается?**
Через технологию WinRM (Windows Remote Management) - удалённое выполнение команд.

**Что проверяет?**
• Сервер работает или нет
• Внешний IP и геолокация (совпадает ли с прокси)
• Запущен ли Proxifier (программа для прокси)
• Запущен ли AnyDesk (удалённый доступ)
• Правильная ли таймзона

Если что-то не так - сразу алерт в Telegram ⚡️
```

---

## Сообщение 6: Что такое WinRM и зачем он нужен

```
**Важная технология: WinRM**

WinRM = Windows Remote Management (удалённое управление Windows)

Зачем нужен?
Чтобы можно было выполнять команды на Windows сервере БЕЗ подключения
через RDP/AnyDesk. Это автоматизация.

Примеры команд через WinRM:
• Запустить/остановить Proxifier
• Проверить, какие процессы запущены
• Изменить таймзону
• Установить языки
• Перезагрузить сервер
• Сменить прокси в Proxifier

**Как настраивается?**
При развёртывании нового сервера нужно выполнить несколько команд
в PowerShell (есть готовая инструкция на 2 минуты).

После этого:
✅ Linux сервер автоматически подключается
✅ Начинает мониторить
✅ Можно отправлять команды из Google Таблицы
```

---

## Сообщение 7: Рабочий процесс - общая картина

```
**Как выглядит типичный рабочий процесс:**

📍 **Заказ нового сервера:**
1. Открываешь нужный профиль в ADS Power (автоматом через свой прокси)
2. Заходишь в панель хостера
3. Заказываешь Windows VPS (выбираешь GEO)
4. Получаешь реквизиты: IP, Username, Password

📍 **Настройка сервера:**
1. Подключаешься по RDP (стандартный удалённый рабочий стол Windows)
2. Устанавливаешь нужные программы (Proxifier, AnyDesk)
3. Настраиваешь прокси в Proxifier (реквизиты от провайдера)
4. Выполняешь команды для включения WinRM (2 минуты)

📍 **Добавление в систему:**
1. Добавляешь сервер в Google Таблицу (формат: IP:User:Pass)
2. Через 20 минут мониторинг автоматически подхватывает сервер
3. В таблице появляется статус, IP, GEO, процессы

📍 **Дальше всё автоматически:**
- Мониторинг следит за сервером
- Статусы обновляются в таблице
- При проблемах приходят алерты в Telegram
```

---

## Сообщение 8: Важные программы на Windows серверах

```
**Какие программы должны быть на каждом Windows сервере:**

🔵 **Proxifier**
Назначение: маршрутизирует ВЕСЬ трафик через SOCKS5 прокси
Зачем: чтобы любая программа работала через прокси, даже если не поддерживает его
Статус: должен быть ВСЕГДА запущен

Как проверить:
- Открыть браузер на сервере → зайти на 2ip.io
- IP должен совпадать с прокси, а не с сервером ✅

🔵 **AnyDesk**
Назначение: удалённый доступ к рабочему столу (альтернатива RDP)
Зачем: когда нужна визуальная диагностика или RDP не работает
Статус: должен быть запущен и в автозагрузке

🔵 **WinRM**
Назначение: фоновый сервис для удалённого управления
Зачем: автоматизация и мониторинг
Статус: должен быть включен (Status = Running)

Проверка:
PowerShell: Get-Service WinRM
```

---

## Сообщение 9: Команды через Google Таблицу

```
**Управление серверами через Google Таблицу**

В таблице есть колонка "Command" - туда можно ввести команду,
и она выполнится на сервере автоматически через WinRM.

**Примеры команд:**

Мониторинг:
• `check` - полная проверка сервера

Программы:
• `start_proxifier` - запустить Proxifier
• `stop_proxifier` - остановить Proxifier
• `restart_proxifier` - перезапустить
• `start_anydesk` - запустить AnyDesk

Таймзона:
• `get_timezone` - показать текущую таймзону
• `set_timezone_msk` - установить MSK (UTC+3)
• `set_timezone_ekt` - установить Ekaterinburg (UTC+5)

Языки:
• `get_languages` - показать установленные языки
• `set_lang_russian` - установить русский
• `set_lang_english` - установить английский

Прокси (новое):
• `set_proxy` - сменить прокси на сервере
• Формат: socks5://user:pass@host:port:GEO

Система:
• `reboot` - перезагрузить сервер

Результат выполнения появится в колонке "Result" через несколько секунд.
```

---

## Сообщение 10: Алгоритм диагностики проблем

```
**Что делать, если сервер показывает проблемы:**

🔴 **Статус OFFLINE в таблице**

Шаг 1: Проверить доступность
- Попробовать подключиться по RDP/AnyDesk
- Если не подключается → проблема на стороне хостера

Шаг 2: Если подключается
- Зайти на сервер
- Проверить, запущен ли Proxifier
- Проверить логи Proxifier (может прокси умер)

Шаг 3: Если прокси проблема
- Зайти в панель прокси-провайдера (через ADS Power профиль)
- Проверить статус прокси
- При необходимости заказать новый

---

🟡 **Неверный IP/GEO**

Причина: Proxifier не работает или настроен на другой прокси

Решение:
1. Подключиться к серверу (AnyDesk)
2. Открыть Proxifier → Profile → Proxy Servers
3. Проверить реквизиты прокси
4. Если не совпадают - исправить
5. Проверить на 2ip.io

Или использовать команду `set_proxy` из таблицы (автоматически)

---

🟡 **Proxifier не запущен**

Самая частая проблема после перезагрузки сервера.

Решение:
- Команда в таблице: `start_proxifier`
- Или подключиться и запустить вручную
- Добавить в автозагрузку Windows (если не добавлен)
```

---

## Сообщение 11: Работа с тикетами хостеров

```
**Когда нужно обращаться в саппорт хостера:**

Типичные ситуации:
❌ Сервер внезапно недоступен (выключен)
❌ Очень медленная скорость
❌ Проблемы с сетью (не проходит трафик)
❌ Нужно что-то изменить (добавить диск, изменить конфиг)

**Как правильно открыть тикет:**

1. Собрать информацию:
   - IP сервера
   - Что именно не работает
   - Когда началась проблема
   - Что уже пробовали

2. Зайти в панель хостера через ADS Power профиль (важно!)

3. Открыть тикет в саппорт:
   - Описать проблему чётко и по делу
   - Приложить скриншоты если надо
   - Указать IP сервера

4. Обычно отвечают в течение 1-2 часов

5. После решения - задокументировать:
   - Добавить в FAQ (база знаний)
   - Обновить таблицу если что-то изменилось

**Важно:** Всегда входить в панель хостера через правильный ADS Power профиль,
чтобы IP входа совпадал с тем, с которого обычно работаешь с этим аккаунтом.
```

---

## Сообщение 12: Принципы работы и best practices

```
**Основные правила работы:**

📌 **Google Таблица - источник истины**
Любые изменения СРАЗУ фиксируются в таблице. Если этого не сделать,
через неделю никто не вспомнит, что менялось.

📌 **ADS Power профили - строго по назначению**
Каждый хостер/прокси = свой профиль. Не пытайся "быстро зайти" через обычный браузер.

📌 **Proxifier должен ВСЕГДА работать**
Это критично. Если Proxifier не запущен - сервер светит реальным IP хостера,
а не прокси. Это нарушает всю логику работы.

📌 **Мониторинг подскажет проблему**
Не нужно каждые 5 минут проверять сервера вручную. Если что-то не так -
придёт алерт в Telegram.

📌 **Документируй нестандартные случаи**
Столкнулся с проблемой первый раз? Решил? Запиши решение, чтобы в следующий
раз не тратить время.

📌 **Тестируй на тестовой среде**
Не уверен в команде? Сначала попробуй на тестовом сервере, потом на продакшне.

📌 **Бэкап важных настроек**
Перед изменением критичных настроек - сделай скриншот или сохрани конфиг.
Легче откатить, чем настраивать с нуля.
```

---

## Сообщение 13: Следующие шаги

```
**Что дальше:**

Я понимаю, что информации много 😅

Давайте действовать поэтапно:

1️⃣ **Сейчас (1-2 дня):**
   - Изучить документацию, которую я скинул
   - Посмотреть на архитектуру системы
   - Задать вопросы по непонятным моментам

2️⃣ **Далее (3-5 дней):**
   - Дам доступ к Google Таблице (только просмотр)
   - Покажу, как всё работает в реальности
   - Проведём call, где пройдёмся по системе

3️⃣ **Практика (1-2 недели):**
   - Создадим тестовую среду (2-3 дешёвых VPS)
   - Будешь сам настраивать, тестировать команды
   - Пройдёшь практические задания

4️⃣ **Реальная работа (постепенно):**
   - Начнём с простых задач на некритичных серверах
   - Постепенно расширим доступ
   - Через месяц-полтора выйдешь на полную самостоятельность

Не переживай, никто не ожидает, что ты всё поймёшь за день.
Главное - задавай вопросы, если что-то непонятно 👍

Готов начать? Есть вопросы по тому, что я сейчас рассказал?
```

---

## 📝 Дополнительно: Ответы на типичные вопросы

### Вопрос 1: "Зачем так сложно с ADS Power профилями?"

```
Отличный вопрос!

Проблема: хостеры и прокси-провайдеры отслеживают, с каких IP заходят в аккаунт.

Если сегодня ты зашёл в аккаунт с IP из России, а завтра с IP из США -
это выглядит подозрительно. Могут заблокировать аккаунт.

ADS Power решает это:
- Профиль автоматически использует нужный прокси
- Невозможно ошибиться и зайти не через тот IP
- Все куки и сессии изолированы

Альтернатива: держать в голове, через какой прокси открывать каждый аккаунт.
Рано или поздно ошибёшься → бан аккаунта → потеря доступа к серверам.
```

### Вопрос 2: "Можно ли управлять серверами через обычный RDP?"

```
Можно, но неэффективно.

Проблема:
- 50+ серверов
- Каждый раз подключаться по RDP → долго
- Рутинные операции (проверка статуса, перезапуск программы) → тратишь кучу времени

Решение:
- Автоматизация через WinRM + Google Таблица
- Команда в таблице → выполняется за 5 секунд на любом количестве серверов
- RDP используем только для визуальной диагностики и первичной настройки

Пример:
Нужно перезапустить Proxifier на 10 серверах.
- Через RDP: 10 подключений × 2 минуты = 20 минут
- Через таблицу: вписать команду в 10 строк = 1 минута
```

### Вопрос 3: "Что если мониторинг показывает ошибку, но я вижу, что всё ок?"

```
Бывают ложные срабатывания:

Причины:
1. Сервер был недоступен в момент проверки (временный сбой сети)
2. WinRM не ответил вовремя (таймаут)
3. Прокси провайдер глючил

Что делать:
1. Подключиться к серверу и проверить вручную
2. Если всё ОК - отправить команду `check` из таблицы (принудительная проверка)
3. Если проблема повторяется - копать глубже

Обычно мониторинг точный, но сеть - штука непредсказуемая.
```

---

## 🎯 Чек-лист: "Администратор готов к работе"

После онбординга администратор должен уметь:

- [ ] Объяснить, зачем нужен ADS Power
- [ ] Открыть правильный профиль для работы с хостером
- [ ] Прочитать статус сервера в Google Таблице
- [ ] Отправить команду на сервер через таблицу
- [ ] Подключиться к серверу по RDP/AnyDesk
- [ ] Проверить, запущен ли Proxifier и правильный ли IP
- [ ] Понять алерт из Telegram и принять решение
- [ ] Открыть тикет в саппорт хостера (если нужно)
- [ ] Настроить WinRM на новом сервере
- [ ] Добавить сервер в систему мониторинга

---

**Готово к использованию!**

Отправляйте сообщения последовательно с интервалом 10-30 минут,
давая администратору время на осмысление.
