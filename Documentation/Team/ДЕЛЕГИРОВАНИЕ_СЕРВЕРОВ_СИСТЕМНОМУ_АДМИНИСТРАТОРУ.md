# 🔧 Делегирование серверов системному администратору

> **Дата создания:** 24.01.2026
> **Назначение:** Безопасная передача управления несколькими серверами новому сисадмину

---

## 📋 Содержание

1. [Схема делегирования](#схема-делегирования)
2. [Этап 1: Подготовка данных](#этап-1-подготовка-данных)
3. [Этап 2: Создание таблицы для сисадмина](#этап-2-создание-таблицы-для-сисадмина)
4. [Этап 3: Настройка доступов](#этап-3-настройка-доступов)
5. [Этап 4: Инструкция для сисадмина](#этап-4-инструкция-для-сисадмина)
6. [Этап 5: Расширение доступа](#этап-5-расширение-доступа)

---

## 🎯 Схема делегирования

```
┌─────────────────────────────────────────────────────────┐
│         ГЛАВНАЯ ТАБЛИЦА (у тебя)                        │
│  • Все сервера (29 штук)                                │
│  • Полный контроль                                      │
│  • Чувствительные данные (прокси, хостеры, почты)       │
│  • Колонка "Выдать" для фильтрации                      │
└────────────────┬────────────────────────────────────────┘
                 │
                 │ IMPORTRANGE (только выбранные строки)
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│      ТАБЛИЦА СИСАДМИНА (ограниченный доступ)            │
│  • Только выбранные сервера (2-5 магазинов)             │
│  • Только необходимые колонки                           │
│  • Скрыты: логины хостеров, прокси-провайдеров, почты   │
│  • Доступ: Editor (может управлять серверами)           │
│  • Данные синхронизируются автоматически                │
└────────────────┬────────────────────────────────────────┘
                 │
                 │ Webhook → Linux VPS
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│         LINUX VPS (Сервер мониторинга)                  │
│  • Проверка выбранных серверов                          │
│  • Выполнение команд                                    │
│  • Обновление статусов в таблице сисадмина              │
└─────────────────────────────────────────────────────────┘
```

---

## 🔹 Этап 1: Подготовка данных

### 1.1. Выбор серверов для делегирования

**В главной таблице:**

1. Открой лист **"Сервера"**
2. Найди колонку **"Выдать"**
3. Выбери 2-5 магазинов для тестовой передачи сисадмину
4. В колонке "Выдать" поставь для них: **"Сисадмин"**

**Критерии выбора серверов:**
- ✅ Некритичные магазины (не основные)
- ✅ Стабильно работающие (без проблем)
- ✅ Разные хостеры (для тестирования)
- ❌ НЕ главные магазины
- ❌ НЕ проблемные сервера

**Пример заполнения:**

| Магазин | ... | Выдать | Статус машины |
|---------|-----|--------|---------------|
| Shop_01 | ... | Да     | Online        |
| Shop_02 | ... | **Сисадмин** | Online |
| Shop_03 | ... | Да     | Online        |
| Shop_04 | ... | **Сисадмин** | Online |
| Shop_05 | ... | **Сисадмин** | Online |

---

### 1.2. Создание формулы фильтрации

В главной таблице добавь вспомогательный лист **"Для_Сисадмина"**:

```
=QUERY(Сервера!A:AZ, "SELECT A,G,H,AE,AF,AG,AH,AI,AJ,AK,AL,AM,AN WHERE AW='Сисадмин'", 1)
```

**Что делает формула:**
- Фильтрует строки где в колонке "Выдать" стоит "Сисадмин"
- Выбирает только безопасные колонки (без логинов хостеров и прокси)

**Список безопасных колонок для сисадмина:**

| Колонка | Зачем нужна |
|---------|-------------|
| **A** - Магазин | Идентификация сервера |
| **G** - RDP IP:Username:Password | Доступ к серверу |
| **H** - AnyDesk | Удалённый доступ |
| **AE** - Статус машины | Мониторинг |
| **AF** - Статус прокси | Проверка прокси |
| **AG** - Текущий IP | Геолокация |
| **AH** - Текущий город | Геолокация |
| **AI** - Запущен anydesk | Статус программ |
| **AJ** - Запущен rustdesk | Статус программ |
| **AK** - Дата проверки | Время последней проверки |
| **AL** - Команда | Выполнение команд |
| **AM** - Результат проверки сервера | Логи |
| **AN** - Результат последней команды | Логи |

**Скрываем чувствительные данные:**
- ❌ Хостер логин/пароль
- ❌ Прокси-провайдер логин/пароль
- ❌ Proxyma API Keys
- ❌ Почты и пароли
- ❌ Прокси реквизиты (только результат работы)
- ❌ Финансовые данные (суммы оплаты)

---

## 🔹 Этап 2: Создание таблицы для сисадмина

### 2.1. Создание новой Google Sheets

1. Создай новую таблицу: **"Сервера - Сисадмин"**
2. Создай лист **"Сервера"** (точно такое же название!)
3. В ячейку A1 добавь формулу IMPORTRANGE:

```
=IMPORTRANGE("URL_ГЛАВНОЙ_ТАБЛИЦЫ", "Для_Сисадмина!A:M")
```

**Где взять URL:**
- Открой главную таблицу
- Скопируй URL из адресной строки
- Вставь в формулу вместо `URL_ГЛАВНОЙ_ТАБЛИЦЫ`

4. Разреши доступ при первом открытии (кнопка "Разрешить доступ")

---

### 2.2. Настройка интерфейса

**Форматирование:**

1. **Шапка таблицы:**
   - Строка 1: жирный шрифт, фон `#4a86e8`, белый текст
   - Закрепить строку 1 (Вид → Закрепить → 1 строку)

2. **Ширина колонок:**
   - Магазин: 100px
   - RDP: 200px
   - AnyDesk: 120px
   - Статусы: 120px каждый
   - IP и Город: 150px
   - Команда: 150px
   - Результаты: 300px

3. **Условное форматирование:**

   **Статус машины (зелёный = Online):**
   - Диапазон: D2:D1000
   - Условие: "Текст содержит" → "Online"
   - Цвет фона: `#d9ead3`

   **Статус машины (красный = ERROR):**
   - Диапазон: D2:D1000
   - Условие: "Текст содержит" → "ERROR"
   - Цвет фона: `#f4cccc`

---

### 2.3. Добавление Google Apps Script (для команд)

**Скопируй этот код в Apps Script редактор:**

1. Открой таблицу сисадмина
2. Расширения → Apps Script
3. Удали весь код
4. Вставь код ниже:

```javascript
var VPS_WEBHOOK_URL = "http://151.241.154.57:8080/execute_command";
var SHEET_NAME = "Сервера";

function onOpen() {
  var ui = SpreadsheetApp.getUi();
  ui.createMenu('🔧 Управление')
      .addItem('⚡ Выполнить команды', 'executeCommands')
      .addItem('🔍 Проверить сервера', 'checkAllServers')
      .addSeparator()
      .addItem('📊 Статистика', 'showStatistics')
      .addItem('🗑️ Очистить результаты', 'clearResults')
      .addToUi();
}

function executeCommands() {
  var ui = SpreadsheetApp.getUi();
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEET_NAME);
  var data = sheet.getDataRange().getValues();

  var rdpCol = 1;  // Колонка B (RDP)
  var cmdCol = 10; // Колонка K (Команда)

  var count = 0;

  for (var i = 1; i < data.length; i++) {
    var command = data[i][cmdCol];
    var rdp = data[i][rdpCol];

    if (command && command.toString().trim() !== '' && rdp) {
      try {
        var payload = {
          rdp: rdp.toString().trim(),
          command: command.toString().trim()
        };

        var options = {
          method: 'post',
          contentType: 'application/json',
          payload: JSON.stringify(payload),
          muteHttpExceptions: true
        };

        UrlFetchApp.fetch(VPS_WEBHOOK_URL, options);
        sheet.getRange(i+1, cmdCol+1).setValue('');
        count++;
        Utilities.sleep(500);
      } catch(err) {
        Logger.log('Error: ' + err);
      }
    }
  }

  ui.alert('✅ Выполнено команд: ' + count);
}

function checkAllServers() {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEET_NAME);
  var data = sheet.getDataRange().getValues();
  var rdpCol = 1;
  var count = 0;

  for (var i = 1; i < data.length; i++) {
    var rdp = data[i][rdpCol];
    if (rdp && rdp.toString().trim()) {
      try {
        var payload = {
          rdp: rdp.toString().trim(),
          command: 'check'
        };

        var options = {
          method: 'post',
          contentType: 'application/json',
          payload: JSON.stringify(payload),
          muteHttpExceptions: true
        };

        UrlFetchApp.fetch(VPS_WEBHOOK_URL, options);
        count++;
      } catch (error) {}
    }
  }

  SpreadsheetApp.getUi().alert('✅ Проверка запущена для ' + count + ' серверов');
}

function showStatistics() {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEET_NAME);
  var data = sheet.getDataRange().getValues();

  var total = 0, online = 0;

  for (var i = 1; i < data.length; i++) {
    if (data[i][1]) {
      total++;
      if (data[i][3] && data[i][3].toString().indexOf('Online') >= 0) online++;
    }
  }

  var ui = SpreadsheetApp.getUi();
  ui.alert('📊 Статистика',
           'Всего серверов: ' + total + '\n' +
           '✅ Онлайн: ' + online + '\n' +
           '❌ Проблемы: ' + (total - online),
           ui.ButtonSet.OK);
}

function clearResults() {
  var ui = SpreadsheetApp.getUi();
  var result = ui.alert('🗑️ Очистить результаты?', 'Продолжить?', ui.ButtonSet.YES_NO);

  if (result == ui.Button.YES) {
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEET_NAME);
    var lastRow = sheet.getLastRow();

    if (lastRow > 1) {
      sheet.getRange(2, 4, lastRow-1, 10).clearContent().clearFormat();
    }

    ui.alert('✅ Результаты очищены!');
  }
}
```

5. Сохрани скрипт (Ctrl+S)
6. Разреши права доступа при первом запуске

---

## 🔹 Этап 3: Настройка доступов

### 3.1. Предоставление доступа сисадмину

1. **Доступ к таблице сисадмина:**
   - Кнопка "Настроить доступ" → Добавить пользователя
   - Email сисадмина
   - Роль: **"Редактор"** (может вносить команды)
   - ✅ Отправить уведомление

2. **Доступ к главной таблице:**
   - **НЕ ДАВАТЬ!** Главная таблица остаётся только у тебя
   - IMPORTRANGE работает без прямого доступа

---

### 3.2. Настройка автообновления

**Главная таблица автоматически обновляет данные сисадмина через IMPORTRANGE:**

- ✅ Статусы обновляются в реальном времени
- ✅ Результаты команд появляются автоматически
- ✅ Мониторинг работает каждые 20 минут

**Что видит сисадмин:**
- ✅ Статусы серверов (Online/Offline)
- ✅ IP и геолокация
- ✅ Результаты команд
- ❌ НЕ видит логины хостеров
- ❌ НЕ видит логины прокси-провайдеров
- ❌ НЕ видит почты и пароли

---

## 🔹 Этап 4: Инструкция для сисадмина

### 📝 Сообщение для отправки сисадмину

```
Привет! Передаю тебе доступ к серверам для тестирования системы.

**Что я дал:**
- Доступ к Google Таблице с несколькими тестовыми серверами
- Реквизиты для подключения (RDP, AnyDesk)
- Возможность выполнять команды через таблицу

**Что нужно сделать:**

1️⃣ **Изучить систему (1-2 дня):**
   - Открой таблицу (ссылка ниже)
   - Посмотри как организованы данные
   - Попробуй подключиться к 1-2 серверам по RDP/AnyDesk
   - Проверь что программы запущены (Proxifier, AnyDesk)

2️⃣ **Протестировать команды (1 день):**
   - Попробуй отправить команду `check` через таблицу
   - Проверь результат в колонке "Результат проверки"
   - Попробуй команды: `start_proxifier`, `get_timezone`

3️⃣ **Написать анализ системы (2-3 дня):**
   - Что можно улучшить для стабильности?
   - Какие узкие места ты видишь?
   - Что можно автоматизировать дополнительно?
   - Какие метрики нужно отслеживать?

**Ссылка на таблицу:**
[вставь ссылку на таблицу сисадмина]

**Документация:**
[вставь ссылку на АРХИТЕКТУРА_СИСТЕМЫ_ОБЕЗЛИЧЕННАЯ.md]
[вставь ссылку на ИСПОЛЬЗУЕМЫЙ_СОФТ_И_ИНСТРУМЕНТЫ.md]

**Доступные команды:**
- `check` - полная проверка сервера
- `start_proxifier` - запустить Proxifier
- `stop_proxifier` - остановить Proxifier
- `restart_proxifier` - перезапустить
- `get_timezone` - показать таймзону
- `set_timezone_msk` - установить MSK
- `reboot` - перезагрузить сервер

**Важно:**
- Это тестовые сервера - можно экспериментировать
- Если что-то сломается - не страшно, главное запиши что делал
- Все действия логируются автоматически
- Вопросы задавай в любое время

Жду твой анализ через неделю. Если всё ОК - передам больше серверов.

Удачи! 🚀
```

---

## 🔹 Этап 5: Расширение доступа

### 5.1. Критерии готовности к расширению

**Сисадмин готов к большему доступу если:**

- [ ] Подключился ко всем тестовым серверам
- [ ] Выполнил команды успешно через таблицу
- [ ] Написал адекватный анализ системы
- [ ] Нашёл реальные точки улучшения
- [ ] Не сломал ничего критичного 😄
- [ ] Задавал правильные вопросы
- [ ] Понимает архитектуру системы

---

### 5.2. Постепенное расширение доступа

**Этап 1 (неделя 1):** 2-3 тестовых сервера
**Этап 2 (неделя 2):** 5-7 серверов (добавить некритичные)
**Этап 3 (неделя 3):** 10-15 серверов
**Этап 4 (месяц 1+):** Полный доступ к нужным серверам

**Как расширять:**

1. В главной таблице измени значение "Выдать" для новых серверов на "Сисадмин"
2. IMPORTRANGE автоматически подтянет новые строки
3. Сисадмин увидит новые сервера в своей таблице

---

### 5.3. Уровни доступа (если нужно разграничение)

**Уровень 1 - Просмотр:**
- Только чтение статусов
- Нет возможности выполнять команды

**Уровень 2 - Мониторинг:**
- Просмотр + команда `check`
- Не может перезагружать сервера

**Уровень 3 - Управление:**
- Все команды кроме критичных
- Не может менять прокси

**Уровень 4 - Полный доступ:**
- Все команды включая критичные
- Доступ к некоторым чувствительным данным

---

## 📊 Мониторинг действий сисадмина

### Что логируется автоматически:

1. **В листе "Логи" главной таблицы:**
   - Кто и когда выполнял команды
   - Результаты выполнения
   - Изменения статусов серверов

2. **На Linux VPS:**
   - Все команды через webhook
   - IP адрес отправителя
   - Время выполнения

3. **История версий Google Sheets:**
   - Кто и что изменил
   - Полная история изменений

---

## ✅ Чек-лист делегирования

**Перед передачей доступа:**

- [ ] Выбраны тестовые сервера (некритичные)
- [ ] Создана таблица "Для_Сисадмина" с формулой QUERY
- [ ] Создана новая таблица для сисадмина с IMPORTRANGE
- [ ] Настроен Google Apps Script в таблице сисадмина
- [ ] Проверено условное форматирование
- [ ] Подготовлена документация (ссылки)
- [ ] Написано сообщение для сисадмина
- [ ] Предоставлен доступ "Редактор" к таблице сисадмина
- [ ] Проверено что IMPORTRANGE работает корректно
- [ ] Проверено что команды выполняются через webhook

**После передачи:**

- [ ] Сисадмин получил доступ к таблице
- [ ] Сисадмин изучил документацию
- [ ] Проведён call с объяснением системы
- [ ] Сисадмин подключился к тестовым серверам
- [ ] Сисадмин выполнил первые команды
- [ ] Получен анализ системы от сисадмина
- [ ] Принято решение о расширении доступа

---

## 🔒 Безопасность

### Что скрыто от сисадмина:

- ❌ Логины и пароли хостеров
- ❌ Логины и пароли прокси-провайдеров
- ❌ Proxyma API Keys
- ❌ Почтовые аккаунты и пароли
- ❌ Финансовые данные (суммы оплаты, даты)
- ❌ Профили ADS Power (названия и настройки)

### Что доступно сисадмину:

- ✅ RDP доступы к серверам (только выданных)
- ✅ AnyDesk ID для подключения
- ✅ Статусы и результаты мониторинга
- ✅ Возможность выполнять команды
- ✅ История проверок (в пределах выданных серверов)

### В случае проблем:

1. **Отозвать доступ:**
   - Удалить сисадмина из списка редакторов таблицы
   - Доступ отключается мгновенно

2. **Сменить пароли:**
   - Поменять RDP пароли на выданных серверах
   - Обновить данные в главной таблице

3. **Провести аудит:**
   - Проверить историю изменений в Google Sheets
   - Проверить логи команд на Linux VPS
   - Проверить логи подключений к серверам

---

## 📞 Контрольные точки

**День 1:**
- Проверить что сисадмин получил доступ
- Ответить на первые вопросы

**День 3:**
- Узнать как идёт изучение системы
- Помочь с подключением если проблемы

**День 7:**
- Получить анализ системы
- Провести обсуждение предложений
- Принять решение о расширении доступа

**День 14:**
- Оценить работу за 2 недели
- Расширить доступ или скорректировать план

---

## 🎯 Ожидаемый результат

**После успешного делегирования:**

- ✅ Сисадмин понимает архитектуру системы
- ✅ Умеет подключаться к серверам и диагностировать проблемы
- ✅ Может выполнять рутинные операции самостоятельно
- ✅ Предложил улучшения для повышения стабильности
- ✅ Готов взять на себя мониторинг и обслуживание серверов
- ✅ Освобождено твоё время для партнёрских задач

---

**Готово к использованию!** 🚀

Следуй этой инструкции шаг за шагом и сможешь безопасно делегировать управление серверами.
