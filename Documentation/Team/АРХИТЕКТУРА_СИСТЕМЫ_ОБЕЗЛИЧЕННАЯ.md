# 🏗️ Архитектура системы управления серверами

> **Уровень доступа:** L0-L1 (Общедоступная версия без чувствительных данных)

---

## 📋 Общее описание

Система предназначена для централизованного управления множеством Windows VPS серверов с использованием прокси для обеспечения требуемой геолокации и изоляции.

---

## 🎯 Основные компоненты

```
┌─────────────────────────────────────────────────────────────┐
│                    УПРАВЛЯЮЩИЙ СЛОЙ                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Google       │  │  ADS Power   │  │  Telegram    │      │
│  │ Sheets       │  │  Profiles    │  │  Bot         │      │
│  │ (Интерфейс)  │  │  (Изоляция)  │  │  (Алерты)    │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                  │                  │               │
└─────────┼──────────────────┼──────────────────┼──────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────┐
│                  АВТОМАТИЗАЦИЯ                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Linux VPS (Сервер управления)                       │   │
│  │  • Google Apps Script Integration                    │   │
│  │  • WinRM Connector                                   │   │
│  │  • Server Monitor (каждые 20 минут)                 │   │
│  │  • Command Handler (Flask Webhook)                  │   │
│  └────────────────────┬──────────────────────────────────┘   │
└────────────────────────┼─────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    СЕТЕВОЙ СЛОЙ                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Прокси-провайдеры (SOCKS5)                          │   │
│  │  • Резидентские прокси                               │   │
│  │  • Выбор GEO / города                               │   │
│  │  • Ротация IP                                       │   │
│  └────────────────────┬──────────────────────────────────┘   │
└────────────────────────┼─────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  СЕРВЕРНЫЙ СЛОЙ                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Windows  │  │ Windows  │  │ Windows  │  │ Windows  │   │
│  │ VPS #1   │  │ VPS #2   │  │ VPS #3   │  │ VPS #N   │   │
│  │          │  │          │  │          │  │          │   │
│  │ Proxifier│  │ Proxifier│  │ Proxifier│  │ Proxifier│   │
│  │ AnyDesk  │  │ AnyDesk  │  │ AnyDesk  │  │ AnyDesk  │   │
│  │ WinRM ON │  │ WinRM ON │  │ WinRM ON │  │ WinRM ON │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 Детальное описание компонентов

### 1. Google Sheets (Центральный интерфейс)

**Назначение:**
- Единая точка управления всеми серверами
- Отображение статуса серверов в реальном времени
- Выполнение команд на серверах
- История всех операций

**Структура:**
- Список серверов с реквизитами доступа
- Колонки статуса (IP, GEO, работающие процессы, время последней проверки)
- Колонка для ввода команд
- Лог выполнения команд

**Интеграция:**
- Google Apps Script для отправки команд на Linux сервер
- Автообновление статусов каждые 20 минут
- Триггеры для отправки команд

---

### 2. ADS Power (Изоляция профилей)

**Назначение:**
- Изоляция доступов к разным хостерам и прокси-провайдерам
- Каждый профиль = изолированная сессия браузера с сохранёнными логинами
- Предотвращение случайных входов не в тот аккаунт

**Принцип работы:**
```
Профиль #1 → Хостер A + Прокси-провайдер A (через конкретный прокси)
Профиль #2 → Хостер B + Прокси-провайдер B (через другой прокси)
Профиль #N → Хостер N + Прокси-провайдер N
```

**Преимущества:**
- ✅ Невозможно перепутать аккаунты
- ✅ Каждый вход с правильного IP (прокси)
- ✅ Сохранение cookies и сессий
- ✅ Быстрое переключение между аккаунтами

---

### 3. Linux VPS (Сервер управления)

**Назначение:**
- Централизованная автоматизация
- Мониторинг всех Windows серверов
- Обработка команд из Google Sheets
- Отправка уведомлений в Telegram

**Основные модули:**

#### 3.1. Server Monitor (server_monitor.py)
- Проверяет все сервера каждые 20 минут
- Собирает данные: IP, GEO, запущенные процессы, таймзона
- Обновляет Google Sheets
- Отправляет алерты при проблемах

#### 3.2. Command Handler (command_handler.py)
- Flask webhook на порту 8080
- Принимает команды от Google Apps Script
- Выполняет команды через WinRM
- Возвращает результаты в Google Sheets

#### 3.3. WinRM Connector (winrm_connector.py)
- Модуль для подключения к Windows серверам
- Выполнение PowerShell команд удалённо
- Обработка ошибок и таймаутов

#### 3.4. Proxyma Monitor (proxyma_monitor.py)
- Мониторинг состояния прокси через API провайдера
- Проверка баланса, статуса прокси
- Автоматическое продление при необходимости

**Работа в режиме systemd сервисов:**
```bash
server-monitor.service       → Работает постоянно (каждые 20 мин)
command-webhook.service      → Flask приложение (всегда активно)
proxyma-monitor.timer        → Периодическая проверка прокси
```

---

### 4. Windows VPS (Управляемые сервера)

**Типичная конфигурация:**
- ОС: Windows Server 2019/2022 или Windows 10/11
- ОЗУ: 2-4 GB
- CPU: 1-2 ядра
- Локация: по требованию (разные страны/города)

**Установленное ПО:**

#### 4.1. Proxifier
**Назначение:**
- Прокси-клиент для маршрутизации всего трафика через SOCKS5
- Позволяет всем программам работать через прокси

**Конфигурация:**
- Прокси сервер: IP:Port:Username:Password (SOCKS5)
- Правила: весь трафик через прокси
- DNS через прокси

#### 4.2. AnyDesk
**Назначение:**
- Удалённый доступ к рабочему столу сервера
- Альтернатива RDP с лучшей производительностью

**Использование:**
- Визуальная диагностика проблем
- Настройка GUI приложений
- Проверка работы Proxifier визуально

#### 4.3. WinRM (Windows Remote Management)
**Назначение:**
- Удалённое выполнение PowerShell команд
- Автоматизация без GUI

**Настройка:**
```powershell
Enable-PSRemoting -Force
Set-Item WSMan:\localhost\Client\TrustedHosts -Value "*" -Force
```

**Порты:**
- 5985 (HTTP) - используется для автоматизации

---

### 5. Хостеры (Провайдеры VPS)

**Основные критерии выбора:**
- ✅ Поддержка Windows Server
- ✅ Почасовая оплата или гибкая биллинг
- ✅ Разные геолокации
- ✅ Адекватная стоимость (300-1000₽/месяц за сервер)
- ✅ Надёжность и uptime
- ✅ Быстрый саппорт

**Примеры характеристик хостеров:**
```
Хостер типа A:
├── Windows Server 2019/2022
├── 2 GB RAM, 1 vCPU
├── Локации: EU, US, RU
├── ~500₽/месяц

Хостер типа B:
├── Windows 10 Pro
├── 4 GB RAM, 2 vCPU
├── Локации: Asia, EU
├── ~800₽/месяц
```

**Процесс работы:**
1. Заказ сервера в панели хостера (через ADS Power профиль)
2. Получение реквизитов доступа (IP:Username:Password)
3. Первичная настройка через RDP
4. Добавление в систему мониторинга (Google Sheets)
5. Автоматизация через WinRM

---

### 6. Прокси-провайдеры (SOCKS5 Residential)

**Тип прокси:**
- Резидентские (Residential) - IP настоящих пользователей
- Протокол: SOCKS5 (всегда)
- Геотаргетинг: выбор страны и города

**Почему SOCKS5:**
- ✅ Работает на уровне сеанса (поддерживает любые протоколы)
- ✅ Поддержка UDP (для некоторых приложений)
- ✅ Авторизация по логин:паролю
- ✅ Меньше накладных расходов по сравнению с HTTP(S)

**Формат реквизитов:**
```
socks5://username:password@proxy-host:port
```

**Процесс настройки прокси на сервере:**
1. Получение реквизитов от провайдера (через ADS Power профиль)
2. Настройка Proxifier на Windows сервере
3. Проверка IP и GEO (должен совпадать с заказанным)
4. Мониторинг статуса прокси через API провайдера

**API интеграция:**
- Проверка баланса
- Статус активных прокси
- Ротация IP (если поддерживается)
- Автопродление подписки

---

## 🔄 Типичные сценарии работы

### Сценарий 1: Добавление нового сервера

```
1. Вход в панель хостера через ADS Power профиль
   └→ Заказ Windows VPS с нужной GEO

2. Подключение к серверу по RDP
   └→ IP:3389, логин:пароль из панели хостера

3. Первичная настройка
   ├→ Включить WinRM
   ├→ Установить Proxifier
   ├→ Установить AnyDesk
   └→ Настроить прокси в Proxifier

4. Добавление в Google Sheets
   └→ Формат: IP:Username:Password

5. Автоматическая проверка
   └→ Server Monitor через 20 минут обновит статус
```

---

### Сценарий 2: Смена прокси на сервере

```
1. Получение новых реквизитов прокси
   └→ Через ADS Power → прокси-провайдер → заказ нового прокси

2. Отправка команды через Google Sheets
   └→ Команда: set_proxy
   └→ Параметры: socks5://user:pass@host:port:GEO

3. Автоматическое выполнение
   ├→ Command Handler получает команду
   ├→ Подключается по WinRM к серверу
   ├→ Запускает PowerShell скрипт изменения Proxifier
   └→ Возвращает результат в Google Sheets

4. Проверка результата
   └→ Server Monitor через 20 минут проверит новый IP/GEO
```

---

### Сценарий 3: Диагностика проблемы

```
1. Обнаружение проблемы
   └→ Server Monitor показывает статус OFFLINE в Google Sheets

2. Первичная диагностика
   ├→ Попытка подключения через WinRM (автоматически)
   ├→ Проверка доступности сервера (ping)
   └→ Проверка статуса у хостера

3. Если сервер доступен, но есть проблемы
   ├→ Подключение через AnyDesk (визуальная диагностика)
   └→ Проверка Proxifier, процессов, логов

4. Если сервер недоступен
   ├→ Вход в панель хостера через ADS Power профиль
   ├→ Проверка статуса сервера (работает/выключен)
   └→ Открытие тикета в саппорт при необходимости

5. Документирование проблемы и решения
   └→ Добавление в FAQ для будущих случаев
```

---

### Сценарий 4: Массовая операция

```
1. Задача: обновить таймзону на 10 серверах

2. Через Google Sheets
   ├→ Выбрать 10 строк с серверами
   ├→ Заполнить команду: set_timezone_msk
   └→ Запустить обработку

3. Автоматическое выполнение
   ├→ Google Apps Script отправляет 10 команд на Linux сервер
   ├→ Command Handler обрабатывает очередь
   ├→ Выполнение на каждом сервере через WinRM
   └→ Результаты записываются обратно в Google Sheets

4. Проверка результатов
   └→ Server Monitor обновит данные таймзон через 20 минут
```

---

## 🛡️ Безопасность и изоляция

### Уровни изоляции:

**1. Профили ADS Power**
- Каждый хостер/прокси-провайдер = отдельный профиль
- Изоляция cookies и сессий
- Разные прокси для входа в панели

**2. Разные аккаунты хостеров**
- Нет единой точки отказа
- Диверсификация рисков

**3. WinRM подключения**
- Логин:пароль авторизация
- Нет публичного доступа (только с Linux сервера управления)

**4. Реквизиты в Google Sheets**
- Доступ только у доверенных лиц
- Логирование всех изменений

---

## 📊 Мониторинг и алертинг

### Что отслеживается:

**Каждые 20 минут:**
- Доступность серверов (ping, WinRM)
- Внешний IP и геолокация
- Запущенные процессы (Proxifier, AnyDesk)
- Таймзона и время на сервере
- Установленные языки

**При обнаружении проблем:**
- ❌ Сервер не отвечает → Telegram алерт
- ❌ Неверный IP/GEO → Telegram алерт
- ❌ Proxifier не запущен → Telegram алерт
- ❌ Неверная таймзона → Запись в лог (не критично)

---

## 🔧 Доступные команды

### Категория: Мониторинг
- `check` - Полная проверка сервера

### Категория: Таймзона
- `get_timezone` - Показать текущую таймзону
- `set_timezone_msk` - Установить MSK (UTC+3)
- `set_timezone_ekt` - Установить EKT (UTC+5)

### Категория: Языки
- `get_languages` - Показать установленные языки
- `set_lang_russian` - Установить русский
- `set_lang_english` - Установить английский

### Категория: Программы
- `start_proxifier` - Запустить Proxifier
- `stop_proxifier` - Остановить Proxifier
- `restart_proxifier` - Перезапустить Proxifier
- `start_anydesk` - Запустить AnyDesk

### Категория: Прокси (новая функциональность)
- `set_proxy` - Сменить прокси на сервере
- `get_proxy` - Получить текущий прокси

### Категория: Система
- `reboot` - Перезагрузить сервер

---

## 🎓 Ключевые концепции для понимания

### 1. Почему нужна изоляция через ADS Power?
- Каждый вход в панель хостера должен быть с "правильного" IP (через прокси)
- Нельзя входить в аккаунт хостера A с IP прокси, используемого для хостера B
- ADS Power автоматически использует нужный прокси для каждого профиля

### 2. Почему WinRM, а не только RDP/AnyDesk?
- WinRM = автоматизация без GUI
- RDP/AnyDesk = для ручных действий и диагностики
- WinRM позволяет управлять сотнями серверов автоматически

### 3. Почему резидентские прокси, а не датацентр?
- Резидентские прокси выглядят как обычные пользователи
- Датацентр прокси часто в бан-листах
- Для работы с некоторыми сервисами нужны именно residential

### 4. Почему Google Sheets, а не специализированная система?
- Низкий порог входа (все умеют в таблицы)
- Гибкость кастомизации
- Визуальный интерфейс
- Интеграция через Apps Script
- Бесплатно

---

## 📈 Масштабирование

### Текущая архитектура поддерживает:
- До 100+ Windows VPS серверов
- Множественные хостеры (нет лимита)
- Множественные прокси-провайдеры
- Автоматизация через один Linux сервер

### Узкие места:
- Google Sheets Apps Script (ограничения на количество запросов)
- WinRM подключения (последовательная обработка команд)

### Возможные улучшения:
- Параллельная обработка команд
- Кэширование статусов
- Более частый мониторинг критичных серверов
- Интеграция с другими системами мониторинга

---

## 🎯 Чек-лист понимания архитектуры

Вы понимаете систему, если можете ответить на вопросы:

- [ ] Зачем нужен ADS Power и как он работает?
- [ ] В чём разница между WinRM, RDP и AnyDesk?
- [ ] Почему используется SOCKS5, а не HTTP прокси?
- [ ] Как происходит автоматическая проверка серверов?
- [ ] Что происходит при отправке команды из Google Sheets?
- [ ] Как диагностировать проблему с сервером?
- [ ] Какова роль Linux сервера в архитектуре?
- [ ] Зачем нужна изоляция профилей для каждого хостера?

---

**Следующий шаг:** После изучения архитектуры переходите к документу **ИСПОЛЬЗУЕМЫЙ_СОФТ_И_ИНСТРУМЕНТЫ.md** для детального разбора каждого компонента.
