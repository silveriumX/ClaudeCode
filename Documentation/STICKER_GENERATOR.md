# Генератор наклеек для Wildberries

> Автоматическая генерация наклеек на основе дизайна из Figma

**Дата создания:** 15.02.2026
**Оригинальный дизайн:** https://www.figma.com/design/3kO5tFdr5j8L46Ot2yBLRR/test-for-mcp

---

## 🎨 Что создаёт

Генератор создаёт наклейки размером **1748x550px** для Wildberries с:
- Градиентным фоном
- Радиальным свечением
- Бэйджем с количеством товара
- Тенями и эффектами

---

## 🚀 Быстрый старт

### Вариант 1: Через батник (самый простой)

**Двойной клик на:**
```
Scripts\generate_wildberries_stickers.bat
```

Автоматически создаст:
- `nakleiyka-4sht-green.png` - Зелёная (4 шт)
- `nakleiyka-6sht-blue.png` - Синяя (6 шт)
- `nakleiyka-original.png` - Оригинал

**Результат:** Папка `exports/stickers/` откроется автоматически

---

### Вариант 2: Через Python скрипт

```bash
cd Scripts
python create_stickers.py
```

---

### Вариант 3: HTML генератор (интерактивный)

**Открыть в браузере:**
```
Scripts\generate_stickers.html
```

Позволяет:
- Просмотреть все версии наклеек
- Скачать PNG напрямую из браузера
- Изменить цвета в real-time (через DevTools)

---

## 🎨 Цветовые схемы

### Доступные схемы:

**Зелёная (green):**
```python
gradient_start: (0, 200, 81)    # Яркий зелёный
gradient_end: (0, 150, 60)      # Темнее зелёный
glow: (0, 255, 100)             # Яркое зелёное свечение
```

**Синяя (blue):**
```python
gradient_start: (0, 123, 255)   # Яркий синий
gradient_end: (0, 90, 200)      # Темнее синий
glow: (100, 150, 255)           # Яркое синее свечение
```

**Красно-желтая (red-yellow)** - оригинал:
```python
gradient_start: (255, 199, 0)   # Желтый
gradient_end: (255, 100, 0)     # Оранжево-красный
glow: (255, 220, 100)           # Желтое свечение
```

---

## ✏️ Кастомизация

### Создать наклейку для другого количества

**Пример: Наклейка "8 шт" в фиолетовых тонах**

Открыть `Scripts/create_stickers.py`, добавить в конец:

```python
# Фиолетовая - 8 шт (добавить в colors dict)
colors["purple"] = {
    "gradient_start": (138, 43, 226),  # Фиолетовый
    "gradient_end": (75, 0, 130),      # Индиго
    "glow": (186, 85, 211, 200)        # Яркое фиолетовое свечение
}

# Создать наклейку
create_sticker(
    output_dir / "nakleiyka-8sht-purple.png",
    color_scheme="purple",
    quantity="8 шт"
)
```

Запустить:
```bash
python create_stickers.py
```

---

### Изменить размер наклейки

По умолчанию: **1748x550px** (оптимально для Wildberries)

Для других размеров:

```python
create_sticker(
    output_dir / "custom-size.png",
    color_scheme="green",
    quantity="4 шт",
    size=(2000, 600)  # Ширина x Высота
)
```

---

### Изменить текст

Можно использовать любой текст:

```python
# "SALE 50%"
create_sticker(
    output_dir / "sale-50.png",
    color_scheme="red-yellow",
    quantity="SALE 50%"
)

# "ХИТ"
create_sticker(
    output_dir / "hit.png",
    color_scheme="green",
    quantity="ХИТ"
)

# "НОВИНКА"
create_sticker(
    output_dir / "new.png",
    color_scheme="blue",
    quantity="NEW"
)
```

---

## 🎯 Примеры использования

### Создать набор наклеек для всех комплектов

```python
from pathlib import Path
from create_stickers import create_sticker

output = Path("exports/stickers/sets")

# Комплекты разных размеров
sets = [
    (2, "green"),
    (4, "blue"),
    (6, "purple"),
    (8, "orange"),
    (10, "red")
]

for quantity, color in sets:
    create_sticker(
        output / f"set-{quantity}pcs-{color}.png",
        color_scheme=color,
        quantity=f"{quantity} шт"
    )

print("Создано 5 наклеек для разных комплектов")
```

---

### Batch генерация с данными из CSV

```python
import csv
from pathlib import Path
from create_stickers import create_sticker

# CSV формат: quantity,color,filename
with open('stickers.csv', 'r', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    for row in reader:
        create_sticker(
            Path(f"exports/{row['filename']}"),
            color_scheme=row['color'],
            quantity=row['quantity']
        )
```

**stickers.csv:**
```csv
quantity,color,filename
4 шт,green,nakleiyka-4-green.png
6 шт,blue,nakleiyka-6-blue.png
10 шт,purple,nakleiyka-10-purple.png
```

---

## 🔧 Технические детали

### Зависимости

```bash
pip install Pillow
```

**Pillow** используется для:
- Создания изображений
- Рисования градиентов
- Применения эффектов (тени, blur)
- Работы с текстом и шрифтами

### Структура проекта

```
Scripts/
├── create_stickers.py          # Python генератор
├── generate_stickers.html      # HTML интерактивный генератор
└── generate_wildberries_stickers.bat  # Батник для быстрого запуска

exports/
└── stickers/                   # Готовые наклейки
    ├── nakleiyka-4sht-green.png
    ├── nakleiyka-6sht-blue.png
    └── nakleiyka-original.png
```

---

## 🎨 Как это работает

### 1. Анализ оригинала из Figma

Через Figma API:
- Получаем структуру дизайна
- Извлекаем цвета градиентов
- Определяем размеры элементов
- Экспортируем референс

### 2. Генерация в Python

```python
# 1. Создать холст
img = Image.new('RGBA', (1748, 550))

# 2. Нарисовать градиентный фон
for x in range(width):
    # Линейный градиент слева направо
    draw.line([(x, 0), (x, height)], fill=color)

# 3. Добавить радиальное свечение
glow_layer = Image.new('RGBA', size)
glow_layer = glow_layer.filter(GaussianBlur(radius=30))

# 4. Наложить белый бэйдж
draw.rounded_rectangle([x, y, x+w, y+h], radius=15, fill=white)

# 5. Добавить текст
draw.text((x, y), "4 шт", font=font, fill=black)

# 6. Сохранить PNG
img.save(output_path)
```

---

## 📋 Требования Wildberries к изображениям

```
Формат: PNG, JPEG
Размер: 900x1200px (основной) или 1748x550px (баннеры)
Минимум: 450x600px
Максимум: 8000x8000px
Вес: до 10 МБ
Фон: белый (#FFFFFF) или прозрачный
```

**Наши наклейки:**
- Формат: PNG ✓
- Размер: 1748x550px ✓
- Вес: ~100-200 КБ ✓
- Прозрачный фон: ✓ (можно использовать как overlay)

---

## 🚀 Workflow: От Figma к готовым наклейкам

### Полный процесс:

```
1. Дизайн в Figma
   - Создать оригинальный дизайн
   - Настроить градиенты, цвета, эффекты
   - Сохранить как референс

2. Анализ через Figma API
   - Python скрипт читает структуру
   - Извлекает цвета RGB
   - Получает размеры элементов

3. Генерация вариаций
   - Python создаёт версии в разных цветах
   - Применяет градиенты и эффекты
   - Добавляет текст

4. Экспорт PNG
   - Размер 1748x550px
   - Прозрачный фон
   - Оптимизированный вес

5. Использование
   - Загрузка на Wildberries
   - Применение к карточкам товаров
```

**Время на создание 10 наклеек:** ~30 секунд

---

## 💡 Советы и лучшие практики

### Цвета для маркетплейсов

**Зелёный:**
- Экологичность
- Натуральность
- Скидки / Акции

**Синий:**
- Надёжность
- Качество
- Премиум сегмент

**Красный/Оранжевый:**
- Срочность
- Горячие предложения
- Ограниченное количество

**Фиолетовый:**
- Эксклюзивность
- Новинки
- Премиум

### Текст на наклейках

**Хорошие примеры:**
- "4 шт" - количество в комплекте
- "SALE 50%" - размер скидки
- "ХИТ" - популярный товар
- "NEW" - новинка
- "-30%" - процент скидки

**Плохие примеры:**
- Длинный текст (не поместится)
- Мелкий шрифт (нечитаемо)
- Слишком много информации

---

## 🔄 Автоматизация (продвинутый уровень)

### Интеграция с Google Sheets

```python
import gspread
from create_stickers import create_sticker

# Подключиться к Google Sheets
gc = gspread.service_account()
sh = gc.open("Товары Wildberries")
worksheet = sh.sheet1

# Получить данные
products = worksheet.get_all_records()

for product in products:
    if product['needs_sticker']:
        create_sticker(
            Path(f"exports/{product['sku']}-sticker.png"),
            color_scheme=product['color_scheme'],
            quantity=product['quantity_text']
        )
```

---

## 📚 Связанные документы

- `FIGMA_MCP_SETUP.md` - Настройка Figma MCP
- `FIGMA_MARKETPLACE_CARDS.md` - Карточки товаров для маркетплейса
- `DESIGN_SYSTEM.md` - Дизайн-система проекта

---

## ❓ FAQ

### Q: Можно ли изменить шрифт?

**A:** Да, в `create_stickers.py` измените:

```python
font_path = "C:/Windows/Fonts/arialbd.ttf"  # Ваш шрифт
font = ImageFont.truetype(font_path, 120)   # Размер
```

### Q: Как добавить свой цвет?

**A:** В `create_stickers.py` добавьте в словарь `colors`:

```python
colors["orange"] = {
    "gradient_start": (255, 140, 0),
    "gradient_end": (255, 69, 0),
    "glow": (255, 165, 0, 200)
}
```

### Q: Можно ли использовать для других маркетплейсов?

**A:** Да, измените размер:
- Ozon: `size=(1200, 1200)`
- Яндекс.Маркет: `size=(1200, 1600)`
- Alibaba: `size=(800, 800)`

### Q: Как добавить логотип?

**A:** В скрипт добавьте:

```python
logo = Image.open('logo.png')
img.paste(logo, (x, y), logo)  # С прозрачностью
```

---

**Автор:** AI Assistant (Claude Sonnet 4.5)
**Дата создания:** 15.02.2026
**Версия:** 1.0
